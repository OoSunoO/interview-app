repos:
  - repo: local
    hooks:
      - id: prettier
        name: prettier
        entry: bash -c 'cd frontend && for f in "$@"; do npx prettier --write "${f#frontend/}"; done' --
        language: system
        files: ^frontend/src/
        types: [text]

repos:
  - repo: local
    hooks:
      - id: prettier
        name: prettier
        entry: bash -c 'cd frontend && npx prettier --check src/'
        language: system
        files: ^frontend/src/
        types_or: [javascript, css, json]

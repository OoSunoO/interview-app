export const knowledge = {
  设计模式: {
    category: "design_patterns",
    content: `## 设计模式核心概念

### 创建型模式

**单例模式**：确保一个类只有一个实例，提供全局访问点。
- 饿汉式：类加载时创建，线程安全。
- 懒汉式：使用时创建，需加锁。
- DCL（双重检查锁定）：volatile + synchronized。
- 静态内部类：利用类加载机制保证线程安全。

**工厂模式**：将对象创建逻辑封装，客户端通过工厂获取对象。
- 简单工厂：一个工厂类，根据参数创建不同产品。
- 工厂方法：每个产品对应一个工厂，符合开闭原则。
- 抽象工厂：创建产品族，支持多个产品等级。

**建造者模式**：将复杂对象的构建与表示分离，一步步构建。
- 适用：对象有多部分组成、构建过程稳定。
- 经典应用：Lombok @Builder。

**原型模式**：通过克隆创建对象，避免重复初始化。
- 浅克隆：复制基本类型，引用类型共享。
- 深克隆：复制所有属性。

### 结构型模式

**代理模式**：为对象提供代理以控制访问。
- 静态代理：手动编写代理类。
- 动态代理：JDK Proxy（接口）或 CGLIB（类）。
- 应用：AOP、延迟加载、访问控制。

**适配器模式**：将一个类的接口转换为客户端期望的另一个接口。
- 类适配器：继承 + 实现。
- 对象适配器：组合 + 实现（更灵活）。

**装饰器模式**：动态给对象添加新功能，替代继承。
- 经典应用：Java IO 流（BufferedInputStream 装饰 FileInputStream）。

**外观模式**：为子系统提供统一的高层接口。
- 经典应用：Slf4J（日志门面）。

### 行为型模式

**策略模式**：定义算法族，使其可互相替换。
- 经典应用：Comparator、线程池拒绝策略。

**观察者模式**：定义一对多依赖，状态变化时通知所有观察者。
- 经典应用：Spring ApplicationListener、RxJava。

**模板方法**：定义算法骨架，子类实现具体步骤。
- 经典应用：Spring JdbcTemplate、AQS。

**责任链模式**：多个处理对象组成链，请求沿链传递直到被处理。
- 经典应用：Spring Security Filter Chain、Servlet Filter。`,
    source: "GoF / 综合整理",
    domain: "design_patterns",
  },
};

export const knowledge = {
  架构: {
    category: "system_design",
    content: `## 系统架构设计

### 架构模式

**单体架构**：所有功能打包在一个应用中。
- 优点：开发简单，部署单一。
- 缺点：团队协作困难，难以扩展。

**分层架构**：将系统分为展示层、业务层、持久层等。
- 典型代表：Spring MVC（Controller → Service → DAO）。

**微服务架构**：将应用拆分为一系列小的、独立的服务。
- 每个服务独立开发、部署、扩展。
- 挑战：服务治理、数据一致性、运维复杂度。

**事件驱动架构**：组件通过事件异步通信。
- 松耦合、高扩展、支持 CQRS 和 Event Sourcing。

### 分层设计原则
- **单一职责**：每层只关注自己的职责。
- **依赖倒置**：上层依赖抽象接口而非具体实现。
- **接口隔离**：不强迫调用者依赖不需要的方法。
- **无环依赖**：避免循环依赖。`,
    source: "JavaGuide",
    domain: "system_design_main",
  },
  微服务架构: {
    category: "microservices",
    content: `## 微服务架构核心概念

### 服务拆分原则
- **领域驱动设计（DDD）**：按限界上下文拆分。
- **单一职责**：每个服务只负责一个业务能力。
- **无共享**：数据库不共享，通过 API 交换数据。
- **自治性**：服务可独立开发、测试、部署、扩展。

### 服务通信
- **同步**：REST / gRPC。简单直接，但会产生调用链依赖。
- **异步**：消息队列（Kafka / RabbitMQ）。解耦削峰，增加最终一致性复杂度。

### 微服务痛点
| 问题 | 解决方案 |
|------|---------|
| 配置管理 | Nacos / Apollo / Consul |
| 服务发现 | Eureka / Nacos / Kubernetes Service |
| 负载均衡 | Ribbon / Spring Cloud LoadBalancer |
| 熔断降级 | Sentinel / Resilience4j |
| 分布式事务 | Seata（AT / TCC / Saga） |
| 分布式追踪 | Jaeger / Zipkin / Skywalking |
| 日志聚合 | ELK / Loki + Grafana |
| 监控告警 | Prometheus + Grafana |

### 微服务设计模式
- **API 网关**：统一入口，路由、认证、限流。
- **熔断器（Circuit Breaker）**：快速失败防止级联雪崩。
- **舱壁隔离（Bulkhead）**：资源池隔离。
- **Saga**：长事务拆分为本地事务 + 补偿操作。
- **Sidecar**：服务治理能力剥离到独立进程（Istio / Linkerd）。`,
    source: "综合整理",
    domain: "microservices",
  },
  系统设计: {
    category: "system_design",
    content: `## 系统设计核心要点

### 设计流程
1. **需求澄清**：功能需求 + 非功能需求（QPS、延迟、可用性、一致性）。
2. **估算**：QPS、存储量、带宽。
3. **数据模型设计**：ER 图，选择存储系统。
4. **高层设计**：系统架构图，核心模块划分。
5. **详细设计**：深入关键组件，权衡取舍。

### 关键指标
| 指标 | 说明 | 目标参考 |
|------|------|---------|
| QPS | 每秒查询数 | 单机 ~1k-2k（MySQL），~5-10k（Redis） |
| 延迟 | 请求响应时间 | <100ms 优秀 |
| P99 延迟 | 99% 请求的延迟上界 | <200ms 优秀 |
| 可用性 | 正常运行时间比 | 99.9%（3 个 9）≈ 8.7h/年 |

### 常见系统设计题

#### 短链服务（TinyURL）
- 核心操作：create(长链) → 短链；访问短链 → 302 重定向。
- 发号器：自增 ID → Base62 编码（6-7 位）。
- 读写比 ≈ 1:10000，缓存热点短链。

#### 分布式 KV 存储
- 一致性哈希解决数据分片和扩缩容。
- 虚拟节点减少数据倾斜。
- Hinted Handoff + Read Repair 保证最终一致性。

#### 限流设计
- **令牌桶**：固定速率放入令牌，突发时消耗累积令牌。
- **漏桶**：固定速率流出，超出的请求排队或丢弃。
- **滑动窗口**：在时间窗口内计数。

### 数据库扩展策略
- **读写分离**：主库写，从库读。
- **分库分表**：按业务分库（垂直），按 ID 范围/哈希分表（水平）。
- **CQRS**：命令和查询分离。

### 一致性方案
- **强一致性**：分布式事务（2PC / 3PC），性能低。
- **最终一致性**：消息队列 + 重试 + 对账。
- **补偿事务**：Saga。
- **共识算法**：Paxos / Raft。`,
    source: "综合整理",
    domain: "system_design_main",
  },
};

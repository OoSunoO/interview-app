// Auto-generated by scripts/generate-data.js. Do not edit.
// Category: java — 64 questions
export const category = "java";
export const questions = [{"category":"java","difficulty":"hard","type":"short_answer","title":"JIT 编译优化技术","content":"请介绍 HotSpot JIT 编译器的几种重要优化技术。逃逸分析（Escape Analysis）如何实现栈上分配和锁消除？方法内联（Inlining）的触发条件是什么？","answer":"答案：JIT（Just-In-Time）编译器将热点代码编译为机器码，并在编译过程中应用多种优化。\n\n逃逸分析（Escape Analysis）：\n- 分析对象的作用域是否逃逸出方法或线程\n- 不逃逸的优化：栈上分配（Stack Allocation）——对象直接在栈帧中分配，无需 GC\n- 不逃逸的优化：锁消除（Lock Elision）——去掉不必要的同步锁\n- 不逃逸的优化：标量替换（Scalar Replacement）——将对象拆散为多个局部变量\n- -XX:+DoEscapeAnalysis 开启（JDK 8+ 默认）\n\n方法内联（Inlining）：\n- 将调用方法的字节码直接嵌入到当前方法的编译代码中\n- 减少方法调用开销（栈帧创建、参数传递）\n- 触发条件：方法足够小（-XX:MaxInlineSize=35 字节）、热点代码（调用计数超过阈值）、final 方法更易内联\n- -XX:CompileThreshold=10000（混合模式下方法调用计数阈值）\n- 内联后的代码可以做更多优化（因为去除了调用边界）\n\n其他优化技术：\n1. 循环优化：循环展开（Loop Unrolling）、循环剥离（Loop Peeling）\n2. 死代码消除（Dead Code Elimination）\n3. 常量折叠与传播（Constant Folding & Propagation）\n4. 公共子表达式消除（Common Subexpression Elimination）\n\n扩展延伸：C1（Client）编译器做轻量快速编译，C2（Server）编译器做深度优化编译。分层编译（Tiered Compilation）结合两者优势——方法先被 C1 快速编译，热点达到阈值后被 C2 重新深度编译。Graal JIT 是新一代 JIT 编译器，支持更激进的优化（如部分逃逸分析）。","hints":["逃逸分析→栈上分配、锁消除、标量替换","方法内联→MaxInlineSize=35，CompileThreshold=10000"],"tags":["Java","JIT","逃逸分析","方法内联"],"id":1761},{"category":"java","difficulty":"hard","type":"short_answer","title":"虚拟线程原理与限制","content":"请深入解释 JDK 21 虚拟线程（Virtual Threads，Project Loom）的实现原理。虚拟线程如何实现轻量级调度？它在哪些场景下不能提升性能？","answer":"答案：虚拟线程是 JDK 21 正式发布的轻量级用户态线程，由 JVM 调度而非 OS。\n\n实现原理：\n1. 虚拟线程由 JVM 的 Carrier Thread（载体线程，普通的平台线程）调度执行\n2. 当虚拟线程执行阻塞操作（如 I/O、锁、Thread.sleep）时，会自动从 Carrier Thread 上 unmount（卸载），Carrier Thread 转而执行其他虚拟线程\n3. 阻塞操作完成后，虚拟线程重新 mount 到 Carrier Thread 继续执行\n4. 这个 unmount/mount 操作称为 Continuation（协程挂起恢复），是虚拟线程的核心机制\n\n调度模型：M:N 调度——M 个虚拟线程映射到 N 个 Carrier Thread（通常 N = CPU 核心数）。\n\n适用场景：\n- 大量并发 I/O 操作（Web 服务器、数据库客户端、微服务间调用）\n- 请求-响应模型（每个请求一个虚拟线程）\n\n不能提升性能的场景：\n1. CPU 密集型计算：虚拟线程不能并行利用 CPU（并行需要平台线程，虚拟线程在载体线程上串行执行）\n2. synchronized 块中使用：虚拟线程在 synchronized 块中执行阻塞操作时不能 unmount（固定到载体线程），会阻塞整个载体线程\n3. 使用原生方法（JNI）：执行 native 代码时会固定到载体线程\n4. 存在锁竞争的热点代码：当多个虚拟线程竞争同一把锁时，载体线程也会被阻塞\n\n扩展延伸：JDK 21 的虚拟线程是预览特性，JDK 21 正式转正。使用 ReentrantLock 替代 synchronized（synchronized 会固定虚拟线程）。虚拟线程的协程机制基于 Continuation，类似 Go 的 goroutine 和 Kotlin 的协程。虚拟线程的栈大小初始很小（~10KB）并按需扩展，支持千万级并发线程。","hints":["虚拟线程在阻塞时自动从 Carrier Thread 卸载（unmount）","synchronized 和 CPU 密集型任务会固定虚拟线程，无法发挥优势"],"tags":["Java","虚拟线程","Project Loom","协程"],"id":1762},{"category":"java","difficulty":"medium","type":"short_answer","title":"Foreign Function & Memory API","content":"请介绍 JDK 22 Foreign Function & Memory API（Project Panama）的原理和用法。它比 JNI 的优势在哪里？如何安全地操作堆外内存？","answer":"答案：FFM API（Foreign Function & Memory API）替代 JNI，提供纯 Java 调用原生代码和操作堆外内存的能力。\n\n核心组件：\n1. Arena：内存生命周期管理器。分配的内存自动在 Arena 关闭时释放（类似 try-with-resources）\n2. MemorySegment：堆外或堆内的连续内存区域。提供类型安全的内存访问。\n3. MemoryLayout：定义内存布局（字节对齐、字段偏移量），确保跨平台的兼容性\n4. Linker：原生函数链接器，将 Java 方法调用链接到 C 函数\n5. SymbolLookup：查找原生库中的符号（函数地址）\n\n比 JNI 的优势：\n1. 消除 JNI 的胶水代码（不再需要 javah 生成头文件，不需要手写 C 桩函数）\n2. 类型安全：MemoryLayout 和 VarHandle 提供类型安全的内存访问，JNI 的 GetIntArrayElement 等不检查边界\n3. 性能更好：FFM API 的内联优化比 JNI 的跨边界调用更高效\n4. 自动内存管理：Arena 关闭时自动释放内存（类似 RAII），JNI 需要手动 Release 或 Delete\n5. 支持堆外内存的直接操作（DirectByteBuffer 的现代替代方案）\n\n安全操作堆外内存：\n1. 使用 Arena 限定内存生命周期（避免 dangling pointer）\n2. MemoryLayout 确保正确的字节对齐和字段布局\n3. MemoryAccess 提供类型安全的 get/set 方法\n4. 通过 MemorySegment 的边界检查防止缓冲区溢出\n\n扩展延伸：FFM API 在 JDK 19 预览，JDK 22 转正。标准库中已有使用——如 java.util.zip 使用 FFM API 调用 zlib。典型应用场景：调用 C 库（如 TensorFlow C API、libfuse）、操作共享内存、实现序列化协议。","hints":["FFM API = Arena + MemorySegment + MemoryLayout + Linker","优势：无胶水代码、类型安全、自动内存管理、性能更好"],"tags":["Java","FFM","panama","JNI 替代"],"id":1763},{"category":"java","difficulty":"medium","type":"short_answer","title":"作用域值与结构化并发","content":"请介绍 JDK 21 的 ScopedValue（作用域值）和 StructuredTaskScope（结构化并发）。ScopedValue 与 ThreadLocal 的区别是什么？结构化并发如何改进线程管理？","answer":"答案：ScopedValue 和 StructuredTaskScope 是 JDK 21 的预览特性，分别改进上下文传递和并发任务管理。\n\nScopedValue：\n- 不可变的、限定作用域的上下文值传递机制\n- 通过 ScopedValue.where(key, value).run(() -> { ... }) 绑定值\n- 在 run 方法的执行边界内，绑定的值对调用栈可见\n- 子任务自动继承父作用域的 ScopedValue（无需手动传递）\n\nScopedValue vs ThreadLocal：\n1. 不可变性：ScopedValue 绑定后不可修改（final 语义），ThreadLocal 可随意 set/get\n2. 生命周期：ScopedValue 绑定到代码块的作用域（try-with-resources），ThreadLocal 绑定到线程的整个生命周期\n3. 继承性：虚拟线程场景下，ScopedValue 自动传递到子任务，ThreadLocal 在虚拟线程中需要手动传递\n4. 内存：ScopedValue 的副本数 = 载体线程数（少量），ThreadLocal 的副本数 = 线程数（可能大量）\n5. 安全性：ScopedValue 是只读的，防止无意中修改上层传入的值\n\nStructuredTaskScope（结构化并发）：\n- 将并发任务的创建-执行-结果收集绑定到一个结构化代码块中\n- 所有子任务必须在作用域结束前完成（join 或 cancel）\n- 确保不会\"泄露\"线程（结构化保证）\n\n示例场景：\n- ShutdownOnSuccess：适用于\"任一子任务成功即可\"的场景（race 模式）\n- ShutdownOnFailure：适用于\"全部子任务成功\"的场景（all 模式）\n\n扩展延伸：结构化并发解决的核心问题是线程泄露——传统 ExecutorService 提交任务后，如果忘记获取 Future 的结果，任务可能一直运行。StructuredTaskScope 在作用域关闭时自动中断未完成的任务。ScopedValue + 虚拟线程 + 结构化并发是 JDK 21 的三大并发特性更新，共同构建 Java 的现代并发编程模型。","hints":["ScopedValue = 不可变的块作用域上下文，ThreadLocal = 线程作用域可变上下文","结构化并发保证所有子任务在作用域结束前完成或取消"],"tags":["Java","ScopedValue","结构化并发","Loom"],"id":1764},{"category":"java","difficulty":"medium","type":"short_answer","title":"动态 CDS 归档","content":"请介绍 JDK 的 Class Data Sharing（CDS）和 Application Class-Data Sharing（AppCDS）机制。动态 CDS 归档（Dynamic CDS Archive, JDK 13+）如何加速应用启动？","answer":"答案：CDS 通过共享类元数据加速 JVM 启动并减少内存占用。\n\n传统 CDS（JDK 5+）：\n- JRE 安装时创建 classes.jsa（Class Sharing Archive），包含核心类库的元数据\n- 多个 JVM 进程共享只读的类元数据（通过内存映射文件）\n- 减少启动时间（类加载时不需要读取和解析 class 文件）\n- 减少内存占用（多个 JVM 共享同一份类元数据）\n\nApplication CDS（AppCDS, JDK 10+）：\n- 将 CDS 从核心类库扩展到应用类\n- 通过三步骤使用：\n  1. 记录：-XX:DumpLoadedClassList=classes.lst 记录应用启动时加载的类\n  2. 归档：-Xshare:dump -XX:SharedArchiveFile=app.jsa -XX:SharedClassListFile=classes.lst\n  3. 使用：-Xshare:auto -XX:SharedArchiveFile=app.jsa\n- 大幅减少应用启动时间（30-50%）\n\n动态 CDS 归档（JDK 13+）：\n- 消除了显式记录类列表的步骤（不需要 -XX:DumpLoadedClassList）\n- JVM 退出时自动生成默认归档文件（默认位置：java_XXXX.jsa in temp dir）\n- 使用：-XX:ArchiveClassesAtExit=app.jsa（退出时生成归档）\n- 后续启动使用 -XX:SharedArchiveFile=app.jsa\n\n进一步优化：\n- 训练运行（Training Run）：启动应用并预热后退出生成归档\n- 生产环境中，应用第一次启动的类加载信息保存在归档中，后续启动直接使用\n- AOT 编译（JaOTC, JDK 17+）将类提前编译为机器码，与 CDS 互补\n\n扩展延伸：Spring Boot 应用的启动加速——通过 CDS 归档 + AOT 编译可以显著缩短启动时间。Spring Framework 6.1+ 支持自动生成 CDS 归档。Pitchfork（Spring 的 AOT 框架）使用 CDS + AOT 降低云原生环境的冷启动延迟。","hints":["AppCDS 将应用类的元数据归档到 .jsa 文件","动态 CDS 归档（JDK 13+）自动在 JVM 退出时生成归档"],"tags":["Java","CDS","AppCDS","启动优化"],"id":1765},{"category":"java","difficulty":"easy","type":"choice","title":"Java 反射与注解","content":"关于 Java 反射（Reflection）API，以下哪项描述是正确的？","options":["A) 反射通过 Class.forName() 获取的 Class 对象是每次调用时重新创建的","B) 反射调用私有方法时，必须调用 setAccessible(true) 来抑制 Java 语言访问检查","C) 反射在编译时即可完成类型安全检查","D) @Inherited 注解标注的元注解会使其修饰的子注解自动被所有子类继承"],"answer":"B) 反射调用私有方法时，必须调用 setAccessible(true) 来抑制 Java 语言访问检查\n\n解析：Class 对象由 JVM 类加载器创建并缓存，同一个类加载器加载的同一个类只有一份 Class 对象（不是每次创建）。反射是在运行时检查类信息（编译时无法做类型安全验证）。@Inherited 只能让「类」上的注解被子类继承——对接口/方法/字段上的注解无效，且不能使子注解自动继承。\n\n扩展延伸：反射性能——反射调用比普通方法调用慢（需要动态解析方法句柄、做访问检查）。优化：setAccessible(true) 关闭访问检查提升性能；MethodHandles.lookup() 比 Class.getMethod() 更高效。Java 9+ 的模块系统限制了反射访问（对未开放模块的私有成员反射会报错 --add-opens）。反射的典型应用——Spring 的 IoC（依赖注入/Bean 实例化）、MyBatis 的映射绑定、JUnit 的测试方法发现。","hints":["Class 对象在同一个类加载器中是缓存复用的（单例）——不是每次 Class.forName() 都新建","@Inherited 只对类上的注解有效，对方法/字段/接口上的注解无效"],"tags":["Java","反射","注解","JVM"],"id":1836},{"category":"java","difficulty":"medium","type":"choice","title":"Java 泛型类型擦除","content":"关于 Java 泛型的类型擦除（Type Erasure），以下哪项描述是正确的？","options":["A) List<String> 和 List<Integer> 在运行时是同一个 Class 对象","B) 可以在运行时通过 getClass() 获取泛型参数类型","C) new T() 在泛型方法中是合法操作","D) 泛型方法在编译后不会进行类型擦除"],"answer":"A) List<String> 和 List<Integer> 在运行时是同一个 Class 对象\n\n解析：Java 泛型通过类型擦除实现——编译时检查类型安全，运行时移除类型参数信息。List<String> 和 List<Integer> 在运行时都是 List（原始类型）。B 错：类型擦除后运行时无法获取泛型参数（除非通过特定手段如 ParameterizedType 取父类的泛型参数）。C 错：T 被擦除后变为 Object，new T() 没有任何语义。D 错：泛型方法同样进行类型擦除。\n\n扩展延伸：类型擦除的影响——1）不能使用 instanceof T（运行时不知道 T 是什么）。2）不能创建泛型数组（new T[] 非法）。3）重载冲突——`void foo(List<String>)` 和 `void foo(List<Integer>)` 擦除后都是 `void foo(List)`，不能作为重载方法。保留泛型信息的方法——1）通过 TypeReference 匿名类的 getGenericSuperclass() 获取父类的泛型参数（Spring 和 Jackson 使用此技巧）。2）通过方法参数保留泛型信息（Java reflection 的 Method.getGenericParameterTypes()）。C# 的泛型是 reified（运行时保留类型信息），Java 的设计选择是为了保持向后兼容（Java 5 之前的代码无需修改即可运行在泛型版本中）。","hints":["Java 泛型是编译期行为——编译后类型参数被擦除为上限类型（通常是 Object）","通过 TypeReference/ParameterizedType 技巧可以在运行时获取泛型参数——利用匿名子类保留超类的泛型信息"],"tags":["Java","泛型","类型擦除","JVM"],"id":1837},{"category":"java","difficulty":"hard","type":"choice","title":"Java 内存模型与可见性","content":"在 Java 内存模型（JMM）中，以下关于可见性的描述哪一项是正确的？","options":["A) 一个线程修改了 volatile 变量，其他线程立即可见（保证可见性）","B) synchronized 块内对一个普通变量的修改，在该线程退出 synchronized 块后其他线程立即可见","C) final 字段在构造器中初始化完成后，其他线程一定能在对象引用发布后看到正确的 final 值","D) 以上全部正确"],"answer":"D) 以上全部正确\n\n解析：JMM 的可见性保证——volatile 变量的写操作立即刷新到主内存，读操作从主内存读取（不会读到缓存值），保证可见性。synchronized 的可见性由管程（Monitor）的进入和退出保证——退出 synchronized 块时线程的本地内存刷新到主内存。final 字段的特殊语义——构造器正确初始化 final 字段后，其他线程看到该对象引用时一定看到正确的 final 值（但不能保证看到构造器中非 final 字段的正确值——因为可能发生重排序）。\n\n扩展延伸：Happens-Before 规则——1）程序顺序规则：同一个线程中的每个操作 happens-before 其后的任意操作。2）monitor 锁规则：一个锁的解锁 happens-before 后续对这个锁的加锁。3）volatile 规则：对 volatile 变量的写 happens-before 后续对该 volatile 变量的读。4）传递性：A happens-before B, B happens-before C → A happens-before C。5）start 规则：线程的 start() happens-before 该线程中的任何操作。6）join 规则：线程中的所有操作 happens-before 其他线程对该线程的 join() 返回。双重检查锁定（DCL）加 volatile——单例模式中 instance 需要声明为 volatile（防止 new 操作的重排序导致另一个线程看到未完全初始化的对象）。","hints":["Happens-Before 是 JMM 的核心——定义了多线程共享变量的可见性规则","final 字段在构造器正确初始化后被其他线程安全可见——但 this 逸出（构造器中传递 this 引用）会破坏此保证"],"tags":["Java","JMM","可见性","并发"],"id":1838},{"category":"java","difficulty":"easy","type":"choice","title":"Java 异常体系","content":"关于 Java 的异常体系，下列哪项描述是正确的？","options":["A) Error 类异常需要开发者在代码中捕获处理，否则无法编译","B) RuntimeException 及其子类属于受检异常（Checked Exception）","C) 如果父类方法没有声明 throws 异常，子类重写方法也不能声明抛出受检异常","D) finally 块中的 return 语句会在 try 块 return 之前执行，因此 finally 的 return 会覆盖 try 的 return"],"answer":"C) 如果父类方法没有声明 throws 异常，子类重写方法也不能声明抛出受检异常\n\n解析：子类重写方法抛出的受检异常范围不能大于父类方法声明的异常范围（Liskov 替换原则）。Error 及其子类属于非受检异常（Unchecked Exception），不需要也不应该捕获。RuntimeException 及其子类属于非受检异常（Unchecked Exception）。D：finally 块在 try 的 return 之前执行，但 finally 中的 return 会覆盖 try 的返回值——这是 Java 的陷阱（try 中计算返回值存入栈，finally 修改返回值变量不影响已存入的返回值，除非 finally 也有 return）。\n\n扩展延伸：异常设计——1）受检异常（Checked Exception）：必须处理的异常（IOException、SQLException）。方法签名中 throws 声明→调用者必须 try-catch 或继续 throws。2）非受检异常（Unchecked Exception）：包括 RuntimeException（NullPointerException、IllegalArgumentException、IndexOutOfBoundsException）和 Error（OutOfMemoryError、StackOverflowError）。3）异常开销：创建异常需要填充栈轨迹（fillInStackTrace），这是主要开销。自定义异常时可以用 `protected` 构造器禁用栈轨迹填充来优化（`super(message, cause, enableSuppression, writableStackTrace)`）。最佳实践——不要用异常控制正常流程（异常应留给真正的异常情况）。不要 catch Exception 或 Throwable（范围太大）。finally 中不要有 return。尽量用更具体的异常类。","hints":["子类重写方法不能抛出比父类更宽泛的受检异常——这是 Liskov 替换原则（LSP）的要求","finally 中的 return 会覆盖 try 中的 return——这是 Java 的陷阱，可能隐藏异常"],"tags":["Java","异常","Exception","编程规范"],"id":1839},{"category":"java","difficulty":"hard","type":"choice","title":"Java NIO 与零拷贝","content":"关于 Java NIO 的零拷贝（Zero-Copy）技术，下列哪项描述是正确的？","options":["A) FileChannel.transferTo() 实现了零拷贝，数据在内核空间直接从一个文件描述符传输到另一个，不需要经过用户空间","B) 零拷贝是指数据在传输过程中不需要任何 Copy 操作","C) MappedByteBuffer 使用堆外内存（Direct Memory）映射文件，其内存分配和回收不受 JVM GC 管理","D) DirectByteBuffer 分配的内存不受 -Xmx 参数限制"],"answer":"A) FileChannel.transferTo() 实现了零拷贝，数据在内核空间直接从一个文件描述符传输到另一个，不需要经过用户空间\n\n解析：FileChannel.transferTo() 在底层调用操作系统的 sendfile（Linux）系统调用，数据从磁盘→内核缓冲区→Socket 缓冲区（两次 DMA 拷贝），不需要经过 JVM 堆内存（用户空间→内核空间的切换）。零拷贝是指「不需要 CPU 参与数据拷贝（DMA 代替 CPU 拷贝）」+「不需要经过用户空间」。B 错：零拷贝并非没有拷贝——DMA 拷贝仍然存在，是减少 CPU 拷贝和上下文切换。\n\n扩展延伸：NIO 核心组件——1）Channel（通道）：与 Buffer 配合，双向数据传输（FileChannel、SocketChannel、ServerSocketChannel）。2）Buffer（缓冲区）：ByteBuffer（HeapByteBuffer/DirectByteBuffer）、CharBuffer、IntBuffer 等。position/limit/capacity 三个指针。3）Selector（选择器）：一个线程管理多个 Channel 的 IO 事件（多路复用）。DirectByteBuffer vs HeapByteBuffer——DirectByteBuffer 在堆外分配内存（Native Memory），读写时减少用户空间→内核空间的拷贝。创建和回收成本高（不适合小缓冲区）。不受 -Xmx 限制（受 -XX:MaxDirectMemorySize 限制）。MappedByteBuffer 基于 mmap（内存映射文件）——将文件映射到虚拟内存，通过读写内存操作文件（性能接近零拷贝但不完全是 sendfile）。MappedByteBuffer 的回收依赖于 GC（可能导致文件句柄泄漏）。","hints":["零拷贝（Zero-Copy）不是零次拷贝——而是减少 CPU 拷贝和内核态/用户态切换（用 DMA 替代 CPU 拷贝）","DirectByteBuffer（堆外内存）不受 -Xmx 控制——受 -XX:MaxDirectMemorySize（默认等于 -Xmx）控制"],"tags":["Java","NIO","零拷贝","IO"],"id":1840},{"category":"java","difficulty":"easy","type":"choice","title":"Java 泛型类型擦除的边界","content":"关于 Java 泛型的类型擦除（Type Erasure），以下说法正确的是？","answer":"D) 泛型方法在被编译为字节码时会生成桥接方法（Bridge Method）来维持多态。解析：类型擦除是 Java 泛型的实现方式——编译器在编译时擦除类型参数，替换为它们的上界（或 Object），并插入必要的类型转换。但为了保持与未擦除前的多态兼容性，编译器会生成桥接方法。例如 class StringList implements List<String> { public void add(String item) {...} } 在类型擦除后 add 方法的参数变为 add(Object)，编译器会生成一个桥接方法 add(Object) -> add(String) 来维持多态。\n\n扩展延伸：桥接方法不仅是泛型的产物——JDK 5 引入协变返回类型后，子类覆盖父类方法返回更具体的类型时也会生成桥接方法。可以通过 javap -c 查看编译后的类文件中的桥接方法（标记为 ACC_BRIDGE + ACC_SYNTHETIC）。","options":["A) 泛型类型在运行期可以通过反射获取完整的类型参数信息","B) 由于类型擦除，运行时 ArrayList<Integer> 和 ArrayList<String> 是同一个 Class 对象","C) 类型擦除后所有的 T 都被替换为 Object，即使是 T extends Comparable<T> 这样的有界类型参数","D) 泛型方法在被编译为字节码时会生成桥接方法（Bridge Method）来维持多态"],"hints":["桥接方法（Bridge Method）的作用是什么——为什么编译器需要生成它","有界类型参数和无界类型参数擦除后的替换类型有什么不同"],"tags":["Java","泛型","类型擦除"],"id":1846},{"category":"java","difficulty":"hard","type":"choice","title":"Java SPI 机制","content":"关于 Java SPI（Service Provider Interface）机制，以下说法正确的是？","answer":"C) SPI 通过 META-INF/services/ 目录下的配置文件加载接口的实现类，适合框架扩展点的发现。解析：Java SPI（ServiceLoader）的核心机制——接口提供方定义接口，实现方在 jar 包的 META-INF/services/ 目录下创建以接口全限定名为名称的文件，文件内容为实现类的全限定名。ServiceLoader.load(Interface.class) 会扫描 ClassPath 下所有 jar 包中的 SPI 配置文件，加载并实例化实现类。JDBC DriverManager 使用 SPI 加载数据库驱动（mysql-connector-java、postgresql.jar 中的 Driver 实现类）。\n\n扩展延伸：SPI 的经典应用——1）JDBC Driver（最经典）。2）Slf4j 日志门面（通过 SPI 加载 Logback/Log4j 实现）。3）Dubbo 的 ExtensionLoader（增强版 SPI，支持按需加载和自适应扩展）。SPI 的缺陷：ServiceLoader.load() 会加载配置文件中列出的所有实现类（即使暂时不需要），可能造成启动变慢和资源浪费。Dubbo 的 Adaptive SPI 机制解决了这个问题。","options":["A) SPI 机制基于注解扫描，需要在接口上标注 @SPI 注解才能被 ServiceLoader 发现","B) ServiceLoader.load() 会一次性加载并实例化 META-INF/services/ 下配置的所有实现类","C) SPI 通过 META-INF/services/ 目录下的配置文件加载接口的实现类，适合框架扩展点的发现","D) SPI 是 Java 9 模块化（JPMS）引入的新功能，Java 8 及以下版本不支持"],"hints":["JDBC 驱动加载中 SPI 的作用——mysql-connector-java 是如何被自动加载的","ServiceLoader 是延迟加载还是立即加载——有什么区别"],"tags":["Java","SPI","ServiceLoader","扩展点"],"id":1847},{"category":"java","difficulty":"hard","type":"choice","title":"sun.misc.Unsafe 的核心功能","content":"关于 sun.misc.Unsafe 类，以下说法正确的是？","answer":"B) Unsafe 可以通过 getUnsafe() 获取实例，但只有启动类加载器（Bootstrap ClassLoader）加载的类才能直接调用，普通用户代码需要通过反射获取。解析：Unsafe 提供了一系列不安全的底层操作：1）直接内存操作（allocateMemory、putLong、getInt）——绕过堆内存直接访问堆外内存。2）对象字段偏移量（objectFieldOffset）——通过偏移量直接读写对象字段（绕过构造器和访问控制）。3）CAS 操作（compareAndSwapObject、compareAndSwapInt）——硬件级别的原子操作，JUC 包（AtomicInteger、AQS）的基石。4）park/unpark（阻塞和唤醒线程）。\n\n扩展延伸：Unsafe 的应用——Netty 使用 DirectByteBuffer（底层依赖 Unsafe）实现堆外内存零拷贝。Java 9+ 引入 Variable Handles（VarHandle）作为 Unsafe 的安全替代方案，但 Unsafe 仍未被移除，因为大量框架（Spring、Netty、Kafka）深度依赖它。风险：使用 Unsafe 不当会导致 JVM 崩溃（Segment Fault），而不是抛出 Java 异常。","options":["A) Unsafe 类的所有方法都是线程安全的，可以直接在高并发场景中使用","B) Unsafe 可以通过 getUnsafe() 获取实例，但只有启动类加载器加载的类才能直接调用，普通用户代码需要通过反射获取","C) Unsafe.allocateInstance() 可以创建任何类的实例，并且会执行该类的构造方法","D) Java 9 之后 Unsafe 的所有功能都被替代，已经废弃不再使用"],"hints":["Unsafe 中的 CAS 方法（compareAndSwap）与 AtomicInteger 的关系","为什么叫 Unsafe——它有什么安全风险"],"tags":["Java","Unsafe","CAS","JVM"],"id":1848},{"category":"java","difficulty":"medium","type":"choice","title":"Java 四种引用类型","content":"关于 Java 的四种引用类型（强、软、弱、虚），以下说法正确的是？","answer":"B) WeakReference 对象在 GC 时不管内存是否充足都会被回收，常用于 WeakHashMap 和 ThreadLocal 的 Key。解析：四种引用类型及 GC 行为——1）强引用（Strong Reference）：Object obj = new Object()，只要强引用存在，对象永远不会被 GC 回收（即便 OOM）。2）软引用（SoftReference）：内存充足时不回收，内存不足（即将 OOM）时回收。适合做缓存（如 ImageCache）。3）弱引用（WeakReference）：每次 GC 时都会被回收（不管内存是否充足）。经典应用：WeakHashMap（Key 为弱引用，Map 中的 Entry 在 Key 不再被使用时自动清理）、ThreadLocalMap 的 Entry（Key 是弱引用 ThreadLocal，防止 ThreadLocal 无法被 GC 导致内存泄漏）。4）虚引用（PhantomReference）：最弱的引用，get() 永远返回 null。唯一用途是跟踪对象被 GC 回收的时刻（与 ReferenceQueue 配合），用于堆外内存的回收（NIO DirectByteBuffer）。\n\n扩展延伸：引用类型的实际应用——软引用适合实现缓存（图片缓存、对象池），但注意 JDK 默认的 SoftReference 在最近一次 GC 后可能会被立即回收（取决于 -XX:SoftRefLRUPolicyMSPerMB 参数，默认 1000ms）。WeakHashMap 适合做元数据缓存（如 ClassLoader 的缓存），当 Key 不再被外部引用时 Entry 自动被清理。虚引用必须与 ReferenceQueue 配合使用，在 PhantomReference 被入队时意味着被引用对象已经 finalized 但内存还未被回收。","options":["A) SoftReference 引用的对象在任何 GC 发生时都会被立即回收","B) WeakReference 对象在 GC 时不管内存是否充足都会被回收，常用于 WeakHashMap 和 ThreadLocal 的 Key","C) PhantomReference 的 get() 方法可以返回被引用的对象，适用于构建对象池","D) 强引用对象在内存不足时会被 JVM 优先回收以避免 OOM"],"hints":["ThreadLocal 的内存泄漏为什么和弱引用有关——Key 是弱引用 Value 是强引用的组合结果","软引用和弱引用在 GC 行为上的本质区别是什么"],"tags":["Java","引用类型","GC","内存管理"],"id":1849},{"category":"java","difficulty":"hard","type":"choice","title":"JMX（Java Management Extensions）","content":"关于 Java JMX（Java Management Extensions），以下说法正确的是？","answer":"C) JMX 通过 MBean（Managed Bean）暴露 Java 应用的运行时状态和操作，支持运行时动态修改配置而无需重启应用。解析：JMX 的核心——MBean 是受管 Bean，将应用内部的配置参数、统计指标、操作（如修改日志级别、查看线程堆栈）封装为可暴露的属性和方法。通过 JMX 连接（JConsole、VisualVM、JMX 客户端）可以远程连接 JVM 查看和操作这些 MBean。标准 MBean 需要定义接口（名称以 MBean 结尾），JMX 通过 ObjectName 唯一标识每个 MBean。\n\n扩展延伸：JMX 的工程实践——1）Spring 集成：使用 @ManagedResource/@ManagedAttribute/@ManagedOperation 注解将 Spring Bean 暴露为 MBean。2）Tomcat 通过 JMX 暴露连接池状态、请求统计等指标。3）Kafka 通过 JMX 暴露 broker 和 consumer 指标。4）监控工具（Prometheus JMX Exporter、Datadog）通过 JMX 采集 JVM 和应用的指标。JMX 的局限：无法跨越防火墙（通常需要 JMX RMI 端口映射），安全性较弱（默认无认证，需配置 SSL/密码认证）。现代替代：Micrometer + Prometheus（基于 HTTP，更适合云原生场景）。","options":["A) JMX 只能用于监控 JVM 本身（内存、GC、线程），无法暴露应用程序的自定义指标","B) JMX 的 MBean 定义后只能读取不能修改（只读），如果要修改需要重启应用","C) JMX 通过 MBean（Managed Bean）暴露 Java 应用的运行时状态和操作，支持运行时动态修改配置而无需重启应用","D) JMX 已经被 Micrometer 和 Prometheus 完全取代，Java 11 后不再支持"],"hints":["JConsole 和 VisualVM 如何连接到本地和远程 JVM——JMX 连接参数是什么","Spring 中的 @ManagedResource/@ManagedAttribute 注解与 JMX 的关系"],"tags":["Java","JMX","MBean","监控"],"id":1850},{"category":"java","difficulty":"easy","type":"choice","title":"ScopedValue 的继承行为","content":"在 JDK 21 中，以下关于 ScopedValue 的描述哪个是正确的？","options":["A) ScopedValue 的值被子线程继承，子线程可以修改该值","B) ScopedValue 的值在绑定作用域内对所有方法调用可见","C) ScopedValue 与 ThreadLocal 功能完全一致","D) ScopedValue 的值通过 set() 方法修改"],"answer":"B","analysis":"ScopedValue 通过 ScopedValue.where(key, value).run() 绑定，值在 run 作用域内对所有调用栈可见（包括子任务）。ScopedValue 是不可变的（不能 set），子线程通过继承机制自动获取父线程的 ScopedValue 副本。ThreadLocal 的 set() 不适用于 ScopedValue。","tags":["Java","ScopedValue","并发","JDK21"],"id":1861},{"category":"java","difficulty":"easy","type":"choice","title":"ZGC 并发处理阶段","content":"ZGC 的哪个阶段需要 STOP THE WORLD？","options":["A) 并发标记（Concurrent Mark）","B) 并发重映射（Concurrent Remap）","C) 初始化堆布局（Heap Initialization）","D) 并发引用处理（Concurrent Reference Processing）"],"answer":"C","analysis":"ZGC 的大部分阶段是并发的（标记、重映射、引用处理等），不需要 STW。但初始化堆布局（Heap Initialization）阶段在 GC 周期开始时需要短暂的 STW。ZGC 的目标是将 STW 时间控制在 10ms 以内，绝大部分阶段都是与 Java 线程并发执行的。","tags":["Java","ZGC","GC","JVM"],"id":1862},{"category":"java","difficulty":"easy","type":"choice","title":"Record 模式匹配","content":"JDK 21 的模式匹配中，以下哪种是 Record 的嵌套模式匹配的正确写法？","options":["A) if (obj instanceof Point(int x, int y))","B) if (obj instanceof Point(var x, var y) && x > 0)","C) if (obj instanceof Point(int x, int y) p)","D) 以上全部正确"],"answer":"D","analysis":"JDK 21 正式支持 Record 模式匹配。以上三种写法都正确：解构 Record 为 x/y 变量、用 var 推断类型、带变量的完整匹配。Record 模式匹配是 Java 模式匹配（Pattern Matching）的重要扩展，允许递归解构嵌套 Record。","tags":["Java","Record","模式匹配","JDK21"],"id":1863},{"category":"java","difficulty":"easy","type":"choice","title":"ForkJoinPool 工作窃取","content":"关于 ForkJoinPool 的工作窃取（Work-Stealing）机制，以下哪个描述是正确的？","options":["A) 每个工作线程从全局队列窃取任务","B) 线程从自己的双端队列头部获取任务，从其他线程尾部窃取任务","C) 所有任务都被均匀分配到各线程，不需要窃取","D) ForkJoinPool 只适合 CPU 密集型任务，对 I/O 密集型无效"],"answer":"B","analysis":"ForkJoinPool 的每个工作线程维护自己的双端队列（Deque）。线程从自己的队列头部取出任务（LIFO），从其他线程的队列尾部窃取任务（FIFO），这种策略最小化了窃取冲突。选项 A 错误（不是全局队列），选项 C 错误（不均匀时才发生窃取），选项 D 错误（虽然主要针对 CPU 密集，但也适用于 I/O 密集型，只是不如虚拟线程高效）。","tags":["Java","ForkJoinPool","工作窃取","并发"],"id":1864},{"category":"java","difficulty":"easy","type":"choice","title":"Valhalla 内联类","content":"Project Valhalla 引入的内联类（Inline Class，值类型）的核心特征是什么？","options":["A) 内联类可以被继承","B) 内联类没有对象标识（Identity），不需要堆分配","C) 内联类内部所有字段必须是 final 的","D) 内联类可以包含 synchronized 方法"],"answer":"B","analysis":"内联类（值类型）的核心特征是没有对象标识（Identity），不需要在堆上分配。这意味着它们可以存储在寄存器中、栈上分配在数组的连续内存中。内联类可以有字段（不是必须 final），可以有方法（包括构造函数）。但内联类不能有对象标识——不能使用 == 比较（除非字段级比较），不能继承其他类，不能有 synchronized 方法（因为没有监视器锁）。","tags":["Java","Valhalla","内联类","值类型"],"id":1865},{"category":"java","difficulty":"medium","type":"short_answer","title":"组合优先于继承","content":"请解释\"组合优先于继承\"（Favor Composition over Inheritance）的设计原则。在什么情况下继承仍然是最佳选择？组合比继承的优势在哪里？","answer":"答案：组合优先于继承是指尽量通过对象组合（在一个类中引用其他类的对象）来实现代码复用，而不是通过继承。\n\n组合的优势：\n1. 更低耦合：组合的类之间通过接口交互，不继承实现的细节。子类耦合父类实现的每个细节。\n2. 更好的封装：继承打破封装，子类可以访问父类的受保护成员。组合通过公共接口访问。\n3. 更灵活：运行时可以替换组件（策略模式），继承在编译时已经固定。\n4. 避免脆弱的基类问题：父类实现变化会破坏所有子类（Fragile Base Class Problem）\n5. 不涉及复杂的层次结构：多层继承容易导致菱形问题（Diamond Problem）\n\n继承仍然是最佳选择的场景：\n1. 子类是父类的一种特化，符合 is-a 关系（如 Dog extends Animal）\n2. 子类不需要改变父类的行为，只需要扩展行为\n3. 父类的设计就是为了被继承（抽象类或设计好的模板方法）\n4. 需要利用多态（Polymorphism）——用统一接口处理不同子类\n\n设计的经验法则：\n- Has-a 关系使用组合，Is-a 关系使用继承\n- 优先使用接口（Interface）定义协议，组合实现具体行为\n- 设计抽象类时，考虑是否真的需要继承层次结构\n\n扩展延伸：Go 语言通过嵌入（Embedding）提供了另一种思路——类似继承的语法但本质是组合。Java 的 @Override 注解帮助检测继承错误（父类方法签名改变时编译报错）。Favor Composition over Inheritance 是 Design Patterns（GoF）中反复出现的核心原则。","hints":["组合（has-a）低耦合灵活，继承（is-a）强耦合但支持多态","脆弱的基类问题：父类变化破坏所有子类"],"tags":["Java","OOP","组合","继承"],"id":1948},{"category":"java","difficulty":"medium","type":"short_answer","title":"接口与抽象类的选型","content":"在 Java 中，接口（Interface）和抽象类（Abstract Class）的使用场景如何选择？JDK 8+ 中接口增加了默认方法（default method）后，两者的区别发生了什么变化？","answer":"答案：接口和抽象类都是抽象类型，但有不同的设计意图和适用场景。\n\nJDK 8+ 之前的核心区别：\n1. 接口：只能声明抽象方法（不能有实现），字段只能是 public static final\n2. 抽象类：可以有具体方法、字段、构造函数\n\nJDK 8 之后的变化：\n1. 接口增加了 default method（默认方法）和 static method\n2. 接口也可以写方法实现，一定程度上模糊了接口和抽象类的界限\n\n接口 vs 抽象类的选择标准：\n\n使用接口的场景：\n- 定义契约（Contract）：\"你的对象能做到什么\"（行为的抽象）\n- 不相关的类之间共享行为：如 Comparable、Runnable\n- 多重类型继承：一个类可实现多个接口\n- 需要被多个不同的抽象层级实现\n\n使用抽象类的场景：\n- 相关类之间共享状态和实现：\"你的对象是什么\"（状态的抽象）\n- 提供部分实现 + 模板方法模式\n- 有受保护成员需要被子类访问\n- 需要非 public static final 字段\n\n本质上：接口表达能力（ability），抽象类表达身份（identity）。\n\n经验法则：\n- 优先使用接口定义类型\n- 优先使用接口定义的默认方法提供辅助实现\n- 只在需要共享状态或模板方法时使用抽象类\n- SimpleDateFormat 是\"抽象类比接口更合适\"的反面例子——DateFormat 是抽象类\n\n扩展延伸：Java 的 函数式接口（Functional Interface，只有一个抽象方法的接口）是 Lambda 表达式的基础。@FunctionalInterface 注解确保接口只有一个抽象方法。Java 8 引入的默认方法的动机是 Stream API 的向后兼容性（让 Collections API 新增 default 方法而不破坏已有实现）。","hints":["接口 = 能力契约（多继承），抽象类 = 身份抽象（状态共享+模板方法）","默认方法模糊了界限但仍保留核心区别：字段、构造函数、受保护成员"],"tags":["Java","接口","抽象类","OOP"],"id":1949},{"category":"java","difficulty":"medium","type":"short_answer","title":"注解处理器","content":"请介绍 Java 注解处理器（Annotation Processor）的原理和使用场景。它在编译时的处理流程是什么？Lombok 和 MapStruct 是如何利用注解处理器实现代码生成的？","answer":"答案：注解处理器在编译时扫描和处理注解，自动生成代码。\n\n处理流程：\n1. 编译开始，javac 调用注解处理器（通过 SPI 注册，类路径下 META-INF/services/javax.annotation.processing.Processor）\n2. 处理器的 process() 方法被调用，传入 RoundEnvironment（包含标注了特定注解的元素）\n3. 处理器分析被注解的元素（类、方法、字段），生成 Java 源文件或字节码\n4. 如果生成了新文件，启动新一轮处理（直到没有新文件生成）\n5. 编译新生成的源文件，最终一起编译为 .class\n\n核心 API：\n- AbstractProcessor：所有处理器的基类，实现 process() 方法\n- RoundEnvironment：当前轮次的注解环境，获取被注解的元素\n- Filer：创建新文件（源文件、类文件、资源文件）的工具\n- Messager：输出编译日志（注意、警告、错误）\n- Elements/TypeMirror：类型和元素的反射 API\n\nLombok 的原理：\n- 利用注解处理器的 API，在编译时修改 AST（抽象语法树）\n- @Getter/@Setter：在 AST 中插入 getter/setter 方法\n- @Builder：生成 Builder 设计模式的辅助类\n- 注：Lombok 使用的不是标准 Filer API（生成新文件），而是直接操作编译器的内部 AST（利用非标准 API）\n\nMapStruct 的原理：\n- 在编译时自动生成 Bean 映射代码（类似于 ModelMapper，但零运行时反射）\n- 读取目标接口的方法签名，为每个方法生成映射实现类\n- 生成的代码是普通的 Java 类，没有运行时依赖\n\n扩展延伸：Google Auto 提供 @AutoService、@AutoValue 等注解。Error Prone 使用注解处理器做静态分析。注解处理器不会增加运行时开销（编译时已完成代码生成）。KAPT（Kotlin Annotation Processing Tool）支持在 Kotlin 中使用 Java 注解处理器。","hints":["注解处理器在编译时通过 SPI 发现，每轮生成新文件触发下一轮","Lombok 操作 AST（非标准），MapStruct 生成映射实现文件（标准 Filer）"],"tags":["Java","注解处理器","Lombok","代码生成"],"id":1950},{"category":"java","difficulty":"hard","type":"short_answer","title":"服务加载器 SPI","content":"请介绍 Java 的 ServiceLoader 和服务加载机制（SPI）。SPI 和 API 的区别是什么？在 Spring Boot 自动配置和 JDBC 驱动加载中是如何使用 SPI 的？","answer":"答案：SPI（Service Provider Interface）是 Java 内置的插件机制，允许在运行时发现和加载服务提供者。\n\nSPI vs API：\n- API（Application Programming Interface）：由提供者定义接口，调用者使用\n- SPI（Service Provider Interface）：由框架定义接口，提供者实现。框架负责发现和加载所有实现\n- 典型例子：JDBC API 是 API（大家调用 java.sql.Connection），JDBC Driver 是 SPI（数据库厂商实现 java.sql.Driver 并通过 META-INF/services 注册）\n\nServiceLoader 机制：\n1. 服务提供者在 JAR 的 META-INF/services/ 目录下创建文件，文件名=服务接口全限定名\n2. 文件内容=实现类的全限定名（每行一个，支持多实现）\n3. 运行时 ServiceLoader.load(ServiceInterface.class) 读取该文件并加载实现类\n4. 实现类通过无参构造函数实例化\n\nJDBC 驱动加载：\n- 传统方法（JDK 6 之前）：Class.forName(\"com.mysql.jdbc.Driver\")——手动注册\n- SPI 方法（JDK 6+）：DriverManager 在初始化时通过 ServiceLoader 加载所有 META-INF/services/java.sql.Driver 中注册的驱动\n- 各数据库厂商标明实现类：mysql-connector-java.jar 中包含 META-INF/services/java.sql.Driver→com.mysql.cj.jdbc.Driver\n\nSpring Boot 自动配置：\n- 使用 spring.factories 或 META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports 注册自动配置类\n- Spring Boot 启动时用 SpringFactoriesLoader（类似于 ServiceLoader 但更灵活）加载注册的自动配置\n- AutoConfiguration.imports 文件按条件（@ConditionalOnClass 等）决定哪些自动配置生效\n\n扩展延伸：Java 9+ 模块化系统中，SPI 通过 provides...with 语句声明（module-info.java）。Java 9 的 ServiceLoader 支持模块路径的跨模块加载。自定义 ServiceLoader 需要考虑线程上下文类加载器（Thread Context ClassLoader），在框架环境中尤为重要。","hints":["SPI = 框架定义接口 + 提供者实现 + META-INF/services 注册","JDBC 驱动通过 SPI 自动注册，Spring Boot 的 spring.factories 是 SPI 的扩展"],"tags":["Java","SPI","ServiceLoader","JDBC"],"id":1951},{"category":"java","difficulty":"easy","type":"short_answer","title":"并发修改异常","content":"请解释 Java 的 ConcurrentModificationException（并发修改异常）的触发条件和解决方法。在 for-each 循环中删除集合元素为什么可能抛出该异常？","answer":"答案：ConcurrentModificationException 发生在遍历集合时，集合的结构被并发修改（增删元素）。\n\n触发原因：\n1. 单线程：在 for-each 循环（使用 Iterator）中直接调用集合的 remove() 方法\n2. 多线程：一个线程遍历集合的同时，另一线程修改集合\n\n问题本质：\n- for-each 循环本质上是 Iterator 的语法糖\n- Iterator 持有 modCount 的副本（期望修改次数）\n- 每次调用 next() 时检查 modCount 是否等于 expectedModCount\n- 集合的 remove() 方法会修改 modCount 但 Iterator 不知道\n- 检测到不一致就抛出 ConcurrentModificationException（fail-fast 行为）\n\n代码示例（触发异常）：\nList<String> list = new ArrayList<>(Arrays.asList(\"a\", \"b\", \"c\"));\nfor (String s : list) {\n    if (s.equals(\"b\")) list.remove(s);  // 抛 ConcurrentModificationException\n}\n\n解决方法：\n1. 使用 Iterator 的 remove()：iterator.remove()（会同步更新 expectedModCount）\n2. 使用 Collection.removeIf()：list.removeIf(s -> s.equals(\"b\"))（内部使用 Iterator 的 remove()）\n3. 收集到新集合后再批量删除：List<String> toRemove = ...; list.removeAll(toRemove);\n4. 使用 CopyOnWriteArrayList（多线程场景）：迭代器遍历的是快照，修改的是新副本\n5. 使用 Stream filter：list.stream().filter(s -> !s.equals(\"b\")).collect(toList())\n\n扩展延伸：ConcurrentHashMap、CopyOnWriteArrayList 等并发集合不会抛 ConcurrentModificationException（使用弱一致性迭代器——返回的数据可能是过时的但不会崩溃）。fail-fast 机制是一种缺陷检测机制，不是锁。Vector 和 Collections.synchronizedList 虽然线程安全但也会在 for-each 时抛该异常（需要手动加锁遍历）。","hints":["for-each 循环中调用集合的 remove() 会触发 modCount 不一致","Iterator.remove()、removeIf()、CopyOnWriteArrayList 是常用解决方案"],"tags":["Java","集合","并发","异常"],"id":1952},{"category":"java","difficulty":"medium","type":"short_answer","title":"HashMap 的遍历方式","content":"HashMap 有哪些遍历方式？效率从高到低如何排序？为什么推荐使用 entrySet 遍历？遍历时修改集合会有什么问题？","answer":"答案：四种遍历方式——1）entrySet 迭代器遍历（最高效，同时获取 key 和 value）。2）keySet + get（遍历 key 再 get value，效率低——需要额外 get 操作）。3）forEach (BiConsumer)（Java 8 Lambda，底层使用 entrySet 迭代器）。4）values() 遍历（只遍历 value）。效率排名：entrySet ≈ forEach > keySet > values。\n\n解析：entrySet 最推荐的原因——1）entrySet 的迭代器直接返回 Map.Entry 对象，同时包含 key 和 value，一次迭代即可获取两者。2）keySet 遍历每次需要额外调用 get(key)——get 操作有 O(1) 复杂度但遍历 n 个元素多出了 n 次 hash 计算和链表/红黑树查找。3）forEach（BiConsumer）在 Java 8 的 HashMap 中实现为 `forEach` 方法，内部封装了 entrySet 遍历，使用 Lambda 表达式更简洁。\n\n扩展延伸：遍历时的修改——1）使用迭代器遍历时，调用 HashMap.put/remove 会触发快速失败（ConcurrentModificationException）。必须使用 iterator.remove() 方法安全删除。2）使用 forEach (BiConsumer) 时同样不能修改集合（内部会检查 modCount）。3）Lambda 中使用 ConcurrentHashMap 的 forEach 可以安全修改（因为其迭代器是弱一致性的）。4）Stream API：`map.entrySet().stream()` 可以配合 filter/collect 使用。","hints":["entrySet 同时返回 key 和 value（一次迭代），keySet 需要额外 get（多出 n 次 hash 查找）","遍历时修改集合必须使用 iterator.remove()——直接调用 HashMap.put/remove 会抛 ConcurrentModificationException"],"tags":["Java","集合","HashMap","遍历"],"id":2123},{"category":"java","difficulty":"hard","type":"short_answer","title":"ConcurrentHashMap 的 size() 方法","content":"ConcurrentHashMap 的 size() 方法在 JDK 7 和 JDK 8 中的实现有何不同？为什么 ConcurrentHashMap 不维护一个全局的 size 变量？JDK 8 中 size() 的计数机制？","answer":"答案：JDK 7：通过遍历 segments 累加每个 segment 的 count（两次对比无修改则返回，否则加锁后累加）。JDK 8：使用 baseCount + CounterCell 数组实现近似计数。size() 返回 `sumCount()`——baseCount 加上所有 CounterCell 的 value 之和。不维护全局 size 变量是为了避免每次写操作竞争同一个计数器。\n\n解析：JDK 8 计数原理——1）baseCount：没有竞争时的基础计数器。每次 put/remove 时通过 CAS 更新 baseCount。2）CounterCell：当 baseCount 的 CAS 竞争激烈时，创建 CounterCell 数组（每个线程映射到不同的 cell）。每个线程只 CAS 更新自己对应的 cell，减少竞争。3）sumCount()：返回 baseCount + sum(counterCells)。由于没有锁，sumCount 是「近似值」。\n\n扩展延伸：为什么是近似值——1）ConcurrentHashMap 的 size() 返回的是一个近似值（不是精确值）。高并发下正在进行的操作可能还未加到计数器中。2）JDK 7 中 size() 可能在两次遍历之间发生变化（无锁的两阶段遍历有可能获得不一致的 snapshot）。3）实际场景中 size() 的精确性不重要（大多数时候只需要判断 map 是否为空或大致大小）。4）mappingCount() 方法返回 long 类型（比 size() 的 int 更适用于大集合）。size() 用于 isEmpty() 判断和负载均衡场景（分片策略）。核心设计思想——高并发场景下「近似正确的计数」比「精确但阻塞的计数」更有价值。","hints":["JDK 8 的 size() 是近似值——没有全局锁，正在执行的操作可能尚未计入总数","CounterCell 减少竞争——多线程各写各的 cell，不比所有线程争同一个 baseCount"],"tags":["Java","集合","ConcurrentHashMap","并发"],"id":2124},{"category":"java","difficulty":"medium","type":"choice","title":"HashMap 键的 hashCode 和 equals","content":"如果将一个自定义对象作为 HashMap 的键，以下哪项描述是正确的？","options":["A) 只要重写 equals() 方法即可保证 HashMap 正确工作","B) HashMap 先通过 equals() 判断键是否相同，再通过 hashCode() 定位桶","C) hashCode() 相同时，HashMap 认为两个 key 一定相等","D) HashMap 先通过 hashCode() 定位 bucket，再通过 equals() 判断 key 是否相等"],"answer":"D) HashMap 先通过 hashCode() 定位 bucket，再通过 equals() 判断 key 是否相等\n\n解析：HashMap 的 put 和 get 流程——1）计算 key 的 hashCode()，通过扰动函数（高 16 位异或低 16 位）后计算 bucket 索引。2）如果该 bucket 为空，直接插入。3）如果 bucket 非空，遍历链表或红黑树，使用 equals() 判断每个节点的 key 是否与插入的 key 相等。如果找到相等的 key，替换 value。如果不相等，插入到链表尾部或红黑树。\n\n扩展延伸：正确实现——1）重写 equals() 时必须重写 hashCode()。保证：如果两个对象 equals() 为 true，则 hashCode() 必须相同。如果两个对象 hashCode() 相同，equals() 不一定为 true（哈希冲突）。2）HashMap 和 HashSet 依赖 hashCode() 和 equals() 的契约一致性。不一致时可能导致 key 无法被正确查找（put 到某个 bucket 后 hashCode 变化 → get 时定位到不同的 bucket → 找不到）。3）最佳实践：使用不可变对象作为 HashMap 的 key（如 String/Integer/Long）。如果使用可变对象作为 key 并修改了 hashCode() 相关的字段，会导致内存泄漏（无法通过 key 找到原始 entry）。","hints":["hashCode 定位 bucket（桶），equals 在 bucket 中查找具体 key——两个方法缺一不可","equals 为 true → hashCode 必须相同（契约）。hashCode 相同 → equals 不一定为 true（哈希冲突）"],"tags":["Java","集合","HashMap","hashCode"],"id":2125},{"category":"java","difficulty":"easy","type":"choice","title":"Collections.synchronizedList 原理","content":"关于 Collections.synchronizedList 方法，以下哪项描述是正确的？","options":["A) 它返回的 List 中的所有方法都是通过添加 synchronized 关键字实现的线程安全","B) 使用迭代器遍历该 List 时，需要外部同步（在 synchronized 块中遍历）","C) 它比 CopyOnWriteArrayList 的并发性能更好","D) 它使用 Lock 接口来实现线程安全"],"answer":"B) 使用迭代器遍历该 List 时，需要外部同步（在 synchronized 块中遍历）\n\n解析：Collections.synchronizedList 使用「包装器模式」——在每个方法上使用 synchronized 块（mutex 对象）实现同步。但迭代器遍历时没有同步包装——因为 iterator() 方法返回的是原始 List 的迭代器。因此遍历时必须手动加 synchronized(list) { list.iterator() }。\n\n扩展延伸：使用注意事项——1）synchronizedList 的线程安全是「方法级」的。复合操作（如 get + set、检查再修改）仍然是线程不安全的，需要外部同步。2）迭代器遍历时必须手动加锁：`synchronized(list) { for (Object o : list) { ... } }`。3）synchronizedList 的迭代器是 fail-fast 的（遍历时其他线程修改会抛 ConcurrentModificationException）。CopyOnWriteArrayList 的迭代器是快照（snapshot）模式——遍历时不会抛异常但也不反映遍历过程中的修改。优缺点——synchronizedList：读和写都加锁（低并发场景可用）。CopyOnWriteArrayList：读无锁（读多写少场景极优），但每次写操作复制整个数组（写代价高）。","hints":["synchronizedList 的迭代器遍历需要外部同步——iterator() 方法本身返回的是原始 List 的迭代器","复合操作（非原子操作）需要额外的同步——synchronizedList 只保证单个方法的原子性"],"tags":["Java","集合","线程安全","Collections"],"id":2126},{"category":"java","difficulty":"easy","type":"true_false","title":"HashMap 红黑树退化条件","content":"HashMap 中当某个桶的链表长度达到 8 时，一定会将链表转换为红黑树。当链表长度退化到 6 时，红黑树会立即退化为链表。","options":["正确","错误"],"answer":"错误。\n\n解析：链转树的双重条件——1）某个桶的链表长度达到 TREEIFY_THRESHOLD=8。2）并且 HashMap 的容量（数组长度）达到 MIN_TREEIFY_CAPACITY=64。如果容量 < 64，即使链表长度 >= 8 也不会树化，而是先扩容数组（resize 将链表分散到新数组中）。树退化为链表的条件——红黑树不会在链表长度退化到 6 时立即退化。树退化发生在 resize（扩容）时，当红黑树的节点数 <= UNTREEIFY_THRESHOLD=6 时才退化为链表。\n\n扩展延伸：为什么是 8 和 6——1）8 作为树化阈值：基于泊松分布——在随机哈希码下，链表长度达到 8 的概率极低（约 0.00000006），达到 8 说明哈希函数有问题或恶意攻击（Hash 碰撞 DoS）。2）6 作为退化阈值：8 和 6 之间留了 2 的缓冲（7），避免链表和红黑树在临界值频繁切换（thrashing）。3）树化是为了在极端哈希冲突下保证 O(log n) 的性能（而非 O(n)）。正常情况下 HashMap 几乎不会触发树化。","hints":["链转树需要两个条件——链表长度 >= 8 且数组容量 >= 64（容量 < 64 时优先扩容而非树化）","8（树化）和 6（退化）之间的 2 差值——防止链表和红黑树频繁互转"],"tags":["Java","集合","HashMap","红黑树"],"id":2127},{"category":"java","difficulty":"hard","type":"short_answer","title":"NavigableMap 与 TreeMap","content":"请介绍 NavigableMap 和 TreeMap 的特有功能。NavigableMap 提供了哪些 Map 标准接口没有的操作？ConcurrentSkipListMap 和 TreeMap 有什么区别？","answer":"答案：NavigableMap 是 Java 6 引入的扩展 Map 接口，提供了基于键的有序导航方法。\n\nNavigableMap 的特有方法：\n1. 查找方法：\n   - lowerKey(K)：严格小于给定键的最大键\n   - floorKey(K)：小于等于给定键的最大键\n   - ceilingKey(K)：大于等于给定键的最小键\n   - higherKey(K)：严格大于给定键的最小键\n   - （对应的 Entry 版本：lowerEntry、floorEntry、ceilingEntry、higherEntry）\n2. 子视图：\n   - subMap(K from, boolean fromInclusive, K to, boolean toInclusive)\n   - headMap(K toKey, boolean inclusive)\n   - tailMap(K fromKey, boolean inclusive)\n   - 返回的视图是原 Map 的动态视图，修改视图会影响原 Map\n3. 首尾操作：\n   - firstEntry()/lastEntry()：获取首尾 Entry\n   - pollFirstEntry()/pollLastEntry()：获取并移除首尾 Entry\n\nConcurrentSkipListMap vs TreeMap：\n- TreeMap：基于红黑树，非线程安全。操作 O(log n)\n- ConcurrentSkipListMap：基于跳表（Skip List），线程安全。操作 O(log n) 平均\n- ConcurrentSkipListMap 使用锁分离（分段链表节点级别的锁），并发性能好\n- ConcurrentSkipListMap 的迭代器是弱一致性的（不抛 ConcurrentModificationException）\n\n应用场景：\n1. 区间查询（range query）：查询数据库中某个时间段的所有记录\n2. 库存管理：查询价格在某个范围内的商品\n3. 一致性哈希算法（Consistent Hashing）的底层数据结构\n4. 最近 N 条记录：lastEntry() + 逆序删除\n\n扩展延伸：NavigableSet 是 Set 的导航版本（由 TreeSet 实现），提供类似的方法。ConcurrentNavigableMap 是 ConcurrentMap + NavigableMap，ConcurrentSkipListMap 是其实现类。红黑树和跳表的对比：红黑树写操作更复杂（旋转+染色），跳表实现更简单且支持无锁并发。","hints":["NavigableMap = lower/floor/ceiling/higher + subMap + pollFirst/Last","TreeMap（红黑树，非线程安全）vs ConcurrentSkipListMap（跳表，并发安全）"],"tags":["Java","NavigableMap","TreeMap","ConcurrentSkipListMap"],"id":2148},{"category":"java","difficulty":"hard","type":"short_answer","title":"Spliterator 原理","content":"请介绍 Spliterator（可分割迭代器）的原理和用途。它如何支持 Stream API 的并行处理？Spliterator 的 trySplit() 方法的分割策略是什么？","answer":"答案：Spliterator = Splittable Iterator，是 Java 8 引入的并行迭代基础接口。\n\nSpliterator 的核心接口：\n1. tryAdvance(Consumer)：遍历一个元素，返回是否还有剩余元素\n2. forEachRemaining(Consumer)：遍历所有剩余元素\n3. trySplit()：尝试将当前 Spliterator 分割为两部分，返回新的 Spliterator 处理部分元素，当前 Spliterator 处理剩余元素\n4. estimateSize()：预估剩余元素数量\n5. characteristics()：该 Spliterator 的特征（有序、非空、不可变、SIZED、SUBSIZED 等）\n\ntrySplit() 的分割策略：\n- ArrayList::spliterator() → 二分分割（从中间切分，完美的负载均衡）\n- LinkedList::spliterator() → 从左到右依次分割（因为链表不支持随机访问，只能从头节点分割）\n- HashSet::spliterator() → 基于桶数组的分割\n- 自适应的分割：当 Spliterator 的元素数量足够小时不再继续分割（避免过于碎片化）\n- ForkJoinPool 的公共池会不断调用 trySplit() 直到分割到合适的粒度\n\n如何支持 Stream API 的并行：\n1. Stream.parallel() 将 Stream 包装为并行流\n2. Stream 的 Spliterator 在需要时被分割\n3. ForkJoinPool 的线程依次处理每个分割后的子集\n4. 最终结果通过 reduce/collect 合并\n\n特征（characteristics）的作用：\n- ORDERED：有顺序（如 List），结果需要保持顺序\n- DISTINCT：元素唯一（如 Set），去重优化\n- SORTED：已排序，可以提前终止\n- SIZED：知道精确大小，可以预先分配数组\n- SUBSIZED：trySplit() 后仍知道精确大小\n\n扩展延伸：自定义 Spliterator 可实现高性能的并行遍历。PrimitiveIterator 的 Spliterator（OfInt/OfLong/OfDouble）避免装箱。Java 9 中 Arrays.spliterator() 提供数组区间的 Spliterator。ForkJoinPool 的 work-stealing 使得 Spliterator 的分割结果可以动态再分配。","hints":["Spliterator 通过 trySplit() 递归分割，支持 Stream 的 ForkJoin 并行","分割策略因数据结构而异：ArrayList 二分，LinkedList 依次"],"tags":["Java","Spliterator","Stream","并行"],"id":2149},{"category":"java","difficulty":"medium","type":"short_answer","title":"PriorityQueue 详解","content":"请介绍 Java PriorityQueue 的实现原理和适用场景。它的堆结构是如何维护的？PriorityQueue 与 TreeSet/TreeMap 的区别是什么？","answer":"答案：PriorityQueue 基于二叉堆（Binary Heap）实现的无界优先队列。\n\n实现原理：\n1. 底层使用平衡二叉堆（数组形式存储，根节点索引 0）\n2. 子节点索引：left=2*i+1, right=2*i+2\n3. 父节点索引：(i-1)/2\n4. 插入（offer）：将元素添加到数组末尾，上浮（siftUp）到合适位置 O(log n)\n5. 删除（poll）：移除根节点，将数组末尾元素放到根，下沉（siftDown）到合适位置 O(log n)\n6. 查看不删除（peek）：返回数组[0] O(1)\n7. 非线程安全，初始容量 11（默认），超出时自动扩容（int newCapacity = oldCapacity + (oldCapacity < 64 ? oldCapacity+2 : oldCapacity>>1) 即<64时翻倍+2，否则增长50%）\n\n排序规则：\n- 自然顺序：元素实现 Comparable\n- 比较器：构造函数传入 Comparator\n- 小顶堆（Min-Heap）：最小的元素在堆顶\n- 大顶堆：通过 Comparator.reverseOrder() 实现\n\nPriorityQueue vs TreeSet/TreeMap：\n- PriorityQueue 允许重复元素（TreeSet/TreeMap 不允许）\n- PriorityQueue 只保证堆顶元素是最小/最大的，不保证全序遍历（迭代器不按顺序返回）\n- TreeSet 基于红黑树，保证全序，迭代器按排序顺序返回\n- PriorityQueue 的插入/删除是 O(log n)，TreeSet 也是 O(log n) 但常数更大（红黑树的自旋和染色比堆的 siftUp/Down 复杂）\n\n应用场景：\n1. 任务调度：按优先级执行任务\n2. 合并 K 个有序列表：最小堆每次取最小元素\n3. 滑动窗口最大值：最大堆 + 惰性删除\n4. Dijkstra 最短路径算法\n5. 霍夫曼编码（Huffman Coding）\n\n扩展延伸：PriorityBlockingQueue 是 PriorityQueue 的线程安全版本（ReentrantLock 保护）。Java 8 的 Stream 的 sorted() 操作内部使用了类似优先队列的容器。DelayQueue 内部使用 PriorityQueue 按过期时间排序元素。","hints":["PriorityQueue = 二叉堆（数组），offer siftUp O(log n)，poll siftDown O(log n)","只保证堆顶有序不保证全序，允许重复元素"],"tags":["Java","PriorityQueue","堆","优先队列"],"id":2150},{"category":"java","difficulty":"hard","type":"short_answer","title":"WeakHashMap 与引用类型","content":"请介绍 Java 的 WeakHashMap 和四种引用类型（强、软、弱、虚引用）。WeakHashMap 如何利用弱引用实现自动回收？在哪些场景下使用 WeakHashMap？","answer":"答案：WeakHashMap 的 Entry 使用弱引用（WeakReference）持有 key，当 key 不再被强引用时自动从 Map 中移除。\n\nJava 的四种引用类型（按回收优先级从低到高）：\n1. 强引用（Strong Reference）：new Object()。只要强引用存在，GC 不会回收。\n2. 软引用（SoftReference）：-XX:SoftRefLRUPolicyMSPerMB=1000。内存不足时 GC 回收。适合实现缓存。\n3. 弱引用（WeakReference）：GC 发现弱引用时立即回收（不管内存是否充足）。\n4. 虚引用（PhantomReference）：不能通过 get() 获取对象。用于跟踪对象被回收的通知（Cleaner/Post-mortem cleanup）。\n\nWeakHashMap 原理：\n1. Entry 继承 WeakReference<Key>，key 被弱引用包装\n2. 当 key 不再有强引用时，下次 GC 时 key 被回收，ReferenceQueue 记录该引用\n3. WeakHashMap 在每次操作（get/put/size 等）时调用 expungeStaleEntries() 遍历 ReferenceQueue\n4. 从 ReferenceQueue 中取出被回收的 key 对应的 Entry，从数组和链表中移除\n5. 整个过程不需要手动清理\n\n应用场景：\n1. 元数据存储：ClassLoader 的缓存——Class 被卸载时 metadata 自动清理\n2. ThreadLocal 的 ThreadLocalMap：ThreadLocal 实例被弱引用持有，防止 ThreadLocal 内存泄漏\n3. 监听器注册：组件注销时自动移除监听（避免未注销导致的 listener leak）\n4. 规范映射（Canonicalizing Mapping）：每个唯一实例只存储一份（类似 intern()）\n\n内存泄漏对比：\n- HashMap：key 引用阻止 GC 回收，可能造成内存泄漏\n- WeakHashMap：key 不用时自动被 GC 回收，value 直到 Map 清理时才被回收\n- 注意：如果 value 强引用 key（如 value = key），WeakHashMap 无法回收（因为 value 的强引用阻止 key 变为弱可达）\n\n扩展延伸：WeakHashMap 的 value 也需要合理管理（如果 value 不被其他强引用持有时，回收 key 后 value 也会泄漏直到 expungeStaleEntries 处理）。Google Guava 的 CacheBuilder 提供了更灵活的缓存方案（可以配置基于软引用/弱引用的回收策略，也支持基于时间和容量的回收）。Collections.newSetFromMap(new WeakHashMap<>()) 得到 WeakHashSet。","hints":["WeakHashMap key 用 WeakReference 包装，key 被 GC 回收时自动从 Map 移除","经典应用：ClassLoader metadata 缓存、ThreadLocal 的 ThreadLocalMap"],"tags":["Java","WeakHashMap","引用","GC"],"id":2151},{"category":"java","difficulty":"hard","type":"short_answer","title":"Collections 实用方法","content":"请介绍 java.util.Collections 工具类的重要实用方法。Collections.unmodifiableXXX 和 List.of() 创建不可变集合的区别是什么？Collections.synchronizedXXX 和 ConcurrentHashMap 的性能差异原因是什么？","answer":"答案：Collections 工具类提供了大量操作集合的静态方法。\n\n不可变集合：\n- Collections.unmodifiableList(list)：返回原列表的不可修改视图（底层仍然是原列表，修改原列表会反映到视图）\n- List.of(elements)：JDK 9+，返回真正的不可变列表（底层是专用实现，非视图，原列表修改不影响）\n- Collections.unmodifiableXXX 是视图包装（开销小，但原集合改变时视图也变）\n- List.of/set.of/Map.of 是真正的不可变集合（不可增删改，也不受原集合影响）\n\n同步包装 vs 并发集合：\n- Collections.synchronizedList(list)：在每个方法上 synchronized(this) 包装\n- Collections.synchronizedMap(map)：在每个方法上 synchronized(this) 包装\n- 问题：复合操作（迭代、put-if-absent）仍然需要外部同步\n- 性能：所有操作竞争同一把锁，并发极低\n\n与 ConcurrentHashMap 的对比：\n- ConcurrentHashMap：内部使用 CAS + 分段/红黑树，多线程可以同时读写不同段\n- 读操作不需要加锁（volatile 可见性保证）\n- 写操作只锁特定的桶（窄粒度）\n- 迭代器是弱一致性的（不会抛 ConcurrentModificationException，返回的数据可能略旧）\n- 性能随并发线程数增长（同步包装的吞吐量几乎不增长，甚至下降）\n\n其他重要方法：\n1. Collections.sort(list)：稳定排序 O(n log n)（TimSort）\n2. Collections.binarySearch(list, key)：二分查找 O(log n)，需要先排序\n3. Collections.reverse(list)：反转列表\n4. Collections.rotate(list, distance)：旋转列表（循环偏移）\n5. Collections.shuffle(list)：洗牌（Fisher-Yates 算法）\n6. Collections.swap(list, i, j)：交换元素\n7. Collections.fill(list, obj)：用同一对象填充\n8. Collections.nCopies(n, obj)：创建 n 个相同元素的不可变列表（优化实现）\n9. Collections.disjoint(c1, c2)：检查两个集合是否没有交集\n10. Collections.frequency(c, obj)：统计元素出现次数\n\n扩展延伸：Java 8 的 Collection 接口增加了 removeIf、replaceAll、sort 默认方法。Collections.emptyList() 返回的类型是 Collections.EmptyList（序列化特殊处理）。Collections.singletonList(elem) 返回单元素优化列表。","hints":["unmodifiableXXX = 视图包装（原集合变则视图变），List.of = 真正的不可变","synchronizedXXX 锁粒度粗（整个集合），ConcurrentHashMap 锁粒度细（桶级+CAS）"],"tags":["Java","Collections","不可变集合","同步"],"id":2152},{"category":"java","difficulty":"medium","type":"choice","title":"HashMap 的扰动函数","content":"HashMap 中 hash 扰动函数（hash()）的作用是什么？JDK 8 的实现方式是什么？","options":["A) 直接返回 key 的 hashCode()，不经过任何处理","B) 将 hashCode 的高 16 位与低 16 位进行异或运算，混合高位和低位信息","C) 将 hashCode 乘以一个质数后取模，减少冲突","D) 将 hashCode 进行 MD5 哈希后取前 32 位"],"answer":"B) 将 hashCode 的高 16 位与低 16 位进行异或运算，混合高位和低位信息\n\n解析：JDK 8 的 hash 函数：`(key == null) ? 0 : (h = key.hashCode()) ^ (h >>> 16)`——将 hashCode 的高 16 位与低 16 位做异或。由于 HashMap 计算 bucket 索引时使用 (n - 1) & hash（n 是数组长度，通常是较小的数），高位信息被淹没。扰动函数将高位信息混合到低位中，减少哈希冲突。\n\n扩展延伸：为什么这样设计——1）大多数场景下 key 的 hashCode 分布并不理想（如连续整数的 hashCode 分布好，但自定义对象的 hashCode 不一定）。2）n-1 & hash 中如果 n 较小（如 16），只有低 4 位参与运算。高位信息完全无用。扰动函数将高 16 位与低 16 位异或，让高位也影响低位的分布。3）JDK 7 中的 hashCode 处理更复杂（多次右移和异或），JDK 8 简化为一次（效果足够好且效率更高）。性能方面——位运算（异或和右移）开销极低，几乎不影响 HashMap 的读写性能。","hints":["扰动函数的目的是让高位影响低位——因为 bucket 索引 (n-1) & hash 中 n 小，只有低位参与索引计算","JDK 8 简化了 JDK 7 的多次扰动（4 次位运算）为 1 次（h ^ (h >>> 16)）——性能更好效果相近"],"tags":["Java","集合","HashMap","哈希"],"id":2222},{"category":"java","difficulty":"hard","type":"choice","title":"ConcurrentHashMap 红黑树转链表","content":"在 JDK 8 的 ConcurrentHashMap 中，关于红黑树退化为链表的条件，哪项描述正确？","options":["A) 当红黑树的节点数小于等于 6 时，在每次 put 操作后立即检查并退化","B) 当红黑树的节点数小于等于 8 时，在扩容时进行退化检查","C) 在扩容（transfer）过程中，如果红黑树节点数小于等于 UNTREEIFY_THRESHOLD（6），则退化为链表","D) 红黑树一旦形成后永远不会退化为链表"],"answer":"C) 在扩容（transfer）过程中，如果红黑树节点数小于等于 UNTREEIFY_THRESHOLD（6），则退化为链表\n\n解析：ConcurrentHashMap 的红黑树退化只发生在扩容（transfer）过程中。因为扩容时需要将红黑树中的节点分配到新的 bucket（低位/高位链表），此时如果树中节点数 <= 6（UNTREEIFY_THRESHOLD），则直接以链表形式存放在新的 bucket 中，不需要维护红黑树结构。\n\n扩展延伸：ConcurrentHashMap vs HashMap 的树化差异——1）ConcurrentHashMap 的树化条件与 HashMap 相同：链表长度 >= 8 且数组长度 >= 64。2）退化都是在 resize（扩容）时检查，不是在每次 put/remove 后。3）ConcurrentHashMap 的树节点（TreeNode）与 HashMap 不同——ConcurrentHashMap 的 TreeNode 继承了 ConcurrentHashMap.Node（保留了 next 字段用于链表遍历）。4）退化检查的位置——在 `ConcurrentHashMap.helpTransfer()` 和 `transfer()` 方法中。ConcurrentHashMap 的树化结构维护（TreeBin）包含读写锁（lockState）来同步树操作。","hints":["红黑树退化只在扩容时发生——put/remove 操作后不会立即退化","ConcurrentHashMap 的 TreeNode 保留了 next 字段——即使在树中也可以按链表方式遍历"],"tags":["Java","集合","ConcurrentHashMap","红黑树"],"id":2223},{"category":"java","difficulty":"medium","type":"choice","title":"ArrayList 的 subList 陷阱","content":"关于 ArrayList 的 subList 方法，以下哪项描述是正确的？","options":["A) subList 返回一个新的独立 ArrayList，修改 subList 不影响原 List","B) subList 返回的是原 List 的一个视图（View），对 subList 的结构性修改会影响原 List","C) subList 返回的 List 支持添加（add）和删除（remove）操作且不影响原 List","D) subList 返回的 List 是只读的，不能通过 add 或 remove 修改"],"answer":"B) subList 返回的是原 List 的一个视图（View），对 subList 的结构性修改会影响原 List\n\n解析：ArrayList.subList() 返回的是原 ArrayList 的内部视图（SubList 类），与原 ArrayList 共享同一个数组。修改 subList 的内容（如 set）会影响原 ArrayList。对 subList 进行结构性修改（add/remove）也会影响原 ArrayList。如果在对 subList 操作期间修改了原 ArrayList（通过原 List 或另一个 subList），subList 会抛 ConcurrentModificationException。\n\n扩展延伸：详细行为——1）subList 的修改反映到原 List：`list.subList(0, 2).clear()` 等价于直接删除 [0, 2) 范围的元素。2）subList 的视图特性：subList 的 add/remove 操作会通过 SubList 内部的 add/remove 方法修改原 ArrayList 的 size 和 elementData。3）ConcurrentModificationException：如果通过原 ArrayList 修改（如 list.add('x')）后继续使用 subList，subList 检测到 modCount 不匹配而抛异常。4）使用场景：安全地通过 subList 进行范围操作（`list.subList(from, to).clear()` 删除范围元素）。5）内存泄漏风险：subList 持有原 ArrayList 的引用，如果长期持有大 List 的 subList，原 List 无法被 GC 回收。","hints":["subList 是视图（View）而非副本——修改 subList 直接影响原 ArrayList","使用 subList 期间通过原 List 修改会抛 ConcurrentModificationException——subList 的 modCount 与原 List 同步"],"tags":["Java","集合","ArrayList","subList"],"id":2224},{"category":"java","difficulty":"hard","type":"choice","title":"TreeMap 与红黑树","content":"关于 TreeMap 的底层实现，以下哪项描述是正确的？","options":["A) TreeMap 的 key 必须实现 Comparable 接口或在构造时传入 Comparator","B) TreeMap 使用哈希表作为底层数据结构，查找时间复杂度为 O(1)","C) TreeMap 允许 null key（和 HashMap 一样）","D) TreeMap 的所有操作都是线程安全的"],"answer":"A) TreeMap 的 key 必须实现 Comparable 接口或在构造时传入 Comparator\n\n解析：TreeMap 基于红黑树（Red-Black Tree）实现——key 通过比较器（Comparator 或 Comparable）决定排序。如果 key 没有实现 Comparable 且构造时没有提供 Comparator，put 操作会抛出 ClassCastException。\n\n扩展延伸：TreeMap 特性——1）有序性：TreeMap 按 key 的自然顺序（Comparable）或 Comparator 顺序存储，遍历时输出有序序列。2）时间复杂度：增删查均为 O(log n)——基于红黑树。3）null key：TreeMap 不允许 null key（需要比较 null 时会抛 NullPointerException——除非自定义 Comparator 处理 null）。4）非线程安全：需要线程安全时使用 Collections.synchronizedSortedMap。5）常用方法：firstKey()/lastKey()、headMap()/tailMap()/subMap()、lowerKey()/higherKey()/floorKey()/ceilingKey()。场景——1）需要有序遍历的集合（如排行榜的分数排序）。2）需要按范围查找（查询某一区间的元素）。3）NavigableMap 接口提供导航方法（如查找最接近给定 key 的 entry）。与 HashMap 选择——需要排序功能时才用 TreeMap（O(log n) vs O(1)）。","hints":["TreeMap 基于红黑树（有序），HashMap 基于哈希表（无序）——选择依据是需要排序还是有 O(1) 的查找","TreeMap 不允许 null key（需要比较时无法处理 null），HashMap 允许一个 null key"],"tags":["Java","集合","TreeMap","红黑树"],"id":2225},{"category":"java","difficulty":"easy","type":"choice","title":"List 与 Set 的区别","content":"关于 List 和 Set 的区别，以下哪项描述是正确的？","options":["A) List 保证元素有序且允许重复，Set 不允许重复但不保证有序","B) Set 一定比 List 的查找速度快","C) List 只能存储相同类型的元素，Set 可以存储不同类型的元素","D) List 和 Set 的底层实现完全相同，只是接口不同"],"answer":"A) List 保证元素有序且允许重复，Set 不允许重复但不保证有序\n\n解析：List（如 ArrayList/LinkedList）维护元素的插入顺序（有序性），允许重复元素，可以通过索引访问。Set（如 HashSet/TreeSet/LinkedHashSet）不允许重复元素但有序性因实现而异——HashSet 无序、TreeSet 排序、LinkedHashSet 按插入顺序。\n\n扩展延伸：详细区别——1）List 特性：有序（按插入顺序或索引访问）。允许 null（取决于实现，ArrayList 允许多个 null）。可通过 get(index) 随机访问。2）Set 特性：元素唯一（equals/hashCode 决定）。TreeSet 不允许 null（需要比较）。HashSet 允许一个 null。EnumSet 不允许 null。3）选择依据：需要索引访问或需要保留插入顺序且允许重复 → List。需要元素唯一性 → Set。需要自动排序 → TreeSet 或 TreeMap。4）HashSet 的底层使用 HashMap（元素作为 key，常量作为 value），TreeSet 使用 TreeMap（元素作为 key，常量作为 value）。LinkedHashSet 使用 LinkedHashMap。","hints":["List — 有序、可重复、索引访问；Set — 元素唯一、有序性因实现而异","HashSet 底层是 HashMap（元素作为 key）——利用 HashMap 的 key 唯一性"],"tags":["Java","集合","List","Set"],"id":2226},{"category":"java","difficulty":"easy","type":"choice","title":"CopyOnWriteArrayList 迭代器","content":"CopyOnWriteArrayList 的迭代器具有什么特性？","options":["A) fail-fast：检测到修改时抛ConcurrentModificationException","B) 弱一致性：迭代器遍历的是快照，不反映后续修改","C) 实时性：迭代器总是返回最新数据","D) 不支持 remove 操作"],"answer":"B","analysis":"CopyOnWriteArrayList 的迭代器是弱一致性（Weakly Consistent）的。迭代器在创建时获得了当前数组的快照（snapshot），遍历过程中即使原数组被修改，迭代器仍然使用创建时的旧数组。所以迭代器不会抛 ConcurrentModificationException，但也看不到后续的修改。选项 C 错误（不是实时）。选项 D 错误——CopyOnWriteArrayList 的迭代器支持 remove() 操作，但会在原数组上执行（创建新数组），不影响当前遍历的快照。","tags":["Java","CopyOnWriteArrayList","迭代器","弱一致性"],"id":2248},{"category":"java","difficulty":"easy","type":"choice","title":"ArrayDeque 与 LinkedList","content":"以下关于 ArrayDeque 和 LinkedList 的描述哪个是正确的？","options":["A) ArrayDeque 和 LinkedList 都实现了 Deque 接口","B) ArrayDeque 在头部插入比 LinkedList 快","C) LinkedList 可以作为队列也可以作为列表，ArrayDeque 只能作为队列","D) 以上全部正确"],"answer":"D","analysis":"ArrayDeque 和 LinkedList 都实现了 Deque 和 Queue 接口。ArrayDeque 在头部插入（addFirst）的时间复杂度虽然是 O(n)（因为数组复制），但由于系统调用 System.arraycopy 的高度优化，实际性能通常优于 LinkedList（每个节点需要创建 Node 对象，产生分配开销和 GC 压力）。LinkedList 还实现了 List 接口（可以作为列表使用），ArrayDeque 只实现了 Deque/Queue。所以三个选项都正确。","tags":["Java","ArrayDeque","LinkedList","Deque"],"id":2249},{"category":"java","difficulty":"easy","type":"choice","title":"集合排序稳定性","content":"关于 Java 集合排序的稳定性，以下哪个描述是正确的？","options":["A) Collections.sort() 使用快速排序，是不稳定的","B) Collections.sort() 使用 TimSort，是稳定的","C) List.sort() 使用归并排序，是稳定的","D) Arrays.sort() 对所有类型都使用 TimSort"],"answer":"B","analysis":"Collections.sort() 内部调用 List.sort()，后者使用 TimSort（归并排序 + 插入排序的混合）。TimSort 是稳定排序。Arrays.sort() 对对象使用 TimSort（稳定），对基本类型使用 Dual-Pivot QuickSort（不稳定，但基本类型不需要稳定性）。","tags":["Java","排序","Collections","TimSort"],"id":2250},{"category":"java","difficulty":"easy","type":"choice","title":"EnumMap 性能","content":"关于 EnumMap 的性能和实现，以下哪个描述是正确的？","options":["A) EnumMap 内部使用红黑树存储键值对","B) EnumMap 内部使用数组存储，键的 ordinal 作为索引","C) EnumMap 与 HashMap 性能完全相同","D) EnumMap 不保证迭代顺序"],"answer":"B","analysis":"EnumMap 内部使用两个等长数组（keys 和 values），数组长度等于枚举常量的数量。键的 ordinal() 值作为数组索引，所以所有操作都是 O(1) 且非常快。EnumMap 的迭代器按枚举常量的声明顺序（ordinal 顺序）返回元素，是有序的。","tags":["Java","EnumMap","枚举","性能"],"id":2251},{"category":"java","difficulty":"easy","type":"choice","title":"PriorityQueue 批量建堆","content":"使用 PriorityQueue 的构造函数传入一个已有集合（如 ArrayList）时，建堆的时间复杂度是多少？","options":["A) O(n) — 使用 heapify 操作","B) O(n log n) — 逐个插入元素","C) O(1) — 直接使用集合的顺序","D) O(n²) — 最坏情况"],"answer":"A","analysis":"PriorityQueue(Collection<? extends E> c) 构造函数调用 initFromCollection(c)，然后调用 heapify() 方法。heapify() 从最后一个非叶子节点开始向下调整（siftDown），时间复杂度为 O(n)。如果逐个调用 offer() 插入，才是 O(n log n)。这是堆的一个重要性质——批量建堆（Bottom-Up Heap Construction）可以在线性时间内完成。","tags":["Java","PriorityQueue","建堆","heapify"],"id":2252},{"category":"java","difficulty":"medium","type":"multiple_choice","title":"线程池饱和策略","content":"ThreadPoolExecutor 的饱和策略（RejectedExecutionHandler）包括哪些？以下哪些是正确的？（多选）","options":["A) AbortPolicy：直接抛出 RejectedExecutionException（默认策略）","B) CallerRunsPolicy：由提交任务的线程自己执行该任务","C) DiscardPolicy：丢弃当前任务，不抛异常","D) DiscardOldestPolicy：丢弃队列中最旧的任务，然后重试提交"],"answer":"A)、B)、C)、D)。\n\n解析：四种内置拒绝策略——1）AbortPolicy（默认）：直接抛 RejectedExecutionException——调用者必须处理异常。2）CallerRunsPolicy：不进入线程池，由提交任务的线程自己执行任务（降低任务提交速率，天然背压机制）。3）DiscardPolicy：丢弃任务，不抛异常（可能丢失重要任务——需谨慎使用）。4）DiscardOldestPolicy：丢弃队列头部（等待最久）的任务，然后重新提交当前任务（重试）。\n\n扩展延伸：拒绝策略触发时机——当线程池核心线程已满、等待队列已满、最大线程已满且新任务提交时。选择策略——1）默认 AbortPolicy：适合调用者能够处理异常的场景。2）CallerRunsPolicy：最推荐——将任务推回调用者（如 Web 服务器中，请求线程自己处理 → 客户端感受到延迟 → 自动降低请求速率）。天然背压。3）DiscardPolicy：适合非关键任务（如日志记录——丢失几条日志影响不大）。4）DiscardOldestPolicy：优先级任务系统——丢弃最早的未处理任务，优先处理新任务。自定义策略——实现 RejectedExecutionHandler 接口的 rejectedExecution(Runnable r, ThreadPoolExecutor executor) 方法（如写入消息队列或数据库，留给后续处理）。","hints":["CallerRunsPolicy 是天然的背压机制——提交任务的线程自己执行，自动降低提交速率","DiscardOldestPolicy 丢弃等待最久的任务——适合优先级高的新任务替代旧任务的场景"],"tags":["Java","并发","线程池","拒绝策略"],"id":2307},{"category":"java","difficulty":"hard","type":"multiple_choice","title":"Java 内存模型与重排序","content":"以下关于 Java 内存模型（JMM）和指令重排序的描述，哪些是正确的？（多选）","options":["A) 编译器和处理器可能对指令进行重排序，as-if-serial 语义保证单线程下的重排序不影响执行结果","B) volatile 关键字可以禁止特定类型的指令重排序","C) 构造函数中的赋值操作一定在对象引用发布之前执行——不存在任何重排序","D) 内存屏障（Memory Barrier）可以阻止屏障前后的特定类型指令重排序"],"answer":"A)、B)、D)。\n\n解析：A 正确：as-if-serial 语义——单线程中不管怎么重排序，执行结果不能改变。B 正确：volatile 的 happens-before 规则——对 volatile 变量的写操作之前的操作不会被重排序到写之后，读操作之后的操作不会被重排序到读之前。D 正确：内存屏障是 CPU 级别的指令，JVM 在适当位置插入屏障来禁止特定类型的重排序。C 错误——构造函数的指令可能被重排序（如 new T() 的分配内存 → 设置引用 → 初始化中可能被重排序为 分配内存 → 设置引用 → 初始化）。这是双重检查锁定（DCL）需要 volatile 的原因。\n\n扩展延伸：重排序类型——1）编译器重排序（Compiler Reorder）：编译器在不改变语义的前提下调整指令顺序。2）处理器重排序（Processor Reorder）：CPU 乱序执行（Out-of-Order Execution）或内存系统重排序。3）JMM 的 happens-before 规则约束了允许的重排序。内存屏障类型——LoadLoad（读屏障）、StoreStore（写屏障）、LoadStore、StoreLoad。volatile 的写操作前插入 StoreStore，写操作后插入 StoreLoad。volatile 的读操作后插入 LoadLoad + LoadStore。final 的特殊语义——JMM 保证在构造器中正确初始化 final 字段后，其他线程看到对象引用时一定看到正确的 final 值（不需要同步）。","hints":["构造函数中对象引用赋值可能在初始化之前发生——这是 DCL 单例需要 volatile 的原因","volatile 禁止特定重排序——写前插入 StoreStore + 写后插入 StoreLoad，读后插入 LoadLoad + LoadStore"],"tags":["Java","JMM","重排序","内存屏障"],"id":2308},{"category":"java","difficulty":"easy","type":"multiple_choice","title":"访问修饰符","content":"以下关于 Java 访问修饰符的描述，哪些是正确的？（多选）","options":["A) private 成员只能在当前类内部访问","B) default（无修饰符）成员只能在同一包内访问","C) protected 成员可以在不同包的子类中访问","D) public 成员在所有类中都可以访问"],"answer":"A)、B)、C)、D)。\n\n解析：Java 访问修饰符的访问范围从严格到宽松——private（当前类）< default / package-private（同一包）< protected（同一包 + 不同包子类）< public（所有类）。四个选项全部正确。\n\n扩展延伸：访问控制细节——1）private：只在同一类中可见。常用于封装内部实现细节。2）default（无修饰符，也称 package-private）：同一包中的所有类可见。是 Java 的默认访问级别。3）protected：同一包可见 + 不同包的子类可见。注意：在不同包的子类中，protected 成员只能通过继承关系访问（不能通过父类引用访问）。4）public：所有类可见。构成类的公开 API。设计原则——最小化访问权限（信息隐藏原则）。能 private 就不要 default，能 default 就不要 protected，能 protected 就不要 public。对测试类——通常使用同包的方式进行测试（测试类和被测试类同包，使用 default 访问级别）。模块系统（Java 9+）的额外控制——模块可以导出特定包给特定模块（exports package to module）。","hints":["访问修饰符范围：private < default < protected < public——最小化访问权限原则","protected 在不同包子类中的访问需要通过继承关系（不能通过父类引用访问 protected 成员）"],"tags":["Java","修饰符","访问控制","封装"],"id":2309},{"category":"java","difficulty":"hard","type":"multiple_choice","title":"Lambda 与函数式接口","content":"关于 Java Lambda 表达式和函数式接口，以下哪些描述是正确的？（多选）","options":["A) 函数式接口是只有一个抽象方法的接口（可以有多个 default/static 方法）","B) @FunctionalInterface 注解可以标记函数式接口，如果有多个抽象方法会编译错误","C) Lambda 表达式编译后生成匿名内部类（字节码层面）","D) Lambda 表达式可以引用外部变量，这些变量必须是 final 或 effectively final"],"answer":"A)、B)、D)。\n\n解析：A 正确：函数式接口必须「有且仅有一个」抽象方法（可以有 default 方法和 static 方法，也可以有 Object 类的 public 抽象方法如 toString/equals/hashCode）。B 正确：@FunctionalInterface 是可选标记——编译器检查是否符合函数式接口定义。C 错误：Lambda 在编译后通过 invokedynamic（方法句柄）实现（而非匿名内部类）。JDK 8+ 生成的 Lambda 使用 bootstrap 方法和 LambdaMetafactory 动态创建函数接口的实例，而不是在编译时生成匿名内部类字节码。D 正确：Lambda 捕获的局部变量必须是 final 或 effectively final（值在初始化后未变化）。\n\n扩展延伸：Lambda 实现原理——1）invokedynamic（JDK 7 引入）：Lambda 表达式编译为 invokedynamic 指令，在运行时通过 LambdaMetafactory 生成函数接口的实现。2）vs 匿名内部类：匿名内部类编译生成独立的 .class 文件（会增加类加载开销和 PermGen/Metaspace 占用）。Lambda 没有额外的 .class 文件。3）变量捕获：Lambda 捕获的局部变量被复制到 Lambda 中（因此要求 effectively final——即使变化了 Lambda 中的副本也不会同步）。实例变量（this.field）可以被修改（因为 this 的引用被传递而非变量本身）。4）性能：Lambda 比匿名内部类性能更好（invokedynamic 避免了额外的类加载和方法分派开销）。串行 Stream 的 Lambda 经过 JIT 内联优化后与手写 for 循环性能相近。","hints":["Lambda 使用 invokedynamic（方法句柄）而非匿名内部类——没有额外 .class 文件，性能更好","Lambda 捕获的局部变量必须是 effectively final——否则编译错误"],"tags":["Java","Lambda","函数式","Stream"],"id":2310},{"category":"java","difficulty":"easy","type":"multiple_choice","title":"Record 的限制","content":"关于 Java Record 的限制，以下哪些是正确的？（多选）","options":["A) Record 不能继承其他类","B) Record 可以声明额外的实例字段","C) Record 可以是 abstract 的","D) Record 可以实现接口","E) Record 自动生成 equals() 和 hashCode()","F) Record 的字段默认是 final 的"],"answer":["A","D","E","F"],"analysis":"Record 不能继承其他类（extends implicit java.lang.Record），但可以实现接口。Record 的所有字段都是 private final 的，自动生成 equals()、hashCode()、toString()。Record 不能声明额外的实例字段（只能声明在 components 中）。Record 不能是 abstract（设计上就是 final 的）。选项 B 错误（不能有额外实例字段），选项 C 错误（不能是 abstract）。","tags":["Java","Record","不可变","JDK 16"],"id":2333},{"category":"java","difficulty":"easy","type":"multiple_choice","title":"Stream 终止操作","content":"以下哪些是 Stream 的终止操作（Terminal Operation）？（多选）","options":["A) map()","B) filter()","C) collect()","D) reduce()","E) sorted()","F) forEach()"],"answer":["C","D","F"],"analysis":"Stream 的终止操作触发流水线的执行并产生结果或副作用。collect()、reduce()、forEach() 是终止操作。map()、filter()、sorted() 是中间操作（Intermediate Operation），返回新的 Stream 且惰性求值。中间操作不会立即执行，直到遇到终止操作才开始处理数据。","tags":["Java","Stream","终止操作","函数式"],"id":2334},{"category":"java","difficulty":"hard","type":"multiple_choice","title":"Java 模块化系统限制","content":"以下哪些是 Java Platform Module System (JPMS) 的特性？（多选）","options":["A) 模块必须显式导出包以允许其他模块访问","B) 模块化 JAR 中的非模块化代码不能访问模块化 JAR 中的类","C) 模块可以指定依赖的模块通过 requires transitive 传递","D) 模块路径上的类自动拥有访问所有模块的权限","E) 允许使用 --add-opens 在运行时打开模块的包进行反射访问","F) 模块化应用一定比非模块化应用启动更快"],"answer":["A","C","E"],"analysis":"JPMS 的核心特性包括：模块通过 exports 显式导出包（选项 A）、requires transitive 传递依赖（选项 C）、反射访问可以用 --add-opens 运行时开放（选项 E，Spring 等框架常用）。选项 B 错误——未命名模块（unnamed module，classpath 上的代码）可以访问模块化 JAR 中导出的类。选项 D 错误——模块路径上的类遵循模块化规则，不能自动访问所有类。选项 F 错误——模块化不一定更快，甚至可能更慢（启动时有模块解析开销），模块化的主要优势是强封装和可靠的配置。","tags":["Java","JPMS","模块化","反射"],"id":2335},{"category":"java","difficulty":"easy","type":"multiple_choice","title":"垃圾回收器适用场景","content":"以下关于 GC 的叙述哪些是正确的？（多选）","options":["A) Serial GC 适合单线程环境或小型应用","B) Parallel GC 适合对吞吐量要求高的多线程应用","C) G1 GC 适合需要可预测暂停时间的大堆应用","D) ZGC 适合需要极低延迟（<10ms）的大堆应用","E) ZGC 只能在 Linux 上使用","F) G1 GC 在 JDK 8 中是默认的 GC"],"answer":["A","B","C","D"],"analysis":"Serial GC（串行收集器）适合单线程、小堆场景（选项 A）。Parallel GC 追求最大吞吐量，适合多线程批处理应用（选项 B）。G1 GC 通过 Region 划分和可预测的暂停时间模型适合大堆应用（选项 C）。ZGC 目标是 <10ms 暂停时间，适合大堆延迟敏感应用（选项 D）。选项 E 错误——ZGC 在 JDK 14+ 支持 Windows 和 macOS（Linux 优先，但现已支持所有主要平台）。选项 F 错误——JDK 8 的默认 GC 是 Parallel GC（G1 从 JDK 9 起成为默认 GC）。","tags":["Java","GC","G1","ZGC"],"id":2336},{"category":"java","difficulty":"hard","type":"multiple_choice","title":"LockSupport 特性","content":"以下关于 LockSupport 的描述哪些是正确的？（多选）","options":["A) park() 使当前线程进入 WAITING 状态","B) unpark(thread) 可以唤醒指定线程","C) park() 和 unpark() 不区分先后顺序，提前 unpark 后再 park 不会阻塞","D) LockSupport 使用基于信号量的许可证机制","E) park() 响应中断但不会抛出 InterruptedException","F) parkNanos() 支持纳秒级别的超时"],"answer":["A","B","C","D","E","F"],"analysis":"所有选项都是正确的。LockSupport 是 Java 并发包的基石——AQS（AbstractQueuedSynchronizer）底层使用 LockSupport 实现线程阻塞/唤醒。park()/unpark() 基于许可证（permit）机制，许可证的累积上限为 1。提前调用 unpark() 后，再调用 park() 会消耗已有的许可证，线程不会阻塞。park() 响应中断（线程设置中断标志，返回而不抛异常）。parkNanos() 支持纳秒超时。ReentrantLock、CountDownLatch、Semaphore 等同步器都基于 AQS + LockSupport 实现。","tags":["Java","LockSupport","AQS","线程"],"id":2337},{"category":"java","difficulty":"easy","type":"true_false","title":"StringBuilder 与 StringBuffer","content":"StringBuilder 是线程安全的，StringBuffer 是线程不安全的。因此 StringBuilder 适合在多线程环境下使用，而 StringBuffer 只适合在单线程环境下使用。","options":["正确","错误"],"answer":"错误。\n\n解析：事实正好相反——StringBuffer 的方法是 synchronized 修饰的（线程安全），适合多线程场景但性能较低。StringBuilder 的方法没有同步（线程不安全），适合单线程场景但性能更高（无锁开销）。一般推荐在单线程环境优先使用 StringBuilder。\n\n扩展延伸：性能对比——StringBuilder 比 StringBuffer 快 10-20%（无 synchronized 开销）。字符串拼接的场景选择——1）单次简单拼接（用 + 操作符即可，编译器优化为 StringBuilder）。2）循环内大量拼接（显式使用 StringBuilder）。3）多线程下共享字符串拼接（使用 StringBuffer 或手动同步 StringBuilder）。String 的不可变性——String 是 immutable 的，每次修改都创建新对象（频繁拼接会产生大量垃圾对象）。","hints":["StringBuffer 才是线程安全的（方法用 synchronized），StringBuilder 不是","单线程下优先使用 StringBuilder——无锁开销，比 StringBuffer 快 10-20%"],"tags":["Java","StringBuilder","StringBuffer","线程安全"],"id":2408},{"category":"java","difficulty":"easy","type":"true_false","title":"try-with-resources 资源关闭","content":"try-with-resources 语句中声明的资源（实现了 AutoCloseable 接口的对象）会在 try 块正常执行完毕后自动关闭，但如果 try 块中抛出了异常，资源将不会被关闭。","options":["正确","错误"],"answer":"错误。\n\n解析：try-with-resources 保证资源在 try 块结束后一定会被关闭——无论正常结束还是异常结束。资源的 close() 方法是在 finally 机制中自动调用的（实际上，编译后 try-with-resources 会展开为 try-finally 结构）。如果 try 块和 close() 都抛出异常，try 块的异常会被抑制（suppressed），close() 的异常可以通过 getSuppressed() 获取。\n\n扩展延伸：try-with-resources 的增强——1）多个资源时声明顺序：`try (FileInputStream fis = new FileInputStream(\"a.txt\"); FileOutputStream fos = new FileOutputStream(\"b.txt\"))`。多个资源的 close 顺序与声明顺序相反（后声明先关闭）。2）Java 9 增强：可以在 try 外部声明资源引用，在 try 中直接引用（`Resource r = ...; try (r) { ... }`）。3）自动关闭的原理：反编译显示 try-with-resources 编译为 try-finally + catch 的嵌套结构。在 finally 中调用 close() 且使用 Throwable.addSuppressed 保存异常。注意——使用 try-with-resources 比手动在 finally 中关闭资源更安全（不会遗漏 close 调用，且正确处理异常抑制）。","hints":["try-with-resources 保证资源一定会关闭（无论正常执行还是异常退出）——编译为 try-finally 闭","try 块的异常可以抑制 close() 的异常（通过 getSuppressed() 获取被抑制的异常）"],"tags":["Java","异常","try-with-resources","资源管理"],"id":2409},{"category":"java","difficulty":"easy","type":"true_false","title":"Java 接口默认方法的冲突","content":"如果一个类同时实现两个接口，两个接口包含同名的 default 方法，该类的实例可以直接调用该 default 方法，编译不会报错。","options":["正确","错误"],"answer":"错误。\n\n解析：如果一个类实现的两个接口包含相同签名的 default 方法，编译时报错（必须在该类中重写该方法来明确调用哪个接口的 default 方法）。使用 `InterfaceName.super.methodName()` 语法明确选择。\n\n扩展延伸：冲突解决规则——1）类优先：如果父类和接口中有相同签名的方法，父类的方法优先于接口的 default 方法（不管接口是何时添加的）。2）接口间冲突：如果两个接口有相同签名的方法，且都提供了 default 实现，则实现类必须重写该方法。3）重写时通过 `super` 调用：`InterfaceA.super.method()` 调用接口 A 的 default 实现。设计注意事项——Default 方法的主要目的是接口进化（向已有接口添加新方法而不破坏现有实现）。但使用时需注意多接口冲突问题。Java 8 在 Collection 接口中添加了 forEach、stream 等 default 方法。","hints":["两个接口的同名 default 方法冲突时——实现类必须重写，用 Interface.super.method() 调用指定接口的默认实现","类优先原则——父类的具体方法优先于接口的 default 方法"],"tags":["Java","接口","Default方法","冲突"],"id":2410},{"category":"java","difficulty":"hard","type":"true_false","title":"Java 静态内部类","content":"静态内部类（static nested class）可以访问外部类的实例成员（包括私有实例成员）。非静态内部类（inner class）不能声明 static 成员。","options":["正确","错误"],"answer":"错误。\n\n解析：前半部分错误——静态内部类只能访问外部类的静态成员（包括私有静态成员），不能直接访问外部类的实例成员（因为没有外部类对象的引用）。后半部分正确——非静态内部类（内部类）不能声明 static 成员（包括 static 方法、static 字段、static 内部类）。因为非静态内部类的实例依赖于外部类实例，static 成员不依赖于实例。\n\n扩展延伸：内部类类型——1）静态内部类：不需要外部类实例的引用，只能访问外部类的静态成员。作为「与外部类相关但不需要外部实例」的封装。2）非静态内部类（成员内部类）：持有外部类实例的引用（`Outer.this`），可以访问外部类的所有成员。创建前需要先创建外部类实例：`new Outer().new Inner()`。3）局部内部类（方法内定义的类）：在方法中定义，访问方法内的局部变量需要是 effectively final。4）匿名内部类：无类名，通常用于事件监听器和回调。持有外部类实例的引用（可能导致内存泄漏）。内存泄漏风险——非静态内部类持有外部类实例的引用。如果内部类对象长期存活而外部类不再使用，外部类和所有引用的对象都不能被 GC 回收（常见于 Android 的 Activity 泄漏和 Swing 的事件监听器泄漏）。","hints":["静态内部类没有外部类实例引用——只能访问外部类的静态成员","非静态内部类持有外部类实例的强引用——可能导致外部类无法被 GC 回收（内存泄漏）"],"tags":["Java","内部类","静态","内存"],"id":2411},{"category":"java","difficulty":"easy","type":"true_false","title":"Java 枚举的构造器","content":"Java 枚举（enum）类型的构造器默认是 private 的，如果显式声明为 public 或 protected 会导致编译错误。枚举常量在枚举类加载时创建，是线程安全的。","options":["正确","错误"],"answer":"正确。\n\n解析：枚举的构造器隐式为 private（即使不加 private 修饰，也默认为 private）。显式声明为 public 或 protected 会编译错误。枚举常量在类加载时由 JVM 保证线程安全地创建（利用 class loader 的同步特性）。这使得枚举是实现单例模式的最安全方式——反射无法创建枚举实例（Constructor.newInstance() 检查是否为枚举类型并拒绝）。\n\n扩展延伸：枚举特性——1）枚举实例的创建：每个枚举常量在 enum 类加载时由 JVM 静态初始化（确保线程安全）。2）枚举实现单例：Effective Java 推荐的单例实现方式——避免反射攻击（Constructor.newInstance() 对枚举类型抛异常）、避免序列化攻击（枚举的序列化由 JVM 特殊处理，不会创建新实例）。3）枚举与 switch：枚举与 switch 配合良好（编译时编译器检查 case 覆盖了所有枚举值，否则可能需要 default）。4）枚举的字段和方法：枚举可以有实例字段（在构造器中初始化）、实例方法、抽象方法（每个枚举常量分别实现）。5）EnumSet 和 EnumMap：针对枚举类型优化的高性能集合实现。EnumSet 使用位向量（bit vector）存储，极省内存且极快。","hints":["枚举构造器默认为 private——反射无法创建枚举实例，枚举是实现安全单例的最佳方式","枚举常量在类加载时由 JVM 保证线程安全——避免了 DCL 和序列化问题"],"tags":["Java","枚举","单例","线程安全"],"id":2412},{"category":"java","difficulty":"easy","type":"true_false","title":"StringBuilder 线程安全","content":"StringBuilder 是线程安全的，可以在多线程环境中使用。","answer":"false","analysis":"StringBuilder 不是线程安全的。其方法（append、insert、delete 等）不进行同步控制。在多线程环境中应使用 StringBuffer（同步方法）或通过外部同步保护 StringBuilder。","tags":["Java","StringBuilder","StringBuffer","线程安全"],"id":2433},{"category":"java","difficulty":"easy","type":"true_false","title":"switch 表达式有返回值","content":"Java 14+ 的 switch 表达式（Switch Expression）可以有返回值。","answer":"true","analysis":"JDK 14 正式引入了 Switch Expression，使用 -> 语法，可以直接赋值给变量：int num = switch(day) { case MON -> 1; case TUE -> 2; default -> 0; }。switch 表达式是穷尽的（必须覆盖所有可能值），使用 yield 关键字从代码块中返回值。","tags":["Java","switch","表达式","JDK 14"],"id":2434},{"category":"java","difficulty":"easy","type":"true_false","title":"Optional 可以序列化","content":"java.util.Optional 实现了 Serializable 接口。","answer":"false","analysis":"Optional 没有实现 Serializable 接口。如果在领域对象中使用 Optional 作为字段并需要序列化（如将对象存入数据库或通过网络传输），会导致 NotSerializableException。正确做法是 Optional 仅作为返回类型使用（方法返回值），不应作为类的字段。","tags":["Java","Optional","序列化","最佳实践"],"id":2435},{"category":"java","difficulty":"easy","type":"true_false","title":"finally 块不执行","content":"在 try-catch-finally 中，如果在 try 块中调用 System.exit(0)，finally 块不会执行。","answer":"true","analysis":"当调用 System.exit(0) 时，JVM 立即退出，不再执行 finally 块。类似的情况还有：线程被杀死（Thread.stop 或 OS 级 kill -9）、JVM 崩溃、电源故障等。除此之外，正常情况下 finally 块保证执行（即使 try 中有 return）。","tags":["Java","finally","异常","JVM"],"id":2436},{"category":"java","difficulty":"hard","type":"true_false","title":"CMS GC 已废弃","content":"CMS（Concurrent Mark Sweep）垃圾回收器在 JDK 14 中被标记为废弃，在 JDK 14 中被正式移除。","answer":"true","analysis":"CMS GC 在 JDK 9 中被标记为废弃（@Deprecated），在 JDK 14 中被正式移除（JEP 363）。废弃的原因包括：CMS 存在碎片化问题（promotion failed）、需要大量 CPU 时间、与 G1 相比没有明显优势。G1 GC 在 JDK 9 中成为默认 GC，很好地替代了 CMS 的角色。","tags":["Java","CMS","GC","JVM"],"id":2437}];

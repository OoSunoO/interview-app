// Auto-generated by scripts/generate-data.js. Do not edit.
// Category: algorithm_extras — 36 questions
export const category = "algorithm_extras";
export const questions = [{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"Manacher 算法与最长回文子串","content":"请解释 Manacher 算法求最长回文子串的线性时间原理。","answer":"答案：Manacher 算法利用回文的对称性和已计算信息避免重复中心扩展\n\n解析：传统中心扩展法 O(N^2)，每个中心向两边扩展。Manacher 核心思想：在已找到的回文范围内，利用对称性直接获得内部位置的部分回文半径，减少不必要的扩展。具体使用一个数组 P 记录每个位置的回文半径，维护当前最右回文边界 R 和对应中心 C。遍历时若 i 在 R 范围内，P[i] = min(R-i, P[2*C-i])（对称位置的回文半径），然后继续扩展。插入分隔符（如 #）统一处理奇偶回文。总复杂度 O(N)。\n\n扩展延伸：Manacher 本质上是 DP 的思想 + 对称性剪枝。相比中心扩展法的提升在于利用已计算结果的镜像信息跳过重复检查。扩展应用：1）最长回文子串长度 2）判断子串 s[l:r] 是否为回文（需同时记录原始索引信息）3）回文子串计数。注意 Manacher 不适用于旋转回文或非相邻回文问题。","hints":["奇偶回文怎么统一处理","P[i] = min(R-i, P[mirror]) 为什么成立"],"tags":["字符串","回文","Manacher"],"id":560},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"KMP 字符串匹配算法","content":"请解释 KMP 算法的核心思想和 next 数组的构建方法。","answer":"答案：KMP 利用模式串的部分匹配信息避免主串指针回溯\n\n解析：暴力匹配在主串不匹配时从头开始，KMP 利用已匹配部分（前缀后缀最长公共长度）来确定模式串向右滑动的位数。next 数组（部分匹配表）记录了模式串每个前缀的最长公共前后缀长度。构建 next 数组时用两个指针 i（后缀）和 j（前缀），若匹配则 j++ 并赋值，不匹配则 j 回到 next[j-1]（递归利用已计算出的信息）。匹配时主串指针 i 不回溯，模式串指针 j 根据不匹配位置跳到 next[j-1]。\n\n扩展延伸：时间复杂度 O(N+M)，空间 O(M)。next 数组的变种：1）next 数组不含自身版本（第一个位置 = 0）用于求循环节 2）优化版 nextval 避免连续相同字符的无效跳转。循环节计算：len(P) - next[len(P)] 为最小循环节长度，能整除 len(P) 表示字符串是循环周期串。KMP 的局限性：含通配符的问题不适合，更复杂的字符串匹配可用 Z 算法或后缀数组。","hints":["next 数组构建时字符不匹配为什么 j = next[j-1]","KMP 怎么判断字符串是否由循环节构成"],"tags":["字符串","匹配","KMP"],"id":561},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"线段树（Segment Tree）","content":"请解释线段树的数据结构、核心操作和典型应用。","answer":"答案：线段树是用于区间查询和区间更新的二叉树，每个节点代表一个区间\n\n解析：线段树将数组区间递归二分，叶子节点存单点值，父节点合并子节点信息（和、最大值、最小值的统计量）。核心操作：1）建树 — O(N)，递归建树，父节点 = merge(左子, 右子) 2）区间查询（query）— O(log N) 3）单点/区间更新（update）— O(log N)。懒标记（Lazy Propagation）使区间更新 O(log N)：更新时标记暂不推送到子节点，查询时才下推，避免每次都更新整个子树。空间通常开 4N。\n\n扩展延伸：线段树的泛化：维护的信息必须满足结合律（求和、最大值、gcd 等）。动态开点线段树 — 不预先开完整 4N 空间，适用于值域大的离散场景。线段树变种：1）权值线段树（维护值出现的频次，用于求第 K 大）2）可持久化线段树（主席树/函数式线段树，每个版本建 log N 个新节点）3）线段树套平衡树（二维区间查询）。线段树 vs 树状数组：线段树功能更强（区间更新 + 区间查询），树状数组代码更短、常数更小。","hints":["懒标记怎么保证区间更新的效率","线段树的 4N 空间是怎么推导的"],"tags":["线段树","区间查询","数据结构"],"id":562},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"树状数组（Fenwick Tree）","content":"请解释树状数组（Fenwick Tree/BIT）的原理及与线段树的对比。","answer":"答案：树状数组用巧妙的前缀和思想实现 O(log N) 单点更新和前缀查询\n\n解析：树状数组利用 lowbit(x) = x & -x 确定索引覆盖范围。数组 BIT[i] 维护 (i - lowbit(i), i] 区间的和。核心操作：1）add(i, val) — 从 i 开始不断 i += lowbit(i) 更新所有覆盖该位置的节点 O(log N) 2）sum(i) — 从 i 开始不断 i -= lowbit(i) 累加前缀和。区间查询 [l, r] = sum(r) - sum(l-1)。lowbit 的原理是利用补码表示，取最低位的 1 及其后的 0。\n\n扩展延伸：树状数组扩展应用：1）区间更新 + 单点查询（用差分数组构建 BIT，更新区间 [l,r] 时 add(l, val) 和 add(r+1, -val)，单点查询就是前缀和）2）区间更新 + 区间查询（需要两个 BIT 配合）3）求逆序对（离散化后按值插入 BIT，统计大于当前值的数量）4）二维 BIT（处理矩阵前缀和）。树状数组的局限：维护的信息必须可逆（求和、异或可用，最值不可用），不支持区间赋值操作（需用线段树）。常数和代码量均优于线段树。","hints":["lowbit(x) = x & -x 为什么能得到最低位的 1","树状数组能处理区间最值查询吗"],"tags":["树状数组","Fenwick","BIT"],"id":563},{"category":"algorithm_extras","difficulty":"medium","type":"short_answer","title":"布隆过滤器（Bloom Filter）","content":"请解释布隆过滤器的原理、优缺点和应用场景。","answer":"答案：布隆过滤器用位数组 + 多个哈希函数判断元素是否存在，允许一定误判\n\n解析：初始化长度为 m 的位数组（全 0），添加元素时用 k 个独立哈希函数将元素映射到 k 个位，置为 1。查询时同样计算 k 个哈希位，如果全部为 1 则认为元素存在（可能有误判），任一为 0 则确定不存在。误判率 p 与位数组长度 m、哈希函数数量 k、插入元素数量 n 相关：p ≈ (1 - e^(-kn/m))^k。最优哈希函数数 k = (m/n)*ln2。布隆过滤器不能删除元素（标准版），计数布隆过滤器（Counting Bloom Filter）用计数器数组替代位数组支持删除。\n\n扩展延伸：应用场景：1）缓存穿透保护 — 查不存在的 key 时先用布隆过滤器拦截 2）垃圾邮件过滤 / URL 去重 3）防止缓存穿透（Redis 布隆过滤器模块 4）Bigtable/Cassandra 用布隆过滤器减少磁盘 I/O。参数选择：m 越大误判率越低但也越占空间，通常 m/n = 10 时误判率约 1%。实际使用建议：使用前预估数据量 n，根据可接受的误判率 p 反推 m 和 k。Python 的 pybloom_live 和 RedisBloom 提供现成实现。","hints":["布隆过滤器能不能支持删除操作","误判率怎么在时间和空间之间权衡"],"tags":["布隆过滤器","概率","缓存"],"id":564},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"最小表示法","content":"请介绍字符串的最小表示法（循环同构的最小字典序表示）的原理和实现思路。","answer":"答案：最小表示法用于求一个字符串的循环同构串中的最小字典序表示。\n\n问题定义：\n- 给定字符串 S，其循环同构串为 S[i:] + S[:i]（所有旋转）\n- 求最小字典序的同构串的起始位置\n- 应用：判断两个字符串是否循环同构\n\n经典 O(n) 算法（双指针法）：\n\n1. 初始化：i = 0, j = 1, k = 0\n2. 比较 S[i+k] 和 S[j+k]：\n   - 如果相等：k++，继续比较\n   - 如果 S[i+k] < S[j+k]：说明以 j 开始的串不是最小表示\n     - j = j + k + 1（j 跳过这个不可能成为最小起始的位置）\n     - 如果 i == j，j++\n   - 如果 S[i+k] > S[j+k]：说明以 i 开始的串不是最小表示\n     - i = i + k + 1\n     - 如果 i == j，j++\n   - 重置 k = 0\n3. 当 i < n 且 j < n 时继续\n4. 最终 min(i, j) 就是最小表示法的起始位置\n\n算法正确性关键：\n- 当 S[i+k] != S[j+k] 时，[i, i+k] 和 [j, j+k] 范围内的位置都不可能是最小表示的起始位置\n- 每次迭代至少推进一个位置\n- 每个位置最多被跳过一次 → O(n)\n\n扩展延伸：\n- 最大表示法：只需将比较逻辑取反（大于改小于）\n- 应用场景：\n  1. 判断两字符串是否循环同构（最小表示相同）\n  2. 在循环字符串中匹配模式串\n  3. 一些环形 DP 问题中避免重复计算\n- 与 KMP 的关系：最小表示法解决的是循环同构问题，KMP 解决的是单模式匹配问题","hints":["双指针法 O(n)：i, j 两个候选起始位置，k 为比较偏移","S[i+k] < S[j+k] 时 j 跳过 [j, j+k]，反之 i 跳过 [i, i+k]","与 KMP 无关——最小表示法解决循环同构，KMP 解决模式匹配"],"tags":["算法","字符串","最小表示法","循环同构"],"id":586},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"匈牙利算法（KM 算法）","content":"请介绍匈牙利算法（Kuhn-Munkres，KM 算法）解决二分图最大权匹配的原理。","answer":"答案：KM 算法（Kuhn-Munkres，匈牙利算法）解决二分图的带权最大匹配问题（完美匹配假设）。\n\n问题定义：\n- 给定二分图 G=(U,V,E)，每条边有权重\n- 求权值最大的完美匹配（|U| = |V| 时）\n- 当 |U| ≠ |V| 时，补 0 权边转换为完美匹配问题\n\n核心概念：\n1. 顶标（Feasible Labeling）：每个顶点赋予一个值\n   - 对每条边 (u,v)：label[u] + label[v] ≥ weight(u,v)\n   - 称为\"可行顶标\"\n\n2. 相等子图（Equality Subgraph）：\n   - 只包含满足 label[u] + label[v] = weight(u,v) 的边\n   - 定理：如果相等子图有完美匹配，这就是原图的最大权匹配\n\n3. 交错树：从非匹配点开始的 DFS，未匹配边→匹配边→未匹配边...\n\n算法流程：\n1. 初始化顶标：\n   - label[u] = max(所有邻边权重)，label[v] = 0\n2. 对每个顶点尝试增广：\n   a) 在相等子图中 DFS 寻找增广路径（匈牙利算法）\n   b) 如果找到增广路径：匹配成功\n   c) 如果找不到：\n      - 计算松弛量 delta = min(label[u]+label[v]-weight(u,v))\n       其中 u 在交错树中，v 不在交错树中\n      - 更新顶标：树中 U 减少 delta，树中 V 增加 delta\n      - 这样至少有一条新边进入相等子图\n      - 继续尝试增广\n3. 重复直到所有顶点匹配\n\n时间复杂度：\n- 原始版本：O(n⁴)（每次更新顶标后重新 DFS）\n- 优化版（Slack 数组）：O(n³)（记录每个 V 的最小松弛量，增量更新）\n\n扩展延伸：\n- 最小权匹配：取反权重或用相反数\n- 当 |U| ≠ |V| 时，补顶点和 0 权边\n- 稠密图上 KM 比费用流快（O(n³) vs O(flow * E)）\n- 稀疏图：推荐用费用流（最小费用最大流），更容易处理非完美匹配","hints":["核心思想：可行顶标 + 相等子图，找到相等子图的完美匹配即原图最大权匹配","delta 计算：树中 U 减、树中 V 加，扩大相等子图","O(n³)：Slack 数组维护最小松弛量，避免每次 DFS 重新计算"],"tags":["算法","二分图","KM算法","最大权匹配"],"id":587},{"category":"algorithm_extras","difficulty":"medium","type":"short_answer","title":"区间信息维护：分块 vs 线段树 vs BIT","content":"请对比分块、线段树和树状数组（BIT）三种区间查询数据结构的适用场景和复杂度。","answer":"答案：分块、线段树和树状数组是三种最常见的一维区间信息维护结构。\n\n1. 树状数组（Binary Indexed Tree / BIT）\n- 原理：利用 lowbit 维护前缀和\n- 支持操作：单点更新、前缀和查询\n- 复杂度：O(log n) 每次操作\n- 优势：常数极小、实现短（5 行）、内存小\n- 限制：只能处理可差分信息（加法、异或、乘法等满足结合律和逆运算的操作）\n- 适用场景：单点更新 + 区间求和\n\n2. 线段树（Segment Tree）\n- 原理：递归分治，将区间划分为左右子区间\n- 支持操作：\n  - 单点/范围更新、区间查询（绝大多数结合律操作）\n  - 懒标记（Lazy Propagation）实现区间更新\n- 复杂度：O(log n) 每次操作\n- 优势：适用范围最广（max, min, gcd, 自定义运算）\n- 限制：实现较复杂（~50 行），常数较大，内存 O(4n)\n- 适用场景：广泛的区间操作\n\n3. 分块（Sqrt Decomposition）\n- 原理：将数组分成 √n 块，整块统计、零散暴力\n- 复杂度：O(√n) 每次操作\n- 优势：思路直观，实现简单；可以处理线段树不好处理的问题（如区间众数）\n- 限制：复杂度不如线段树\n- 适用场景：n ≤ 10^5，需要灵活维护复杂信息（每个块内维护哈希表等）\n\n选择建议：\n| 场景 | 推荐 | 原因 |\n|------|------|------|\n| 前缀和/差分 | BIT | 常数最小，代码最短 |\n| 区间更新+查询 | 线段树 | 懒标记原生支持 |\n| 复杂区间查询 | 分块 | 块内可维护任意结构 |\n| 值域线段树 | 线段树 | 动态开点/离散化 |\n\n扩展延伸：\n- BIT 可以扩展到二维（O(log²n)），适合平面上的矩形求和\n- 分块也可以带懒标记（整块范围的 O(1) 更新）\n- 线段树可持久化后支持历史版本查询（主席树）","hints":["BIT：常数最小，只能处理可差分信息（加法等），单点更新+前缀和","线段树：适用范围最广，O(log n) 支持各种区间运算，可实现懒标记","分块：O(√n) 但最灵活，块内可维护任意数据结构"],"tags":["算法","数据结构","区间查询","BIT","线段树","分块"],"id":588},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"高斯消元与线性基","content":"请介绍高斯消元求解线性方程组以及线性基（XOR Basis）处理异或问题的原理。","answer":"答案：高斯消元和线性基都是利用线性代数思想解决算法问题的方法。\n\n高斯消元：\n原理：将增广矩阵通过行变换化为行阶梯形（上三角矩阵），再回代求解。\n\n步骤：\n1. 前向消元：\n   - 选择主元（当前列绝对值最大的行，提高数值稳定性）\n   - 将主元行交换到当前位置\n   - 用主元行消去下方所有行的当前列\n2. 回代求解：\n   - 从最后一行开始，逐一求解每个变量\n   - 将已知解代入上方方程\n\n特殊情况：\n- 无解：消元后出现矛盾行（0 = 非零常数）\n- 无穷解：自由变量个数 = n - rank\n\n复杂度：O(n³)\n\n线性基（XOR Basis）：\n原理：\n- 一组数的线性基是原集合的一个最小子集，能 XOR 出原集合能 XOR 出的所有值\n- 本质上是 GF(2) 上的高斯消元\n\n构建方法：\n1. 对每个数 x，从高位到低位遍历\n2. 如果当前位没有基向量：设 base[i] = x\n3. 如果有：x = x XOR base[i]（消去当前位），继续\n4. 最终得到一个上三角矩阵\n\n应用：\n1. 最大异或和：从高位到低位贪心（选择异或后更大的）\n2. 第 k 大异或和：将线性基化为简化行阶梯形，按二进制分解 k\n3. 判断某个值能否被异或表示：类似插入的查询过程\n\n扩展延伸：\n- 线性基可以支持插入、删除（带时间戳的线性基）和合并\n- 线性基的大小不超过位数（如 60 位整数：最多 60 个基）\n- 线性空间的概念可以推广到一般域（不只是 GF(2)）","hints":["高斯消元：前向消元（选主元）→ 行阶梯形 → 回代求解，O(n³)","线性基：GF(2) 上的高斯消元，每位最多一个基向量，大小 ≤ 位数","线性基应用：最大/第k大异或和、判断可表示性"],"tags":["算法","线性代数","高斯消元","线性基"],"id":589},{"category":"algorithm_extras","difficulty":"easy","type":"short_answer","title":"快速幂与矩阵快速幂","content":"请介绍快速幂的原理以及如何推广到矩阵快速幂。","answer":"答案：快速幂（Binary Exponentiation）用 O(log n) 的时间计算 a^n。\n\n快速幂原理：\n- 将 n 写成二进制形式：n = b0*2^0 + b1*2^1 + ... + bk*2^k\n- a^n = a^(b0*2^0) × a^(b1*2^1) × ... × a^(bk*2^k)\n- 每次迭代：a = a * a（平方），如果当前位为 1 则乘到结果中\n\n实现要点：\n- 使用位运算：while n > 0: if n & 1: res *= a; a *= a; n >>= 1\n- 注意取模运算：(a * b) % mod\n- 处理 n = 0 的情况：结果为 1\n\n矩阵快速幂：\n- 同样原理，将 a 从标量换成矩阵\n- 矩阵乘法：C[i][j] = sum(A[i][k] * B[k][j])\n- 需要实现矩阵乘法函数（O(k³)，k 为矩阵边长）\n\n典型应用（用矩阵快速幂优化递推）：\n1. 斐波那契数列：\n   - 转移矩阵 [[1,1],[1,0]] 的 n 次方\n   - [F[n], F[n-1]] = [F[1], F[0]] × M^(n-1)\n2. 线性递推：任意 k 阶线性递推都可以用 k×k 转移矩阵 O(k³ log n) 求第 n 项\n3. 图论：邻接矩阵的 k 次方 = 长度为 k 的路径数\n\n复杂度：\n- 快速幂：O(log n)\n- 矩阵快速幂：O(k³ log n)，k 为矩阵边长\n\n扩展延伸：\n- 常系数齐次线性递推可以用 BM 算法（Berlekamp-Massey）求出递推式后再用矩阵快速幂\n- 对于大 k（>1000）的递推，线性递推（Linear Recurrence）用多项式技巧（多项式取模）优化到 O(k² log n) 或 O(k log k log n)","hints":["快速幂：二进制分解指数，迭代平方，O(log n)","矩阵快速幂：将标量乘法替换为矩阵乘法，用于线性递推 O(k³ log n)","经典应用：斐波那契数列、线性递推、图论路径计数"],"tags":["算法","快速幂","矩阵快速幂","递推"],"id":590},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"回文子串计数与 Manacher 算法","content":"请解释 Manacher 算法计算回文子串数量的原理及其时间复杂度分析。","answer":"答案：Manacher 算法利用回文的对称性在 O(N) 时间内找出所有回文子串\n\n解析：Manacher 的核心思想是维护一个当前最右回文边界（center, right），利用已计算的回文半径避免重复扩展。预处理：在字符间插入分隔符（如 #）使奇偶回文统一处理。定义 p[i] 为以 i 为中心的回文半径（包括 i 本身）。遍历字符串时，如果 i 在当前最右回文范围内（i < right），利用对称性得到 p[i] 的初始值 p[i] = min(right-i, p[2*center-i])，然后尝试向两边扩展。当扩展后的回文右边界超过当前 right 时更新 center 和 right。每个字符最多被扩展失败一次（当扩展到新的右边界时），所以总扩展次数 O(N)。总复杂度 O(N)，空间 O(N)。回文子串总数 = Σ(p[i] // 2)（经过分隔符处理后的真实回文数量）。\n\n扩展延伸：Manacher 与中心扩展法对比：中心扩展 O(N²)，Manacher O(N)。在实际面试中，如果 N ≤ 10^5 且只需要最长回文（而不是计数），中心扩展 + 优化也可用（P[i][j] DP O(N²) 不可行）。回文相关问题：1）最长回文子串（Manacher 跟踪最大 p[i]）2）回文子串数量（Σp[i]//2）3）分割回文串（回文 DP + 回溯或 Manacher 预处理）。本质上 Manacher 可以一步到位得到所有回文中心的最长回文半径。KMP、Z-algorithm、Manacher 三者本质上是同一类算法——利用前缀后缀的对称信息避免重复比较。面试中 Manacher 的写法复杂度高于中心扩展法，但理解原理后不难写。","hints":["Manacher 预处理插入分隔符的作用是什么","p[i] 的初始值为什么可以设置为 min(right-i, p[2*center-i])"],"tags":["字符串","回文","Manacher"],"id":591},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"数组中的逆序对计数","content":"请解释使用归并排序计算数组中逆序对数量的原理。","answer":"答案：在归并排序合并阶段统计逆序对——当右半边元素小于左半边时，左半边剩余元素都与该元素构成逆序\n\n解析：在归并排序的合并过程中，左右两个子数组已经分别有序（升序）。当从右半边取元素放入结果数组时，如果右半边的当前元素小于左边当前元素，则左半边从当前到末尾的所有元素都与这个右半边元素构成逆序对（因为左半边元素 > 右半边当前元素，且左半边所有元素的原始索引都在右半边当前元素之前）。在 merge 步骤中统计 count += mid - i + 1 即可累加。算法总复杂度 O(N log N)（归并排序本身），额外空间 O(N)（归并用的临时数组）。\n\n扩展延伸：逆序对数等于冒泡排序的交换次数（每次相邻交换恰好消除一个逆序对），也反映数组有序程度。Predecessor 问题：求每个元素在前面有多少个比它大的元素（前缀逆序），可以用 Fenwick Tree（树状数组）或 Segment Tree 在线查询（离散化 + BIT，从右向左遍历，对每个元素查询 BIT 中比它小的数量）。更一般的扩展：求数组中满足 a[i] > a[j] * k 的逆序对（带权重），BIT 同样适用。LeetCode 315（Count of Smaller Numbers After Self）即求每个元素右边的逆序数量——从右向左遍历，离散化后用 BIT 维护频次即可 O(N log N)。三元素逆序对（a[i] > a[j] > a[k], i<j<k）可以用两次 BIT——分别维护「以 j 结尾的逆序对数量」和「以 k 结尾的三元组数量」。","hints":["归并排序合并时统计逆序对的时机和累加公式","树状数组怎么在线计算每个元素右边的比它小的元素个数"],"tags":["数组","逆序对","归并排序"],"id":592},{"category":"algorithm_extras","difficulty":"medium","type":"choice","title":"二维矩阵搜索策略","content":"在一个每行从左到右递增且每列从上到下递增的矩阵中搜索目标值，最优算法的复杂度是？","options":["A) O(M*N) 暴力搜索","B) O(M*logN) 每行二分","C) O(M+N) 从右上角开始搜索","D) O(log(M*N)) 全局二分"],"answer":"C","hints":["从右上角开始，比目标大向左移，比目标小向下移，每次排除一行或一列"],"tags":["矩阵","搜索","算法"],"id":593},{"category":"algorithm_extras","difficulty":"medium","type":"short_answer","title":"字符串循环移位","content":"请解释字符串循环移位（Rotate String）的高效实现方法。","answer":"答案：字符串循环移位可以通过三次反转（Three-Reverse）在 O(N) 时间和 O(1) 额外空间实现\n\n解析：将字符串 s 循环左移 k 位的方法：1）反转前 k 个字符 2）反转剩余的 N-k 个字符 3）反转整个字符串。例如 s = \"abcdef\", k=2：反转前 2 个 \"ab\"→\"ba\"，反转后 4 个 \"cdef\"→\"fedc\"，得到 \"bafedc\"，再全部反转得到 \"cdefab\"。原理：XY -> (X^T Y^T)^T = YX。另一种方法：使用 s[(i+k)%N] = s[i] 的原地置换（类似循环队列），但需要 gcd 知识——如果 N 和 k 的最大公约数为 g，则需进行 g 轮循环（每轮从 i 开始跳 k 步直到回到 i）。Python 和 Java 中也可以 s = s[k:] + s[:k] 一行实现但 O(N) 空间。\n\n扩展延伸：循环移位相关的问题：1）字符串旋转匹配（s1 旋转后能否等于 s2）——判断 s2 in s1+s1 即可 2）字符串环的最小表示法（求循环同构串中字典序最小的表示）——用双指针 O(N) 算法（最小表示法）3）旋转后数组的二分搜索——旋转数组搜索 target（LeetCode 33），先二分找旋转点再二分搜索。KMP 算法也可以用于旋转匹配（判断 s1+s1 不拼接而用模式匹配）。手写原地循环移位的 gcd 版本：内层 for j in range(g), i=j, tmp=s[i], do{next=(i+k)%N, swap(s[next], tmp), i=next} while i!=j。工程中优先使用三步反转法（简洁且 cache-friendly）。","hints":["三步反转法为什么能实现循环移位","判断 s1 旋转后能否等于 s2 为什么可以用 s1+s1 包含 s2 来判断"],"tags":["字符串","旋转","算法"],"id":594},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"最近公共祖先 LCA 算法","content":"请解释二叉树中最近公共祖先（LCA）的递归算法和离线 Tarjan 算法。","answer":"答案：LCA 递归算法在 DFS 过程中判断目标节点是否在左右子树中，Tarjan 算法使用并查集离线处理所有查询\n\n解析：递归法：从根节点 DFS，如果当前节点等于 p 或 q 则返回当前节点。递归查询左右子树，如果左子树返回非空且右子树也返回非空，说明 p 和 q 分别在左右子树中，当前节点就是 LCA。如果只有一侧返回非空，则 LCA 在那侧。复杂度 O(N) 每次查询，适合少量查询。空间取决于树高（递归栈深度）。Tarjan 离线 LCA：一次 DFS 处理所有查询。DFS 遍历树时，用并查集将已回溯的节点与父节点合并。当访问到一个节点时，检查所有与该节点相关的查询——如果另一个节点已被访问过，则 find(另一个节点) 就是它们的 LCA。复杂度 O(N + Q*α(N))，其中 Q 是查询数。\n\n扩展延伸：LCA 的其他方法：1）Binary Lifting（倍增法）— 预处理每个节点往上跳 2^k 步的祖先，每次查询 O(log N)。预处理 O(N log N)。2）RMQ + Euler Tour — 树的欧拉序转化为区间最小值查询（Sparse Table 或 Segment Tree），查询 O(1)（RMQ 预处理 O(N log N)）。在算法竞赛中，Binary Lifting 是最常用的——实现简单且支持在线的 O(log N) 查询。Tarjan 适合离线处理大量查询的场景。树上差分（Differential on Tree）用到 LCA——给路径上的所有节点加权的经典方法：diff[u]++, diff[v]++, diff[lca]-=2。注意 diff[lca] 的减 2 是回退 lca 本身被重复计算的路径。实际工程中 LCA 还可用于求树上两点间距离（dist = depth[u] + depth[v] - 2*depth[lca]）。","hints":["递归找 LCA 为什么 p 和 q 分别在左右子树时当前节点就是 LCA","Binary Lifting 的 up[v][k] 数组怎么递推"],"tags":["树","LCA","算法"],"id":595},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"吉司机线段树","content":"请说明吉司机线段树（Segment Tree Beats）的核心概念和设计动机。它解决了哪些传统线段树无法高效处理的问题？以「区间取 min」操作为例，说明如何维护区间最大值和次大值来实现均摊 O(n log n) 的复杂度。重点解释势能分析如何保证复杂度上界。","answer":"吉司机线段树（Segment Tree Beats，由吉如一提出）是一种扩展线段树，用于高效处理区间取最值（区间 chmin/chmax）操作，同时支持区间求和。传统线段树无法高效处理区间取 min 是因为无法快速更新区间内所有大于 x 的值。核心思路：对每个节点维护区间最大值 mx、次大值 smx、最大值出现次数 cnt。区间取 min 操作（将区间内所有 > x 的值设为 x）：1) 若当前区间最大值 mx <= x：无需操作。2) 若 smx < x < mx：更新 mx = x，并根据 cnt 更新区间和（sum -= cnt * (old_mx - x)），打 lazy tag。3) 若 x <= smx：递归向下处理子树。势能分析：每个节点被递归到「第三层」时，其最大值会被减小到次大值以下，即该区间的「不同值的数量」严格减少。整个线段树的不同值总数是 O(n log n) 的势能上界，每次操作消耗势能，总复杂度 O((n + q) log n)。但要注意当同时需要区间加操作时，势能分析不再适用，复杂度退化为 O(q log^2 n)。类似地，区间 chmax（取最大）、区间加、区间求和的组合操作也可以在 STB 框架下实现，但实现复杂度显著增加。","hints":["为什么 mx、smx、cnt 三个信息足以判断是否需要递归？","势能分析中「不同值的数量」为什么是 O(n log n)？"],"tags":["线段树","Segment Tree Beats","势能分析","区间操作","吉司机"],"id":601},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"回文自动机","content":"请说明回文自动机（Palindrome Automaton / Palindromic Tree / Eertree）的数据结构原理和构建过程。包括：如何定义回文自动机的状态和转移？如何在线性时间内构建？它支持哪些高效的回文相关查询？","answer":"回文自动机（又叫 Palindromic Tree / Eertree，由 Mikhail Rubinchik 和 Arseny M. Shur 提出）是一种处理回文串问题的自动机结构。其原理是用两个根节点（偶根 -1 和奇根 0）代表所有回文串，每个状态代表一个不同的回文子串（本质不同的回文子串）。核心定义：1) 每个节点（状态）代表一个回文子串：存储长度 len、后缀链接 fail（指向最长的不同回文后缀）、转移边 next（按字符转移）。2) 两个根节点：偶根 len=0（空串），奇根 len=-1。奇根的 fail 指向偶根，偶根的 fail 指向奇根。3) fail 链接：从一个回文串的末尾添加字符后得到的新回文串的最长回文后缀。构建过程（在线，O(n)）：逐字符插入。设当前字符 s[i]，维护上一次插入后最长回文后缀的节点 last。1) 从 last 开始沿 fail 链找到第一个回文串其前一个字符等于 s[i]：while s[i-len[cur]-1] != s[i]: cur = fail[cur]。2) 如果当前节点 cur 没有转移边 s[i]，创建新节点：next[cur][s[i]] = new_node，len[new] = len[cur] + 2。3) 设置新节点的 fail：如果 len[new] == 1，fail[new] = 偶根（0）。否则沿 cur 的 fail 链找第一个满足条件的节点。4) last = next[cur][s[i]]。高效查询：1) 每个回文子串出现次数（沿 fail 链拓扑 DP）。2) 以 i 结尾的回文子串数量（通过 last 节点 len 信息可递推）。3) 最小回文划分（通过 fail 链的等差数列优化）。4) 双端添加字符。时间复杂度 O(n)，空间 O(n * |alphabet|) 或 O(n)。","hints":["回文自动机的两个根节点分别代表什么，为什么需要两个？","fail 链在构建过程中起到什么作用？"],"tags":["回文自动机","Palindromic Tree","字符串","Eertree","数据结构"],"id":602},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"最小割 Stoer-Wagner 算法","content":"请说明 Stoer-Wagner 算法求无向图全局最小割（Global Minimum Cut）的核心思想和算法步骤。它与基于最大流的最小割算法（如求 s-t 最小割）相比有什么优势？算法的时间复杂度和正确性直观理解是什么？","answer":"Stoer-Wagner 算法是一种用于求无向带权图全局最小割的算法，不需要指定源点和汇点。其核心思想是迭代执行「最大相邻序列」（Maximum Adjacency Ordering / MA 序）的切分过程。算法步骤：1) 初始图 G，累积权重数组 w[v]（初始 0），合并后的顶点集。2) 重复以下步骤直到图中只剩一个顶点：a) 从任意顶点开始，每次选择与当前已选集合外部连接权重最大的顶点加入集合，并用 w[v] 记录。此过程称为 MA 序。b) 设 MA 序最后两个加入的顶点为 s 和 t，则 w[t] 就是「s-t 相切的最小割容量」，记录此割容量。c) 合并 s 和 t（将 t 合并到 s），更新所有与 t 相连的边的权重到 s。3) 所有记录的割容量中的最小值即为全局最小割。正确性直观理解：按照 MA 序构造过程中，最后加入的顶点 t 与之前集合的割是某个 s-t 割的容量，且该割是当前图的最小 s-t 割。合并后不影响全局最小割的容量（因为全局最小割或者在此轮被切掉，或者完全包含在合并后的结果中）。优势：1) 不需要指定源汇点。2) 不需要运行多次最大流（全局最小割需要 O(n) 次最大流或 Gomory-Hu 树），实现更简洁。3) 对于稠密图，Stoer-Wagner 的 O(nm + n^2 log n)（使用优先队列）比 Dinic 求 n 次最大流的 O(n^2 * m) 更快。4) 但 Stoer-Wagner 只能用于无向图。简单实现使用邻接矩阵 O(n^3)，用二叉堆 O(nm log n)，用 Fibonacci 堆 O(nm + n^2 log n)。","hints":["MA 序的选取规则是什么，s 和 t 是怎么确定的？","为什么合并 s-t 后不会丢失全局最小割？"],"tags":["最小割","Stoer-Wagner","图论","全局最小割","无向图"],"id":603},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"树链剖分","content":"请说明树链剖分（Heavy-Light Decomposition, HLD）的核心原理和典型应用场景。包括重儿子/轻儿子的定义、剖分过程、如何将树上路径查询转化为 O(log^2 n) 的区间查询。以「树上路径求和」和「LCA 查询」为例说明实现细节。","answer":"树链剖分将树剖分为若干条链（重链），使得任意节点到根的路径上经过 O(log n) 条链，从而将树上路径操作转化为多段线段树区间操作。核心定义：1) 重儿子：子树大小最大的子节点。2) 轻儿子：其他子节点。3) 重边：父节点与重儿子的边。4) 重链：由重边连接而成的链（从某个节点开始不断走重儿子直到叶子）。5) 链头（top）：重链中最浅的节点。剖分过程（两遍 DFS）：DFS1：计算每个节点的子树大小 size、父节点 parent、深度 dep、重儿子 heavy_child。DFS2：按重儿子优先顺序分配 dfs 序（dfn），使每条重链上的节点在 dfn 数组中连续。维护链头 top：从根开始，沿重边走时共享链头，转到轻边时自身为新链头。应用一：树上路径求和（u 到 v）。while top[u] != top[v]：if dep[top[u]] < dep[top[v]]: swap(u, v)。在 dfn[top[u]] 到 dfn[u] 区间上查询线段树区间和。u = parent[top[u]]。最后 u 和 v 在同一条链上，查询 dfn[u] 到 dfn[v]（假设 dep[u] < dep[v]）。应用二：LCA 查询。while top[u] != top[v]：if dep[top[u]] > dep[top[v]]: u = parent[top[u]] else: v = parent[top[v]]。返回 dep[u] < dep[v] ? u : v。复杂度：单条路径操作 O(log^2 n)（O(log n) 条链 * O(log n) 线段树查询）。子树操作更简单（dfn[u] 到 dfn[u] + size[u] - 1 的连续区间）。HLD 不仅支持求和，还可支持 max/min、赋值、区间加等线段树操作。常见题型：路径权值更新/查询、子树更新/查询、结合树上差分处理路径覆盖问题。","hints":["为什么 dfn 序中每条重链上的节点是连续的？","路径向上跳转时，每次跳到链头的父节点起到什么作用？"],"tags":["树链剖分","HLD","LCA","线段树","树"],"id":604},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"动态图连通性","content":"请说明动态图连通性（Dynamic Connectivity）问题的定义和主要算法。针对完全动态（边插入和删除）场景，解释 Holm-de Lichtenberg-Thorup（HDLT）算法的核心思想和复杂度。对于仅删除（decremental）或仅插入（incremental）这两个特例，分别用什么更简单的数据结构？","answer":"动态图连通性指在支持边插入和删除的动态图中，实时回答两个顶点是否连通的问题。主要难度在于删除操作可能破坏连通性。完全动态（边增删皆可）：Holm-de Lichtenberg-Thorup（HDLT）算法是第一个达到近最优 O(log^2 n) 均摊时间/操作的确定性算法。核心思想：维护多级生成森林（Euler Tour Tree 管理各级森林），利用边的「层次」概念。1) 每条边赋予一个层次 level（0 到 L = log n），新插入边在 level 0。2) 维护 L+1 层生成森林 F_0 ... F_L，每个 F_l 是 level >= l 的边构成的生成森林。F_0 包含所有边，F_L 是空集。3) 删除边 (u,v) 时：找到该边所在最高层级 l。将该边从 F_l 及以下所有层中移除。如果 F_l 中 u-v 被断开，则需要在 level l 找一条替代边（在 F_{l-1} 中找与 u 连通且通过 level >= l 的边连接到 v 的边）。4) 用 Euler Tour Tree（ETT）维护各层生成森林，支持在 O(log n) 内检查连通性和替换边。5) 每条边最多被 promotion 到更高层一次（层数上限 log n），每次 promotion 涉及图中 O(log n) 次操作，均摊 O(log^2 n)。对于特例：1) 仅插入（Incremental）：用并查集（Union-Find）即可，每次插入用 union 操作合并边，查询 O(alpha(n))。2) 仅删除（Decremental）：更复杂但仍有高效方法。a) 对于连通性仅删除（如删边数上限 K）：可用 DFS 树 + 并查集，或使用 Holm 等提出的随机数据结构。b) 一般 decremental：可用 HD 算法（Holm 和 de Lichtenberg 的早期工作），O(m log n) 总时间。c) 确定性算法：可用「连通性认证子」（connectivity certificate）技术构造稀疏化子图降低复杂度。","hints":["HDLT 算法的「层级提升」机制如何保证均摊复杂度？","为什么并查集不能处理边删除操作？"],"tags":["动态图","连通性","HDLT","Euler Tour Tree","并查集"],"id":605},{"category":"algorithm_extras","difficulty":"medium","type":"coding","title":"火星词典（Alien Dictionary）","content":"现有一种使用英语字母的外星语言，这门语言的字母顺序与英语顺序不同。给定一个按「新字母顺序」排序的该语言单词列表 words，请返回这门语言的排序后的字母顺序。如果排序不合法（存在循环依赖），返回空字符串。","answer":"```java\npublic String alienOrder(String[] words) {\n    Map<Character, Set<Character>> graph = new HashMap<>();\n    Map<Character, Integer> inDegree = new HashMap<>();\n    \n    // 初始化所有出现的字母\n    for (String word : words) {\n        for (char c : word.toCharArray()) {\n            graph.putIfAbsent(c, new HashSet<>());\n            inDegree.putIfAbsent(c, 0);\n        }\n    }\n    \n    // 比较相邻单词，提取边\n    for (int i = 0; i < words.length - 1; i++) {\n        String w1 = words[i], w2 = words[i + 1];\n        int minLen = Math.min(w1.length(), w2.length());\n        boolean found = false;\n        for (int j = 0; j < minLen; j++) {\n            char c1 = w1.charAt(j), c2 = w2.charAt(j);\n            if (c1 != c2) {\n                if (!graph.get(c1).contains(c2)) {\n                    graph.get(c1).add(c2);\n                    inDegree.put(c2, inDegree.get(c2) + 1);\n                }\n                found = true;\n                break;\n            }\n        }\n        // 处理前缀相同但 w1 比 w2 长的情况（如 w1=\"abc\", w2=\"ab\" -> 非法）\n        if (!found && w1.length() > w2.length()) return \"\";\n    }\n    \n    // Kahn 算法拓扑排序\n    Queue<Character> queue = new LinkedList<>();\n    for (char c : inDegree.keySet()) {\n        if (inDegree.get(c) == 0) queue.offer(c);\n    }\n    \n    StringBuilder sb = new StringBuilder();\n    while (!queue.isEmpty()) {\n        char c = queue.poll();\n        sb.append(c);\n        for (char next : graph.get(c)) {\n            inDegree.put(next, inDegree.get(next) - 1);\n            if (inDegree.get(next) == 0) queue.offer(next);\n        }\n    }\n    \n    return sb.length() == inDegree.size() ? sb.toString() : \"\";\n}\n```\n时间复杂度 O(C)（C 是所有单词的总字符数），空间复杂度 O(1)（最多 26 个字母）。\n\n核心思路：比较相邻单词找到第一个不同的字母，构建有向边（c1 -> c2 表示 c1 在 c2 之前）。用拓扑排序（Kahn 算法）确定字母顺序。如果图中存在环（排序结果字符数少于总字符数），说明排序非法。\n\n边界情况：如果 w1 是 w2 的前缀且 w1 比 w2 长（如 w1=\"abcd\", w2=\"abc\"），排序非法——因为按排序规则短单词应该在更前面。","hints":["如何从有序单词列表推断字母顺序——比较相邻单词的第一个不同字母","拓扑排序检测循环依赖——Kahn 算法中如果排序结果不全说明有环"],"tags":["拓扑排序","图","Kahn","字符串"],"options":[],"id":606},{"category":"algorithm_extras","difficulty":"medium","type":"coding","title":"设计搜索自动补全系统","content":"设计一个搜索自动补全系统（Search Autocomplete System）。用户输入句子时，系统返回排名前 3 的热门补全建议。\n\n实现 AutocompleteSystem 类：\n- AutocompleteSystem(String[] sentences, int[] times)：系统初始化时给定历史句子及其出现次数。\n- List<String> input(char c)：用户输入一个字符。如果 c == '#'，表示输入结束，将当前输入的句子记录到系统（次数 +1），返回空列表。否则返回前缀匹配且排名前 3 的补全建议。\n\n排名规则：先按热度（出现次数）降序，次数相同按句子的字典序升序。","answer":"```java\nclass AutocompleteSystem {\n    class TrieNode {\n        Map<Character, TrieNode> children = new HashMap<>();\n        Map<String, Integer> freqMap = new HashMap<>(); // 以该前缀开头的所有句子及其热度\n    }\n    \n    private TrieNode root;\n    private StringBuilder currentInput;\n    private TrieNode currentNode;\n    \n    public AutocompleteSystem(String[] sentences, int[] times) {\n        root = new TrieNode();\n        currentInput = new StringBuilder();\n        currentNode = root;\n        for (int i = 0; i < sentences.length; i++) {\n            addSentence(sentences[i], times[i]);\n        }\n    }\n    \n    private void addSentence(String sentence, int freq) {\n        TrieNode node = root;\n        for (char c : sentence.toCharArray()) {\n            node.children.putIfAbsent(c, new TrieNode());\n            node = node.children.get(c);\n            node.freqMap.put(sentence, node.freqMap.getOrDefault(sentence, 0) + freq);\n        }\n    }\n    \n    public List<String> input(char c) {\n        if (c == '#') {\n            String sentence = currentInput.toString();\n            addSentence(sentence, 1);\n            currentInput = new StringBuilder();\n            currentNode = root;\n            return new ArrayList<>();\n        }\n        currentInput.append(c);\n        if (!currentNode.children.containsKey(c)) {\n            currentNode.children.put(c, new TrieNode());\n        }\n        currentNode = currentNode.children.get(c);\n        \n        // 取热度前 3\n        PriorityQueue<Map.Entry<String, Integer>> pq = new PriorityQueue<>(\n            (a, b) -> a.getValue() == b.getValue() \n                ? b.getKey().compareTo(a.getKey())  // 字典序降序（为了让优先队列弹出较大的）\n                : a.getValue() - b.getValue()        // 热度升序\n        );\n        for (Map.Entry<String, Integer> entry : currentNode.freqMap.entrySet()) {\n            pq.offer(entry);\n            if (pq.size() > 3) pq.poll();\n        }\n        \n        List<String> result = new ArrayList<>();\n        while (!pq.isEmpty()) {\n            result.add(0, pq.poll().getKey()); // 逆序加入使结果按热度降序\n        }\n        return result;\n    }\n}\n```\n\n时间复杂度：input 操作 O(N * log 3) = O(N)（N 是以当前前缀开头的句子数），addSentence 操作 O(L)（L 是句子长度）。\n\n核心设计：每个 Trie 节点存储以该前缀开头的所有句子及其频率（freqMap）。用户在输入过程中逐字符走 Trie，在 end 节点收集所有候选并按规则排序取前 3。\n\n优化思路：\n- 如果热门句子集合是静态的，可以在每个节点预计算 Top 3（离线计算，查询 O(1)）。\n- 如果句子数量极大（百万级），freqMap 的内存占用会很大——可以将匹配限制在 Top K 范围内，用堆或布隆过滤器剪枝。\n- 实际搜索引擎的自动补全使用「Trie + 预计算 Top N + LRU 缓存」的组合方案。","hints":["Trie 的每个节点存储以该前缀开头的所有句子的频率——空间换时间","Top 3 可以用最小堆维护——堆大小固定为 3，每次弹出最小的"],"tags":["Trie","设计","堆","字符串"],"options":[],"id":607},{"category":"algorithm_extras","difficulty":"hard","type":"coding","title":"最小窗口子序列","content":"给定字符串 S 和 T，请在 S 中找到最短的连续子串，使得 T 是该子串的「子序列」（Subsequence，即 T 中字符按顺序出现在子串中，不一定连续）。如果不存在返回空字符串。\n\n注意：本题与「最小覆盖子串（Minimum Window Substring）」不同——后者要求子串包含 T 的所有字符（可乱序），本题要求子串包含 T 作为子序列（必须按顺序）。","answer":"```java\npublic String minWindow(String S, String T) {\n    int m = S.length(), n = T.length();\n    // dp[i][j]：S 的前 i 个字符中，匹配 T 的前 j 个字符作为子序列时，\n    //          子序列在 S 中的「起始位置的最大值」（最靠右的起始位置）\n    // 初始化为 -1 表示不可达\n    int[][] dp = new int[m + 1][n + 1];\n    for (int[] row : dp) Arrays.fill(row, -1);\n    for (int i = 0; i <= m; i++) dp[i][0] = i; // T 为空串时，起始位置就是 i\n    \n    int minLen = Integer.MAX_VALUE;\n    int start = -1;\n    \n    for (int i = 1; i <= m; i++) {\n        for (int j = 1; j <= Math.min(i, n); j++) {\n            if (S.charAt(i - 1) == T.charAt(j - 1)) {\n                dp[i][j] = dp[i - 1][j - 1]; // 字符匹配，继承 T[0..j-2] 的起始位置\n            } else {\n                dp[i][j] = dp[i - 1][j];     // 不匹配，跳过 S[i-1]\n            }\n        }\n        // 检查是否完整匹配了 T\n        if (dp[i][n] != -1) {\n            int len = i - dp[i][n];\n            if (len < minLen) {\n                minLen = len;\n                start = dp[i][n];\n            }\n        }\n    }\n    \n    return start == -1 ? \"\" : S.substring(start, start + minLen);\n}\n```\n\n时间复杂度 O(m * n)，空间复杂度 O(m * n)（可优化为 O(n)）。\n\n核心思想：dp[i][j] 表示对于 T[0..j-1] 作为子序列，在 S[0..i-1] 中的「起始位置」——注意是起始位置，不是是否存在。值越大表示起始位置越靠右（即子串越短），因为 dp[i][n] 记录的是匹配完整 T 时 S 中子序列的最靠右的起始位置。\n\n优化的子序列匹配方式：如果字符匹配（S[i-1] == T[j-1]），则匹配位置回溯到 dp[i-1][j-1]（T 的前 j-1 个字符匹配时的起始位置）；如果不匹配，沿用 dp[i-1][j]（跳过 S 的当前字符）。\n\n另一种解法：对 T 中每个字符在 S 中的出现位置建立索引，再用「跳表」思路（next 数组）逐字符跳跃查找——适用于 S 很长但 T 很短的情况。","hints":["本题和「最小覆盖子串」的关键区别——子序列要求按顺序，子串要求包含所有字符（可乱序）","DP 中 dp[i][j] 定义为起始位置而非长度——这是为了支持「最短」的约束"],"tags":["动态规划","字符串","子序列","双指针"],"options":[],"id":608},{"category":"algorithm_extras","difficulty":"hard","type":"coding","title":"戳气球","content":"有 n 个气球，编号为 0 到 n - 1，每个气球上都标有一个数字，这些数字存在数组 nums 中。\n\n现在要求你戳破所有的气球。戳破第 i 个气球，你可以获得 nums[i - 1] * nums[i] * nums[i + 1] 枚硬币。这里的 i - 1 和 i + 1 代表和 i 相邻的两个气球的序号。如果 i - 1 或 i + 1 超出了数组的边界，那么就当它是一个数字为 1 的气球。\n\n求所能获得硬币的最大数量。","answer":"```java\npublic int maxCoins(int[] nums) {\n    int n = nums.length;\n    // 数组边界扩展为 1\n    int[] arr = new int[n + 2];\n    arr[0] = 1;\n    arr[n + 1] = 1;\n    for (int i = 0; i < n; i++) arr[i + 1] = nums[i];\n    \n    // dp[i][j]：戳破 (i, j) 范围（不包含 i 和 j）内所有气球的最大硬币数\n    int[][] dp = new int[n + 2][n + 2];\n    \n    // len 是区间长度，从 1 到 n\n    for (int len = 1; len <= n; len++) {\n        for (int i = 1; i <= n - len + 1; i++) {\n            int j = i + len - 1;\n            // 枚举区间中最后一个被戳破的气球 k\n            for (int k = i; k <= j; k++) {\n                // arr[i-1] * arr[k] * arr[j+1]：戳破 k 时，它的左右邻居是 i-1 和 j+1\n                // （因为 i..j 中其他气球已经被戳破了，k 是最后一个）\n                int coins = dp[i][k - 1] + arr[i - 1] * arr[k] * arr[j + 1] + dp[k + 1][j];\n                dp[i][j] = Math.max(dp[i][j], coins);\n            }\n        }\n    }\n    return dp[1][n];\n}\n```\n时间复杂度 O(n^3)，空间复杂度 O(n^2)。\n\n核心思路：经典区间 DP。关键转换——不决定「第一个戳破谁」，而是决定「最后一个戳破谁」。如果 k 是区间 [i, j] 中最后一个被戳破的气球，那么戳破它时，它的左右邻居是 i-1 和 j+1（因为区间内的其他气球都已经被戳破了）。所以分数贡献 = arr[i-1] * arr[k] * arr[j+1]。加上左右子区间的最大分数 dp[i][k-1] + dp[k+1][j]。\n\n这个「反向思考」是本题的精华——传统的「先戳破谁」思路会导致子问题互相依赖（因为戳破一个后左右邻居变化了），而「最后戳破谁」的思路让左右子区间完全独立。","hints":["区间 DP 的关键是「最后一个被戳破的气球是谁」而不是「第一个被戳破的是谁」","为什么「最后一个」比「第一个」好——最后一个被戳破时，其左右邻居是固定的（区间的左右边界）"],"tags":["动态规划","区间DP","分治"],"options":[],"id":609},{"category":"algorithm_extras","difficulty":"hard","type":"coding","title":"计算右侧小于当前元素的个数","content":"给你一个整数数组 nums，按要求返回一个新数组 counts。其中 counts[i] 的值是 nums[i] 右侧小于 nums[i] 的元素的数量。\n\n要求时间复杂度 O(n log n)。","answer":"```java\npublic List<Integer> countSmaller(int[] nums) {\n    int n = nums.length;\n    Integer[] result = new Integer[n];\n    Arrays.fill(result, 0);\n    \n    // 归并排序的同时计算右侧小于当前元素的个数\n    int[] indices = new int[n];\n    for (int i = 0; i < n; i++) indices[i] = i;\n    \n    mergeSort(nums, indices, 0, n - 1, result);\n    return Arrays.asList(result);\n}\n\nprivate void mergeSort(int[] nums, int[] indices, int left, int right, Integer[] result) {\n    if (left >= right) return;\n    int mid = left + (right - left) / 2;\n    mergeSort(nums, indices, left, mid, result);\n    mergeSort(nums, indices, mid + 1, right, result);\n    merge(nums, indices, left, mid, right, result);\n}\n\nprivate void merge(int[] nums, int[] indices, int left, int mid, int right, Integer[] result) {\n    int[] temp = new int[right - left + 1];\n    int i = left, j = mid + 1, k = 0;\n    int rightCount = 0; // 右侧已归并的元素中，小于当前左侧元素的个数\n    \n    while (i <= mid && j <= right) {\n        if (nums[indices[j]] < nums[indices[i]]) {\n            // 右侧元素小于左侧元素，归并右侧，增加 rightCount\n            temp[k++] = indices[j++];\n            rightCount++;\n        } else {\n            // 左侧元素 <= 右侧元素，归并左侧\n            result[indices[i]] += rightCount;\n            temp[k++] = indices[i++];\n        }\n    }\n    while (i <= mid) {\n        result[indices[i]] += rightCount;\n        temp[k++] = indices[i++];\n    }\n    while (j <= right) {\n        temp[k++] = indices[j++];\n    }\n    \n    System.arraycopy(temp, 0, indices, left, temp.length);\n}\n```\n时间复杂度 O(n log n)，空间复杂度 O(n)。\n\n核心思路：在归并排序的过程中，当左侧元素被放入辅助数组时，右侧已经放入了多少个比它小的元素？答案就是在那个时刻已经归并进去的右侧元素个数（rightCount）。\n\n算法框架：\n1. 不直接对 nums 排序，而是对索引数组 indices 排序。\n2. 在合并过程中，如果右侧当前元素小于左侧当前元素，则 rightCount++（表示「还有一个更小的右侧元素」）。\n3. 当左侧元素被归并时，result[原索引] += rightCount。\n\n另一种解法：Fenwick 树（树状数组）/ 线段树。从右向左遍历 nums，离散化后维护已遍历元素的频次 BIT，查询比当前元素小的个数 = query(currentValue - 1)。复杂度同为 O(n log n)。两种解法都需要掌握：归并排序解法考察「分治思维」，BIT 解法考察「数据结构应用」。","hints":["归并排序解法为什么能计数？——合并时，左侧元素归并时右侧已归并的元素数就是比它小的右侧元素数","Fenwick 树解法怎么做？——从右向左遍历，已遍历的元素插入 BIT，查询比当前小的数量"],"tags":["归并排序","分治","树状数组","数组"],"options":[],"id":610},{"category":"algorithm_extras","difficulty":"medium","type":"coding","title":"实现一个布隆过滤器","content":"实现一个简单的布隆过滤器（Bloom Filter），支持 add() 和 mightContain() 操作。布隆过滤器使用 k 个哈希函数和 m 位的位数组。请实现一个基于 Java BitSet 的版本，并讲解误判率（False Positive）与 k、m、n（插入元素数）的关系。","answer":"```java\nimport java.util.BitSet;\nimport java.util.function.Function;\n\nclass BloomFilter<T> {\n    private final BitSet bits;\n    private final int bitSize;         // m\n    private final int numHashFunctions; // k\n    private final Function<T, Integer>[] hashFunctions;\n\n    @SuppressWarnings(\"unchecked\")\n    public BloomFilter(int expectedInsertions, double falsePositiveRate) {\n        // 最优 bit 数 m = -n * ln(p) / (ln(2)^2)\n        this.bitSize = (int) Math.ceil(-expectedInsertions * Math.log(falsePositiveRate) / (Math.log(2) * Math.log(2)));\n        // 最优哈希函数数 k = (m/n) * ln(2)\n        this.numHashFunctions = (int) Math.ceil((double) bitSize / expectedInsertions * Math.log(2));\n        this.bits = new BitSet(bitSize);\n\n        // 使用不同的种子模拟 k 个独立的哈希函数\n        this.hashFunctions = new Function[numHashFunctions];\n        for (int i = 0; i < numHashFunctions; i++) {\n            final int seed = i;\n            hashFunctions[i] = (element) -> (element.hashCode() ^ seed * 0x9e3779b9) & Integer.MAX_VALUE;\n        }\n    }\n\n    // 使用 String 版本的构造更方便\n    public BloomFilter(int m, int k) {\n        this.bitSize = m;\n        this.numHashFunctions = k;\n        this.bits = new BitSet(bitSize);\n        this.hashFunctions = new Function[numHashFunctions];\n        for (int i = 0; i < numHashFunctions; i++) {\n            final int seed = i;\n            hashFunctions[i] = (element) -> (element.hashCode() ^ seed * 0x9e3779b9) & Integer.MAX_VALUE;\n        }\n    }\n\n    public void add(T element) {\n        for (Function<T, Integer> f : hashFunctions) {\n            int hash = f.apply(element) % bitSize;\n            bits.set(hash);\n        }\n    }\n\n    public boolean mightContain(T element) {\n        for (Function<T, Integer> f : hashFunctions) {\n            int hash = f.apply(element) % bitSize;\n            if (!bits.get(hash)) return false;  // 任何一个位为 0 → 一定不存在\n        }\n        return true;  // 所有位都为 1 → 可能存在（可能误判）\n    }\n\n    // 当前填充的 bit 数\n    public int getBitCount() {\n        return bits.cardinality();\n    }\n}\n```\n\n核心原理：布隆过滤器的误判率公式——P ≈ (1 - e^{-kn/m})^k。当 k = (m/n) * ln(2) 时误判率最低。例如 m = 8n（每个元素 8 bit）、k = 5 时误判率约 2.1%。优点：空间效率极高，缺点：不支持删除（标准布隆过滤器），存在误判（False Positive）但不存在漏判（False Negative）。\n\n扩展延伸：1）Guava 的 BloomFilter——Google 的 Guava 库提供了成熟的 BloomFilter 实现，使用 Funnel 接口 + 64 位哈希分高低位模拟多个哈希函数，生产直接用。2）布隆过滤器的变种——Counting Bloom Filter（支持删除，用计数器数组替代位数组）、Scalable Bloom Filter（可扩容，误判率达到阈值时新增一层）、Cuckoo Filter（支持删除，误判率更低）。3）大 A 在系统设计中的用途——缓存穿透防护（查 DB 前先查布隆过滤器，不存在直接返回）、推荐系统去重、比特币 SPV 节点（轻钱包用布隆过滤器过滤交易）。4）海量数据判重——当数据量大到无法用 HashSet 存储时（如 10 亿个 URL 判重），布隆过滤器用几百 MB 内存即可实现低误判率的判重。","hints":["布隆过滤器能删除元素吗——标准版不能，因为无法确定一个位是哪个元素设置的（多个元素可能共享同一个位）","实际生产中怎么确定 m 和 k——根据预期的 n 和可接受的误判率 p 用公式计算最优值"],"tags":["算法","布隆过滤器","概率数据结构","判重"],"id":611},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"一致性哈希在分布式负载均衡中的工程实现","content":"一致性哈希在分布式负载均衡中如何精确实现？虚拟节点怎么确定数量？Ketama 一致性哈希算法的工作原理是什么？如果后端服务器的权重不同（如 16 核 64G 和 8 核 32G 的机器混部），一致性哈希如何支持加权分配？","answer":"答案：一致性哈希在负载均衡中的核心是：将服务器节点和虚拟节点哈希到环上，请求按 key 哈希后顺时针找到最近的虚拟节点，从而路由到对应物理节点。加权分配通过调整虚拟节点数量实现——权重越高的机器分配的虚拟节点越多，被命中的概率越大。\n\n解析：1）环的构建——哈希环通常使用 2^32 个槽位（0 ~ 2^32-1）。对每个物理节点的标识（IP:Port 或名称）进行哈希，映射到环上。2）虚拟节点——为什么需要虚拟节点：物理节点的哈希值在环上分布可能不均匀（少量节点时尤其明显）。解决方案是为每个物理节点创建 N 个虚拟节点（如 100-200 个），每个虚拟节点是「物理节点 ID + 编号」的哈希。请求命中虚拟节点后映射回物理节点。3）加权一致性哈希——权重高的物理节点分配更多的虚拟节点。例如权重 2:1 的两台机器，虚拟节点数 200:100。Ketama 算法（最早在 Memcached 客户端中实现）是加权一致性哈希的典型实现：对每个物理节点，根据其权重计算虚拟节点数（权重 × 160 或其他基数），生成「服务器名-编号」的 MD5 哈希，截取 4 字节作为环上的位置，存到 TreeMap 中。4）请求路由——对请求 key 做哈希，用 TreeMap 的 tailMap() 找到第一个 >= 哈希值的节点，如果没有则取 TreeMap 的第一个（环的尾部回到头部）。\n\n扩展延伸：工程实现细节：1）TreeMap 的 tailMap + 取首元素是 O(log n) 的操作，适合单机几千个虚拟节点级别的环。2）虚拟节点数量的 trade-off——虚拟节点越多分布越均匀，但构建和查找的开销越大。行业中常见每个物理节点 100-200 个虚拟节点（Ketama 默认 160）。3）一致性哈希 vs 哈希槽——Redis Cluster 用的是哈希槽（16384 个固定槽），不是一致性哈希。每个 key 映射到槽，槽分配到节点。增减节点时只需要迁移槽（迁移的只是槽范围，不需要 rehash 所有 key）。4）一致性哈希的变体——Jump Consistent Hash：O(log n) 时间复杂度，不需要维护环结构。只适合节点按线性编号增减的场景（不支持任意节点名）。Google 的 Maglev 哈希：用于负载均衡器的快速连接调度，使用「查找表（LUT）」方式实现几乎均匀的分配。\n\n加权分配的细节——如果机器性能不同（如新机器 32 核 128G vs 老机器 8 核 32G），可以按 benchmark 得分或 CPU 核数比例分配虚拟节点。注意权重不是精确的流量分配——一致性哈希是按 key 分配的，不是按请求数均匀分配。如果要精确的流量分配比例，需要用「有界负载一致性哈希」（Consistent Hashing with Bounded Loads）——在分配时检查节点的当前负载是否超过平均负载的一定比例，超过则跳过。","hints":["虚拟节点为什么能解决分布不均——量变引起质变，虚拟节点越多分布越均匀","一致性哈希能保证请求均匀分配到后端吗——不能保证精确均匀，只能保证「概率上均匀」+「增减节点时最小化重新分配」"],"tags":["算法","一致性哈希","负载均衡","分布式","Ketama"],"id":612},{"category":"algorithm_extras","difficulty":"hard","type":"coding","title":"实现 Trie 树（前缀树）","content":"实现一个 Trie 树（前缀树 / 字典树），支持 insert(word)、search(word) 和 startsWith(prefix) 操作。Trie 树常用于自动补全、拼写检查和前缀匹配场景。请讲解为什么 Trie 树在字符串搜索/匹配中比哈希表有优势。","answer":"```java\nclass Trie {\n    static class TrieNode {\n        TrieNode[] children = new TrieNode[26]; // 只包含小写字母\n        boolean isEnd = false;                   // 标记是否存在完整单词\n    }\n\n    private final TrieNode root;\n\n    public Trie() {\n        root = new TrieNode();\n    }\n\n    public void insert(String word) {\n        TrieNode node = root;\n        for (char c : word.toCharArray()) {\n            int idx = c - 'a';\n            if (node.children[idx] == null) {\n                node.children[idx] = new TrieNode();\n            }\n            node = node.children[idx];\n        }\n        node.isEnd = true;\n    }\n\n    public boolean search(String word) {\n        TrieNode node = searchPrefix(word);\n        return node != null && node.isEnd;\n    }\n\n    public boolean startsWith(String prefix) {\n        return searchPrefix(prefix) != null;\n    }\n\n    private TrieNode searchPrefix(String prefix) {\n        TrieNode node = root;\n        for (char c : prefix.toCharArray()) {\n            int idx = c - 'a';\n            if (node.children[idx] == null) return null;\n            node = node.children[idx];\n        }\n        return node;\n    }\n}\n```\n\n核心思想：Trie 树利用字符串的公共前缀来减少存储空间和查询时间。每个节点代表一个字符，路径表示一个字符串。插入和查询的时间复杂度都是 O(L)，L 是字符串长度，与 Trie 树中已有的字符串数量无关！\n\n扩展延伸：Trie vs 哈希表——1）前缀搜索：哈希表不支持高效的 startsWith / 前缀匹配（需要遍历所有 key）。Trie 树的 startsWith 时间复杂度 O(L)。2）自动补全：Trie 树可以遍历以某个前缀开头的所有单词（DFS 从前缀节点往下）。3）内存占用：Trie 树的每个节点都存储了一个 26 元素的数组（或 HashMap），在数据稀疏时内存浪费严重（每个字符消耗一个对象）。优化：用 HashMap<Character, TrieNode> 替代固定数组（节省内存但增加哈希开销），或用「压缩 Trie」/「Radix Tree」（将单链路的路径压缩到一个节点）。4）应用场景——浏览器/IDE 的自动补全、搜索框联想（Search Suggestion）、拼写检查、路由表查找（CIDR 匹配用二进制 Trie）。\n\n空间优化扩展：1）数组 vs HashMap——字符集大（如 Unicode）时数组太浪费，用 HashMap。2）Radix Tree（基数树）——将只有一个子节点的路径压缩成一个节点，大幅减少节点数。Linux 路由表、Nginx 的 location 匹配用 Radix Tree。3）Double-Array Trie（双数组 Trie）——用两个数组（base + check）实现，内存占用极小（CJK 分词常用）。Elasticsearch 的索引前缀查询用 Double-Array Trie。4）不适合的场景——字符串数量少且长度长，Trie 的内存开销高于哈希表。","hints":["为什么 Trie 树的前缀匹配比哈希表高效——哈希表需要遍历所有 key，Trie 沿着前缀路径走 O(L) 即可","Trie 树的空间开销主要在哪里——每个节点存储子节点数组/哈希表 + 节点对象开销，在稀疏字符串时内存浪费"],"tags":["算法","Trie","前缀树","字符串","数据结构"],"id":613},{"category":"algorithm_extras","difficulty":"medium","type":"choice","title":"排序算法稳定性与工程选型","content":"在需要对「先按分数降序排列，分数相同的按姓名升序排列」的多字段排序中，以下哪种策略利用「稳定排序」特性可以最简洁地实现？","options":["A) 自定义比较器同时比较分数和姓名，一次排序完成","B) 先按姓名升序排序（次要字段），再按分数降序排序（主要字段），使用稳定排序算法","C) 先按分数降序排序，再对分数相同的组内按姓名升序排序","D) 稳定排序在这里没有优势，直接使用不稳定排序 + 复杂比较器即可"],"answer":"答案：B) 先按姓名升序排序（次要字段），再按分数降序排序（主要字段），使用稳定排序算法\n\n解析：稳定排序的定义是：当两个元素 key 相等时，排序前后的相对顺序不变。利用这个特性，先将数据按次要字段（姓名）排序，再按主要字段（分数）降序排序。因为第二次排序是稳定的，分数相等时两元素的相对顺序保持第一次排序的结果（即姓名升序）。这是「稳定排序反向运用」的经典技巧。A 虽然也能正确实现，但需要写一个复杂的比较器（同时处理两个字段的顺序）。C 走了弯路——先分组再排序不如直接两次稳定排序简洁。D 错误地认为稳定排序没有优势。\n\n扩展延伸：各排序算法的稳定性：1）稳定排序——归并排序（O(n log n)）、插入排序（O(n²)，n 小时最快）、冒泡排序（O(n²)）。2）不稳定排序——快速排序、堆排序、选择排序、希尔排序。3）工程中 Java 的 Arrays.sort(Object[]) 用 TimSort（稳定，归并 + 插入混合排序，O(n log n)），Arrays.sort(int[]) 用 Dual-Pivot QuickSort（不稳定，但对基本类型来说稳定性无意义）。4）Python 的 list.sort() 和 sorted() 也是稳定排序（Timsort）。5）为什么基本类型排序不需要稳定（int[]/double[]）——基本类型没有「相等」之外的额外属性，相等即相同，排序结果无需区分。\n\n扩展——真实的排序场景：SQL 的 ORDER BY 子句可以直接实现：ORDER BY score DESC, name ASC。数据库排序引擎（通常使用归并排序或 Timsort）自动处理。如果是内存中排序，在 Java 中用 stream：list.stream().sorted(Comparator.comparingInt(Student::getScore).reversed().thenComparing(Student::getName))——这是自定义比较器方案（即 A），底层调用 TimSort 是稳定的。如果排序数据量超大（内存放不下），需要外部排序（多路归并排序），也是稳定性天然支持。","hints":["两次稳定排序分别排序要字段和次字段——先在稳定的排序中搞定次要顺序","Java 的 Arrays.sort(Object[]) 为什么用 Timsort（稳定）而 Arrays.sort(int[]) 用 Dual-Pivot QuickSort（不稳定）"],"tags":["算法","排序","稳定性","Timsort"],"id":614},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"BitMap 与 RoaringBitmap：海量数据位图压缩原理","content":"BitMap（位图）的核心思想是用一个 bit 表示一个元素是否存在，在海量数据中有广泛的应用。但当数据稀疏时，BitMap 的空间浪费严重。RoaringBitmap（咆哮位图）如何在保持高效位运算的同时大幅压缩存储空间？它和传统的 BitMap / Java BitSet 相比有什么优势？求交集/并集的时间复杂度是多少？","answer":"答案：BitMap 用一个 bit 代表一个整数是否存在，查询和交集/并集操作可以用位运算高效完成。但当数据稀疏（如存储 [1, 1000000, 2000000]）时，一个 200 万位的 BitMap 需要 250KB，而 RoaringBitmap 通过分桶 + 自适应编码压缩到几 KB。\n\n解析：1）传统 BitMap——适合稠密数据（如存储 0 ~ 1000 万连续整数），每个元素 1 bit，查询 O(1)，交集/并集用位运算（popcount 或 SSE/AVX 指令集）非常快。缺点：数据稀疏时浪费空间（不管有没有值都要占一个 bit）。2）RoaringBitmap 的分桶策略——将 32 位整数的空间分为高 16 位（65536 个桶）和低 16 位（桶内编号）。高 16 位决定元素属于哪个桶（key），低 16 位决定桶内的位置。每个桶包含一个 Container，有三种类型：ArrayContainer（当桶内元素 < 4096 个时，直接存 sorted short 数组，每个元素 2 字节）、BitmapContainer（当桶内元素 >= 4096 个时，用 65536 bit = 8KB 的 BitMap）、RunContainer（当桶内元素连续成段时，存 (start, length) 对，适合连续范围如 [1,2,3,4,5] 存为 (1,5)）。\n\n扩展延伸：优点与性能：1）自适应压缩：RoaringBitmap 在数据稀疏时用 ArrayContainer（几个整数只占几字节），数据稠密时自动升为 BitmapContainer（位运算效率高）。连续数据用 RunContainer（压缩比最高）。这比固定使用 BitMap 灵活得多。2）求交集——遍历两个 RoaringBitmap 的桶，只有 key 相同的桶才需要做交集。两个 ArrayContainer 的交集用「双指针归并」O(n + m)；两个 BitmapContainer 的交集用位运算 AND；Array vs Bitmap 时将 Array 逐个检查是否在 Bitmap 中。3）求并集——类似。全集并集性能与数据分布有关。4）内存对比——存储 1000 万个随机整数：传统 BitMap 需要 1.2GB（不考虑分页时），RoaringBitmap 约 18MB。5）应用场景——Lucene/Elasticsearch 的倒排索引存储（文档 ID 集合）、Apache Druid 的 Bitmap 索引列、Redis 的扩展模块（Redis-Roaring）、ClickHouse 的位图函数。6）RoaringBitmap 在 64 位场景的局限——32 位分桶设计在 64 位整数上不能直接用，需要用多个 RoaringBitmap 或 64 位变体（如 Roaring64Bitmap 用高位 32 位做桶再进行类似处理）。","hints":["RoaringBitmap 什么时候用 ArrayContainer 什么时候用 BitmapContainer——以 4096 个元素为阈值（65536 的 1/16）","RoaringBitmap 为什么在 OLAP 引擎中这么受欢迎——海量数据场景下内存效率极高，且位运算性能好"],"tags":["算法","数据结构","BitMap","RoaringBitmap","压缩"],"id":615},{"category":"algorithm_extras","difficulty":"hard","type":"coding","title":"接雨水 II (Trapping Rain Water II)","content":"给定一个 m x n 的矩阵，其中的值代表每个单元格的高度。请你计算该矩阵形状能够接住的最大雨水量（三维接雨水）。","answer":"解题思路：三维接雨水不能直接使用二维接雨水的双指针法，需要使用最小堆（优先队列）从边界向内BFS。\n\n算法步骤：1）将矩阵最外圈的所有单元格加入最小堆（按高度排序），并标记为已访问。2）初始化总水量为0，边界最高水位为堆顶最小高度。3）从堆中弹出最低的单元格，检查其四个方向邻居。4）如果邻居高度低于当前边界水位，则水量 += 边界水位 - 邻居高度；否则更新边界水位。5）将邻居加入堆并标记已访问。6）重复直到堆为空。\n\n关键思路：水从边界向内流，永远从最低的缺口处向外溢。所以用最小堆保证每次处理的是当前所有边界中最矮的点。\n\n复杂度：O(m*n*log(m+n)) 时间，O(m*n) 空间。","hints":["为什么三维接雨水不能直接复用二维的左右双指针解法","最小堆中存储的元素应该包含哪些信息——坐标和高度，为什么高度用于决定水位"],"tags":["算法","堆","BFS","接雨水"],"id":620},{"category":"algorithm_extras","difficulty":"hard","type":"coding","title":"二叉树的最大宽度","content":"给定一棵二叉树，求其最大宽度。宽度定义为每一层的节点数，包括中间的空节点（null 节点也要计入位置）。二叉树的最大宽度是所有层的最大宽度。","answer":"核心思路：对节点进行编号（类似堆的索引），利用 BFS 层序遍历计算每层首个和末尾节点的编号差。\n\n```java\npublic int widthOfBinaryTree(TreeNode root) {\n    if (root == null) return 0;\n    Queue<Pair<TreeNode, Integer>> queue = new LinkedList<>();\n    queue.offer(new Pair<>(root, 1));  // 根节点编号为 1\n    int maxWidth = 1;\n\n    while (!queue.isEmpty()) {\n        int size = queue.size();\n        int left = queue.peek().getValue();  // 当前层最左节点编号\n        int right = left;\n\n        for (int i = 0; i < size; i++) {\n            Pair<TreeNode, Integer> pair = queue.poll();\n            TreeNode node = pair.getKey();\n            int idx = pair.getValue();\n            right = idx;\n\n            if (node.left != null) {\n                queue.offer(new Pair<>(node.left, idx * 2));\n            }\n            if (node.right != null) {\n                queue.offer(new Pair<>(node.right, idx * 2 + 1));\n            }\n        }\n        maxWidth = Math.max(maxWidth, right - left + 1);\n    }\n    return maxWidth;\n}\n```\n\n时间复杂度 O(n)，空间 O(n)。\n\n关键细节：1）如果树的深度很大（如 1000 层），编号指数级增长会导致整数溢出。解决方案：每层偏移归一化——将最左节点的编号重新映射为 1。2）DFS 解法也通用，用 Map<level, firstIndex> 记录每层第一个节点编号。\n\n扩展延伸：如果题目问的是「二叉树最大宽度（不考虑空节点）」则是简单的每层 BFS 计数。本题的难点在于空节点也计入位置——堆编号法是直观解法。","hints":["为什么要用堆编号的方式而不是简单的层节点计数","树的深度很大时编号溢出如何解决"],"tags":["二叉树","BFS","宽度","编号"],"id":621},{"category":"algorithm_extras","difficulty":"medium","type":"coding","title":"用 Rand7 实现 Rand10","content":"已有 API rand7() 可以等概率生成 1 到 7 的随机整数，请实现 rand10() 等概率生成 1 到 10 的随机整数。不允许使用系统随机函数。分析期望调用次数。","answer":"核心思路：拒绝采样（Rejection Sampling）——用 rand7() 生成更大范围的等概率整数。\n\n```java\npublic int rand10() {\n    while (true) {\n        int row = rand7();\n        int col = rand7();\n        int idx = (row - 1) * 7 + col;  // 生成 1..49 等概率\n        if (idx <= 40) {\n            return (idx - 1) % 10 + 1;\n        }\n        // idx 为 41..49，拒绝并重试\n    }\n}\n```\n\n期望调用次数：每次生成需要 2 次 rand7()，接受概率 40/49 ≈ 81.6%，期望调用 rand7 次数 = 2 * 49/40 = 2.45 次。\n\n优化：拒绝的 9 个值（41..49）不直接丢弃，而是映射为 1..9 再次采样：\n```java\npublic int rand10() {\n    while (true) {\n        int a = rand7();\n        int b = rand7();\n        int idx = (a - 1) * 7 + b;\n        if (idx <= 40) return (idx - 1) % 10 + 1;\n\n        a = idx - 40;  // 1..9\n        b = rand7();   // 1..7\n        idx = (a - 1) * 7 + b;  // 1..63\n        if (idx <= 60) return (idx - 1) % 10 + 1;\n\n        a = idx - 60;  // 1..3\n        b = rand7();\n        idx = (a - 1) * 7 + b;  // 1..21\n        if (idx <= 20) return (idx - 1) % 10 + 1;\n    }\n}\n```\n\n扩展延伸：通用解法——用 randM 实现 randN。如果 M >= N，直接拒绝采样；如果 M < N，多次调用组合扩展范围。核心原则是保证每个候选值等概率生成。","hints":["(row - 1) * 7 + col 为什么能生成 1..49 的等概率分布","拒绝采样的期望运行时间如何计算"],"tags":["随机数","拒绝采样","概率"],"id":622},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"LFU 缓存淘汰策略的数据结构设计","content":"设计并实现一个 LFU（Least Frequently Used）缓存，支持 O(1) 时间的 get 和 put 操作。核心数据结构如何设计？","answer":"LFU 的核心挑战是在 O(1) 时间内同时维护「访问频率」和「访问时间」两个维度。\n\n经典解法——双哈希表 + 频率链表：1）keyToNode——HashMap<Key, Node>。Node 包含 key、value、freq、prev、next 指针。2）freqToList——HashMap<Frequency, DoubleLinkedList>。每个频率对应一个双向链表，同一频率的节点按最近访问时间排序（头部最新，尾部最旧）。3）minFreq——全局变量记录当前最小频率，用于 O(1) 淘汰。\n\n操作逻辑：1）get(key)——存在则 freq++，将节点从原 freq 链表移到 freq+1 链表头部。如果原链表为空且 freq == minFreq，则 minFreq++。2）put(key, value)——key 已存在则更新 value 并升级 freq。key 不存在时检查容量，满则淘汰 minFreq 链表尾节点（同一频率中最久未访问的）。插入新节点，freq=1，minFreq=1。\n\n```java\nclass LFUCache {\n    int capacity, minFreq, size;\n    Map<Integer, Node> keyToNode;\n    Map<Integer, DoublyLinkedList> freqToList;\n\n    public LFUCache(int capacity) { ... }\n\n    public int get(int key) {\n        Node node = keyToNode.get(key);\n        if (node == null) return -1;\n        updateFreq(node);\n        return node.value;\n    }\n\n    public void put(int key, int value) { ... }\n\n    void updateFreq(Node node) {\n        freqToList.get(node.freq).remove(node);\n        if (freqToList.get(node.freq).isEmpty() && node.freq == minFreq) {\n            minFreq++;\n        }\n        node.freq++;\n        freqToList.computeIfAbsent(node.freq, k -> new DoublyLinkedList())\n                  .addFirst(node);\n    }\n}\n```\n\n扩展延伸：LFU 的缺点——缓存污染：某个 key 被高频访问后即使不再使用也会长期存在。解决方案：LFU-Aging（频率定期衰减）或 Window-TinyLFU（Caffeine 使用的策略，结合频率和新鲜度）。","hints":["LFU 相比 LRU 多维护了哪个维度信息","同一频率内有多个节点时淘汰哪个"],"tags":["缓存","LFU","数据结构","O(1)"],"id":623},{"category":"algorithm_extras","difficulty":"medium","type":"short_answer","title":"位运算实现集合幂运算与子集枚举","content":"如何用位运算高效地枚举一个集合的所有子集？请解释「枚举子集的子集」的二进制技巧及其时间复杂度分析。","answer":"用整数的二进制位表示集合，子集枚举是位运算高效算法的基石。\n\n基本技巧：1）枚举所有子集——for (int mask = 0; mask < (1 << n); mask++)，mask 的二进制位表示元素选/不选。2）枚举子集的子集（Submask Enumeration）——对给定集合 mask，枚举其所有子集：for (int sub = mask; sub > 0; sub = (sub - 1) & mask)。该循环从 mask 本身开始，每次将最低位的 1 转化为 0 并填充后续位，跳到下一个子集。\n\n时间复杂度分析：枚举所有集合的子集的子集的总复杂度是 O(3^n)。证明：每个元素有三种状态——「不在 mask 中」「在 mask 中且在 sub 中」「在 mask 中但不在 sub 中」，总枚举量为 ΣC(n,k)*2^k = (1+2)^n = 3^n。\n\n常用模式：1）状态压缩 DP（如 TSP）——dp[mask][i] 表示经过集合 mask 个城市且最后到达城市 i 的最短路径。2）容斥原理——利用子集枚举计算并集的大小。3）SOS DP（Sum Over Subsets）——对每个 mask 计算其所有子集的贡献之和。\n\n```java\n// 枚举 mask 的所有子集（循环包含空集）\nfor (int sub = mask; ; sub = (sub - 1) & mask) {\n    // 处理 sub\n    if (sub == 0) break;\n}\n```\n\n扩展延伸：Gray Code（格雷码）枚举子集——相邻子集只差一个元素，适合需要增量计算差异的场景。","hints":["sub = (sub - 1) & mask 这一步为什么能跳过无效组合","3^n 的时间复杂度推导过程是什么"],"tags":["位运算","子集枚举","状态压缩","SOS DP"],"id":624},{"category":"algorithm_extras","difficulty":"hard","type":"choice","title":"图着色问题与贪心近似算法","content":"对于一个无向图 G，以下关于图着色问题（Graph Coloring）的描述哪一项是正确的？","options":["A) 图着色问题是 P 类问题，贪心算法总能找到最优解","B) Welsh-Powell 算法的核心思想是按度数降序着色，每次选择可用的最小颜色编号，但不保证最优","C) 四色定理证明任何平面图都可以用最多 5 种颜色着色，贪心算法可以找到最优解","D) 图着色问题与最大团问题的关系是：团的颜色数等于团的大小"],"answer":"B","hints":["图着色问题的计算复杂度属于哪个类","四色定理的具体结论是什么"],"tags":["图着色","贪心算法","NP完全","Welsh-Powell"],"id":625}];

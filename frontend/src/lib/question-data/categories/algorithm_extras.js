// Auto-generated by scripts/generate-data.js. Do not edit.
// Category: algorithm_extras — 12 questions
export const category = "algorithm_extras";
export const questions = [{"category":"algorithm_extras","difficulty":"medium","type":"short_answer","title":"树状数组（Fenwick Tree）","content":"请解释树状数组（Fenwick Tree/BIT）的原理及与线段树的对比。","answer":"树状数组用巧妙的前缀和思想实现 O(log N) 单点更新和前缀查询\n\n解析：树状数组利用 lowbit(x) = x & -x 确定索引覆盖范围。数组 BIT[i] 维护 (i - lowbit(i), i] 区间的和。核心操作：1）add(i, val) — 从 i 开始不断 i += lowbit(i) 更新所有覆盖该位置的节点 O(log N) 2）sum(i) — 从 i 开始不断 i -= lowbit(i) 累加前缀和。区间查询 [l, r] = sum(r) - sum(l-1)。lowbit 的原理是利用补码表示，取最低位的 1 及其后的 0。\n\n扩展延伸：树状数组扩展应用：1）区间更新 + 单点查询（用差分数组构建 BIT，更新区间 [l,r] 时 add(l, val) 和 add(r+1, -val)，单点查询就是前缀和）2）区间更新 + 区间查询（需要两个 BIT 配合）3）求逆序对（离散化后按值插入 BIT，统计大于当前值的数量）4）二维 BIT（处理矩阵前缀和）。树状数组的局限：维护的信息必须可逆（求和、异或可用，最值不可用），不支持区间赋值操作（需用线段树）。常数和代码量均优于线段树。","hints":["lowbit(x) = x & -x 为什么能得到最低位的 1","树状数组能处理区间最值查询吗"],"tags":["树状数组","Fenwick","BIT"],"id":669},{"category":"algorithm_extras","difficulty":"medium","type":"short_answer","title":"快速幂与矩阵快速幂","content":"请介绍快速幂的原理以及如何推广到矩阵快速幂。","answer":"快速幂（Binary Exponentiation）用 O(log n) 的时间计算 a^n。\n\n快速幂原理：\n- 将 n 写成二进制形式：n = b0*2^0 + b1*2^1 + ... + bk*2^k\n- a^n = a^(b0*2^0) × a^(b1*2^1) × ... × a^(bk*2^k)\n- 每次迭代：a = a * a（平方），如果当前位为 1 则乘到结果中\n\n实现要点：\n- 使用位运算：while n > 0: if n & 1: res *= a; a *= a; n >>= 1\n- 注意取模运算：(a * b) % mod\n- 处理 n = 0 的情况：结果为 1\n\n矩阵快速幂：\n- 同样原理，将 a 从标量换成矩阵\n- 矩阵乘法：C[i][j] = sum(A[i][k] * B[k][j])\n- 需要实现矩阵乘法函数（O(k³)，k 为矩阵边长）\n\n典型应用（用矩阵快速幂优化递推）：\n1. 斐波那契数列：\n   - 转移矩阵 [[1,1],[1,0]] 的 n 次方\n   - [F[n], F[n-1]] = [F[1], F[0]] × M^(n-1)\n2. 线性递推：任意 k 阶线性递推都可以用 k×k 转移矩阵 O(k³ log n) 求第 n 项\n3. 图论：邻接矩阵的 k 次方 = 长度为 k 的路径数\n\n复杂度：\n- 快速幂：O(log n)\n- 矩阵快速幂：O(k³ log n)，k 为矩阵边长\n\n扩展延伸：\n- 常系数齐次线性递推可以用 BM 算法（Berlekamp-Massey）求出递推式后再用矩阵快速幂\n- 对于大 k（>1000）的递推，线性递推（Linear Recurrence）用多项式技巧（多项式取模）优化到 O(k² log n) 或 O(k log k log n)","hints":["快速幂：二进制分解指数，迭代平方，O(log n)","矩阵快速幂：将标量乘法替换为矩阵乘法，用于线性递推 O(k³ log n)","经典应用：斐波那契数列、线性递推、图论路径计数"],"tags":["算法","快速幂","矩阵快速幂","递推"],"id":689},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"数组中的逆序对计数","content":"请解释使用归并排序计算数组中逆序对数量的原理。","answer":"在归并排序合并阶段统计逆序对——当右半边元素小于左半边时，左半边剩余元素都与该元素构成逆序\n\n解析：在归并排序的合并过程中，左右两个子数组已经分别有序（升序）。当从右半边取元素放入结果数组时，如果右半边的当前元素小于左边当前元素，则左半边从当前到末尾的所有元素都与这个右半边元素构成逆序对（因为左半边元素 > 右半边当前元素，且左半边所有元素的原始索引都在右半边当前元素之前）。在 merge 步骤中统计 count += mid - i + 1 即可累加。算法总复杂度 O(N log N)（归并排序本身），额外空间 O(N)（归并用的临时数组）。\n\n扩展延伸：逆序对数等于冒泡排序的交换次数（每次相邻交换恰好消除一个逆序对），也反映数组有序程度。Predecessor 问题：求每个元素在前面有多少个比它大的元素（前缀逆序），可以用 Fenwick Tree（树状数组）或 Segment Tree 在线查询（离散化 + BIT，从右向左遍历，对每个元素查询 BIT 中比它小的数量）。更一般的扩展：求数组中满足 a[i] > a[j] * k 的逆序对（带权重），BIT 同样适用。LeetCode 315（Count of Smaller Numbers After Self）即求每个元素右边的逆序数量——从右向左遍历，离散化后用 BIT 维护频次即可 O(N log N)。三元素逆序对（a[i] > a[j] > a[k], i<j<k）可以用两次 BIT——分别维护「以 j 结尾的逆序对数量」和「以 k 结尾的三元组数量」。","hints":["归并排序合并时统计逆序对的时机和累加公式","树状数组怎么在线计算每个元素右边的比它小的元素个数"],"tags":["数组","逆序对","归并排序"],"id":690},{"category":"algorithm_extras","difficulty":"easy","type":"choice","title":"二维矩阵搜索策略","content":"在一个每行从左到右递增且每列从上到下递增的矩阵中搜索目标值，最优算法的复杂度是？","options":["A) O(M*N) 暴力搜索","B) O(M*logN) 每行二分","C) O(M+N) 从右上角开始搜索","D) O(log(M*N)) 全局二分"],"answer":"C","hints":["从右上角开始，比目标大向左移，比目标小向下移，每次排除一行或一列"],"tags":["矩阵","搜索","算法"],"id":691},{"category":"algorithm_extras","difficulty":"medium","type":"short_answer","title":"字符串循环移位","content":"请解释字符串循环移位（Rotate String）的高效实现方法。","answer":"字符串循环移位可以通过三次反转（Three-Reverse）在 O(N) 时间和 O(1) 额外空间实现\n\n解析：将字符串 s 循环左移 k 位的方法：1）反转前 k 个字符 2）反转剩余的 N-k 个字符 3）反转整个字符串。例如 s = \"abcdef\", k=2：反转前 2 个 \"ab\"→\"ba\"，反转后 4 个 \"cdef\"→\"fedc\"，得到 \"bafedc\"，再全部反转得到 \"cdefab\"。原理：XY -> (X^T Y^T)^T = YX。另一种方法：使用 s[(i+k)%N] = s[i] 的原地置换（类似循环队列），但需要 gcd 知识——如果 N 和 k 的最大公约数为 g，则需进行 g 轮循环（每轮从 i 开始跳 k 步直到回到 i）。Python 和 Java 中也可以 s = s[k:] + s[:k] 一行实现但 O(N) 空间。\n\n扩展延伸：循环移位相关的问题：1）字符串旋转匹配（s1 旋转后能否等于 s2）——判断 s2 in s1+s1 即可 2）字符串环的最小表示法（求循环同构串中字典序最小的表示）——用双指针 O(N) 算法（最小表示法）3）旋转后数组的二分搜索——旋转数组搜索 target（LeetCode 33），先二分找旋转点再二分搜索。KMP 算法也可以用于旋转匹配（判断 s1+s1 不拼接而用模式匹配）。手写原地循环移位的 gcd 版本：内层 for j in range(g), i=j, tmp=s[i], do{next=(i+k)%N, swap(s[next], tmp), i=next} while i!=j。工程中优先使用三步反转法（简洁且 cache-friendly）。","hints":["三步反转法为什么能实现循环移位","判断 s1 旋转后能否等于 s2 为什么可以用 s1+s1 包含 s2 来判断"],"tags":["字符串","旋转","算法"],"id":692},{"category":"algorithm_extras","difficulty":"hard","type":"coding","title":"火星词典（Alien Dictionary）","content":"现有一种使用英语字母的外星语言，这门语言的字母顺序与英语顺序不同。给定一个按「新字母顺序」排序的该语言单词列表 words，请返回这门语言的排序后的字母顺序。如果排序不合法（存在循环依赖），返回空字符串。","answer":"```java\npublic String alienOrder(String[] words) {\n    Map<Character, Set<Character>> graph = new HashMap<>();\n    Map<Character, Integer> inDegree = new HashMap<>();\n    \n    // 初始化所有出现的字母\n    for (String word : words) {\n        for (char c : word.toCharArray()) {\n            graph.putIfAbsent(c, new HashSet<>());\n            inDegree.putIfAbsent(c, 0);\n        }\n    }\n    \n    // 比较相邻单词，提取边\n    for (int i = 0; i < words.length - 1; i++) {\n        String w1 = words[i], w2 = words[i + 1];\n        int minLen = Math.min(w1.length(), w2.length());\n        boolean found = false;\n        for (int j = 0; j < minLen; j++) {\n            char c1 = w1.charAt(j), c2 = w2.charAt(j);\n            if (c1 != c2) {\n                if (!graph.get(c1).contains(c2)) {\n                    graph.get(c1).add(c2);\n                    inDegree.put(c2, inDegree.get(c2) + 1);\n                }\n                found = true;\n                break;\n            }\n        }\n        // 处理前缀相同但 w1 比 w2 长的情况（如 w1=\"abc\", w2=\"ab\" -> 非法）\n        if (!found && w1.length() > w2.length()) return \"\";\n    }\n    \n    // Kahn 算法拓扑排序\n    Queue<Character> queue = new LinkedList<>();\n    for (char c : inDegree.keySet()) {\n        if (inDegree.get(c) == 0) queue.offer(c);\n    }\n    \n    StringBuilder sb = new StringBuilder();\n    while (!queue.isEmpty()) {\n        char c = queue.poll();\n        sb.append(c);\n        for (char next : graph.get(c)) {\n            inDegree.put(next, inDegree.get(next) - 1);\n            if (inDegree.get(next) == 0) queue.offer(next);\n        }\n    }\n    \n    return sb.length() == inDegree.size() ? sb.toString() : \"\";\n}\n```\n时间复杂度 O(C)（C 是所有单词的总字符数），空间复杂度 O(1)（最多 26 个字母）。\n\n核心思路：比较相邻单词找到第一个不同的字母，构建有向边（c1 -> c2 表示 c1 在 c2 之前）。用拓扑排序（Kahn 算法）确定字母顺序。如果图中存在环（排序结果字符数少于总字符数），说明排序非法。\n\n边界情况：如果 w1 是 w2 的前缀且 w1 比 w2 长（如 w1=\"abcd\", w2=\"abc\"），排序非法——因为按排序规则短单词应该在更前面。","hints":["如何从有序单词列表推断字母顺序——比较相邻单词的第一个不同字母","拓扑排序检测循环依赖——Kahn 算法中如果排序结果不全说明有环"],"tags":["拓扑排序","图","Kahn","字符串"],"options":[],"id":693},{"category":"algorithm_extras","difficulty":"hard","type":"coding","title":"设计搜索自动补全系统","content":"设计一个搜索自动补全系统（Search Autocomplete System）。用户输入句子时，系统返回排名前 3 的热门补全建议。\n\n实现 AutocompleteSystem 类：\n- AutocompleteSystem(String[] sentences, int[] times)：系统初始化时给定历史句子及其出现次数。\n- List<String> input(char c)：用户输入一个字符。如果 c == '#'，表示输入结束，将当前输入的句子记录到系统（次数 +1），返回空列表。否则返回前缀匹配且排名前 3 的补全建议。\n\n排名规则：先按热度（出现次数）降序，次数相同按句子的字典序升序。","answer":"```java\nclass AutocompleteSystem {\n    class TrieNode {\n        Map<Character, TrieNode> children = new HashMap<>();\n        Map<String, Integer> freqMap = new HashMap<>(); // 以该前缀开头的所有句子及其热度\n    }\n    \n    private TrieNode root;\n    private StringBuilder currentInput;\n    private TrieNode currentNode;\n    \n    public AutocompleteSystem(String[] sentences, int[] times) {\n        root = new TrieNode();\n        currentInput = new StringBuilder();\n        currentNode = root;\n        for (int i = 0; i < sentences.length; i++) {\n            addSentence(sentences[i], times[i]);\n        }\n    }\n    \n    private void addSentence(String sentence, int freq) {\n        TrieNode node = root;\n        for (char c : sentence.toCharArray()) {\n            node.children.putIfAbsent(c, new TrieNode());\n            node = node.children.get(c);\n            node.freqMap.put(sentence, node.freqMap.getOrDefault(sentence, 0) + freq);\n        }\n    }\n    \n    public List<String> input(char c) {\n        if (c == '#') {\n            String sentence = currentInput.toString();\n            addSentence(sentence, 1);\n            currentInput = new StringBuilder();\n            currentNode = root;\n            return new ArrayList<>();\n        }\n        currentInput.append(c);\n        if (!currentNode.children.containsKey(c)) {\n            currentNode.children.put(c, new TrieNode());\n        }\n        currentNode = currentNode.children.get(c);\n        \n        // 取热度前 3\n        PriorityQueue<Map.Entry<String, Integer>> pq = new PriorityQueue<>(\n            (a, b) -> a.getValue() == b.getValue() \n                ? b.getKey().compareTo(a.getKey())  // 字典序降序（为了让优先队列弹出较大的）\n                : a.getValue() - b.getValue()        // 热度升序\n        );\n        for (Map.Entry<String, Integer> entry : currentNode.freqMap.entrySet()) {\n            pq.offer(entry);\n            if (pq.size() > 3) pq.poll();\n        }\n        \n        List<String> result = new ArrayList<>();\n        while (!pq.isEmpty()) {\n            result.add(0, pq.poll().getKey()); // 逆序加入使结果按热度降序\n        }\n        return result;\n    }\n}\n```\n\n时间复杂度：input 操作 O(N * log 3) = O(N)（N 是以当前前缀开头的句子数），addSentence 操作 O(L)（L 是句子长度）。\n\n核心设计：每个 Trie 节点存储以该前缀开头的所有句子及其频率（freqMap）。用户在输入过程中逐字符走 Trie，在 end 节点收集所有候选并按规则排序取前 3。\n\n优化思路：\n- 如果热门句子集合是静态的，可以在每个节点预计算 Top 3（离线计算，查询 O(1)）。\n- 如果句子数量极大（百万级），freqMap 的内存占用会很大——可以将匹配限制在 Top K 范围内，用堆或布隆过滤器剪枝。\n- 实际搜索引擎的自动补全使用「Trie + 预计算 Top N + LRU 缓存」的组合方案。","hints":["Trie 的每个节点存储以该前缀开头的所有句子的频率——空间换时间","Top 3 可以用最小堆维护——堆大小固定为 3，每次弹出最小的"],"tags":["Trie","设计","堆","字符串"],"options":[],"id":694},{"category":"algorithm_extras","difficulty":"hard","type":"coding","title":"接雨水 II (Trapping Rain Water II)","content":"给定一个 m x n 的矩阵，其中的值代表每个单元格的高度。请你计算该矩阵形状能够接住的最大雨水量（三维接雨水）。","answer":"解题思路：三维接雨水不能直接使用二维接雨水的双指针法，需要使用最小堆（优先队列）从边界向内BFS。\n\n算法步骤：1）将矩阵最外圈的所有单元格加入最小堆（按高度排序），并标记为已访问。2）初始化总水量为0，边界最高水位为堆顶最小高度。3）从堆中弹出最低的单元格，检查其四个方向邻居。4）如果邻居高度低于当前边界水位，则水量 += 边界水位 - 邻居高度；否则更新边界水位。5）将邻居加入堆并标记已访问。6）重复直到堆为空。\n\n关键思路：水从边界向内流，永远从最低的缺口处向外溢。所以用最小堆保证每次处理的是当前所有边界中最矮的点。\n\n复杂度：O(m*n*log(m+n)) 时间，O(m*n) 空间。\n\n```java\n// BFS + 最小堆解法\npublic int trapRainWater(int[][] heightMap) {\n    if (heightMap == null || heightMap.length == 0) return 0;\n    int m = heightMap.length, n = heightMap[0].length;\n    boolean[][] visited = new boolean[m][n];\n    PriorityQueue<int[]> pq = new PriorityQueue<>((a, b) -> a[2] - b[2]);\n    for (int i = 0; i < m; i++) {\n        for (int j = 0; j < n; j++) {\n            if (i == 0 || i == m-1 || j == 0 || j == n-1) {\n                pq.offer(new int[]{i, j, heightMap[i][j]});\n                visited[i][j] = true;\n            }\n        }\n    }\n    int[][] dirs = {{0,1},{0,-1},{1,0},{-1,0}};\n    int res = 0, maxH = 0;\n    while (!pq.isEmpty()) {\n        int[] cell = pq.poll();\n        maxH = Math.max(maxH, cell[2]);\n        for (int[] d : dirs) {\n            int x = cell[0]+d[0], y = cell[1]+d[1];\n            if (x >= 0 && x < m && y >= 0 && y < n && !visited[x][y]) {\n                visited[x][y] = true;\n                res += Math.max(0, maxH - heightMap[x][y]);\n                pq.offer(new int[]{x, y, Math.max(heightMap[x][y], maxH)});\n            }\n        }\n    }\n    return res;\n}\n```","hints":["为什么三维接雨水不能直接复用二维的左右双指针解法","最小堆中存储的元素应该包含哪些信息——坐标和高度，为什么高度用于决定水位"],"tags":["算法","堆","BFS","接雨水"],"id":698},{"category":"algorithm_extras","difficulty":"hard","type":"coding","title":"二叉树的最大宽度","content":"给定一棵二叉树，求其最大宽度。宽度定义为每一层的节点数，包括中间的空节点（null 节点也要计入位置）。二叉树的最大宽度是所有层的最大宽度。","answer":"核心思路：对节点进行编号（类似堆的索引），利用 BFS 层序遍历计算每层首个和末尾节点的编号差。\n\n```java\npublic int widthOfBinaryTree(TreeNode root) {\n    if (root == null) return 0;\n    Queue<Pair<TreeNode, Integer>> queue = new LinkedList<>();\n    queue.offer(new Pair<>(root, 1));  // 根节点编号为 1\n    int maxWidth = 1;\n\n    while (!queue.isEmpty()) {\n        int size = queue.size();\n        int left = queue.peek().getValue();  // 当前层最左节点编号\n        int right = left;\n\n        for (int i = 0; i < size; i++) {\n            Pair<TreeNode, Integer> pair = queue.poll();\n            TreeNode node = pair.getKey();\n            int idx = pair.getValue();\n            right = idx;\n\n            if (node.left != null) {\n                queue.offer(new Pair<>(node.left, idx * 2));\n            }\n            if (node.right != null) {\n                queue.offer(new Pair<>(node.right, idx * 2 + 1));\n            }\n        }\n        maxWidth = Math.max(maxWidth, right - left + 1);\n    }\n    return maxWidth;\n}\n```\n\n时间复杂度 O(n)，空间 O(n)。\n\n关键细节：1）如果树的深度很大（如 1000 层），编号指数级增长会导致整数溢出。解决方案：每层偏移归一化——将最左节点的编号重新映射为 1。2）DFS 解法也通用，用 Map<level, firstIndex> 记录每层第一个节点编号。\n\n扩展延伸：如果题目问的是「二叉树最大宽度（不考虑空节点）」则是简单的每层 BFS 计数。本题的难点在于空节点也计入位置——堆编号法是直观解法。","hints":["为什么要用堆编号的方式而不是简单的层节点计数","树的深度很大时编号溢出如何解决"],"tags":["二叉树","BFS","宽度","编号"],"id":699},{"category":"algorithm_extras","difficulty":"hard","type":"coding","title":"用 Rand7 实现 Rand10","content":"已有 API rand7() 可以等概率生成 1 到 7 的随机整数，请实现 rand10() 等概率生成 1 到 10 的随机整数。不允许使用系统随机函数。分析期望调用次数。","answer":"核心思路：拒绝采样（Rejection Sampling）——用 rand7() 生成更大范围的等概率整数。\n\n```java\npublic int rand10() {\n    while (true) {\n        int row = rand7();\n        int col = rand7();\n        int idx = (row - 1) * 7 + col;  // 生成 1..49 等概率\n        if (idx <= 40) {\n            return (idx - 1) % 10 + 1;\n        }\n        // idx 为 41..49，拒绝并重试\n    }\n}\n```\n\n期望调用次数：每次生成需要 2 次 rand7()，接受概率 40/49 ≈ 81.6%，期望调用 rand7 次数 = 2 * 49/40 = 2.45 次。\n\n优化：拒绝的 9 个值（41..49）不直接丢弃，而是映射为 1..9 再次采样：\n```java\npublic int rand10() {\n    while (true) {\n        int a = rand7();\n        int b = rand7();\n        int idx = (a - 1) * 7 + b;\n        if (idx <= 40) return (idx - 1) % 10 + 1;\n\n        a = idx - 40;  // 1..9\n        b = rand7();   // 1..7\n        idx = (a - 1) * 7 + b;  // 1..63\n        if (idx <= 60) return (idx - 1) % 10 + 1;\n\n        a = idx - 60;  // 1..3\n        b = rand7();\n        idx = (a - 1) * 7 + b;  // 1..21\n        if (idx <= 20) return (idx - 1) % 10 + 1;\n    }\n}\n```\n\n扩展延伸：通用解法——用 randM 实现 randN。如果 M >= N，直接拒绝采样；如果 M < N，多次调用组合扩展范围。核心原则是保证每个候选值等概率生成。","hints":["(row - 1) * 7 + col 为什么能生成 1..49 的等概率分布","拒绝采样的期望运行时间如何计算"],"tags":["随机数","拒绝采样","概率"],"id":700},{"category":"algorithm_extras","difficulty":"hard","type":"short_answer","title":"LFU 缓存淘汰策略的数据结构设计","content":"设计并实现一个 LFU（Least Frequently Used）缓存，支持 O(1) 时间的 get 和 put 操作。核心数据结构如何设计？","answer":"LFU 的核心挑战是在 O(1) 时间内同时维护「访问频率」和「访问时间」两个维度。\n\n经典解法——双哈希表 + 频率链表：1）keyToNode——HashMap<Key, Node>。Node 包含 key、value、freq、prev、next 指针。2）freqToList——HashMap<Frequency, DoubleLinkedList>。每个频率对应一个双向链表，同一频率的节点按最近访问时间排序（头部最新，尾部最旧）。3）minFreq——全局变量记录当前最小频率，用于 O(1) 淘汰。\n\n操作逻辑：1）get(key)——存在则 freq++，将节点从原 freq 链表移到 freq+1 链表头部。如果原链表为空且 freq == minFreq，则 minFreq++。2）put(key, value)——key 已存在则更新 value 并升级 freq。key 不存在时检查容量，满则淘汰 minFreq 链表尾节点（同一频率中最久未访问的）。插入新节点，freq=1，minFreq=1。\n\n```java\nclass LFUCache {\n    int capacity, minFreq, size;\n    Map<Integer, Node> keyToNode;\n    Map<Integer, DoublyLinkedList> freqToList;\n\n    public LFUCache(int capacity) { ... }\n\n    public int get(int key) {\n        Node node = keyToNode.get(key);\n        if (node == null) return -1;\n        updateFreq(node);\n        return node.value;\n    }\n\n    public void put(int key, int value) { ... }\n\n    void updateFreq(Node node) {\n        freqToList.get(node.freq).remove(node);\n        if (freqToList.get(node.freq).isEmpty() && node.freq == minFreq) {\n            minFreq++;\n        }\n        node.freq++;\n        freqToList.computeIfAbsent(node.freq, k -> new DoublyLinkedList())\n                  .addFirst(node);\n    }\n}\n```\n\n扩展延伸：LFU 的缺点——缓存污染：某个 key 被高频访问后即使不再使用也会长期存在。解决方案：LFU-Aging（频率定期衰减）或 Window-TinyLFU（Caffeine 使用的策略，结合频率和新鲜度）。","hints":["LFU 相比 LRU 多维护了哪个维度信息","同一频率内有多个节点时淘汰哪个"],"tags":["缓存","LFU","数据结构","O(1)"],"id":701},{"category":"algorithm_extras","difficulty":"medium","type":"short_answer","title":"位运算实现集合幂运算与子集枚举","content":"如何用位运算高效地枚举一个集合的所有子集？请解释「枚举子集的子集」的二进制技巧及其时间复杂度分析。","answer":"用整数的二进制位表示集合，子集枚举是位运算高效算法的基石。\n\n基本技巧：1）枚举所有子集——for (int mask = 0; mask < (1 << n); mask++)，mask 的二进制位表示元素选/不选。2）枚举子集的子集（Submask Enumeration）——对给定集合 mask，枚举其所有子集：for (int sub = mask; sub > 0; sub = (sub - 1) & mask)。该循环从 mask 本身开始，每次将最低位的 1 转化为 0 并填充后续位，跳到下一个子集。\n\n时间复杂度分析：枚举所有集合的子集的子集的总复杂度是 O(3^n)。证明：每个元素有三种状态——「不在 mask 中」「在 mask 中且在 sub 中」「在 mask 中但不在 sub 中」，总枚举量为 ΣC(n,k)*2^k = (1+2)^n = 3^n。\n\n常用模式：1）状态压缩 DP（如 TSP）——dp[mask][i] 表示经过集合 mask 个城市且最后到达城市 i 的最短路径。2）容斥原理——利用子集枚举计算并集的大小。3）SOS DP（Sum Over Subsets）——对每个 mask 计算其所有子集的贡献之和。\n\n```java\n// 枚举 mask 的所有子集（循环包含空集）\nfor (int sub = mask; ; sub = (sub - 1) & mask) {\n    // 处理 sub\n    if (sub == 0) break;\n}\n```\n\n扩展延伸：Gray Code（格雷码）枚举子集——相邻子集只差一个元素，适合需要增量计算差异的场景。","hints":["sub = (sub - 1) & mask 这一步为什么能跳过无效组合","3^n 的时间复杂度推导过程是什么"],"tags":["位运算","子集枚举","状态压缩","SOS DP"],"id":702}];

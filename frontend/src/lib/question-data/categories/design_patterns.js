// Auto-generated by scripts/generate-data.js. Do not edit.
// Category: design_patterns — 66 questions
export const category = "design_patterns";
export const questions = [{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"适配器模式（Adapter）","content":"适配器模式的目的是什么？类适配器和对象适配器的区别？适配器模式和外观模式（Facade）的区别？适配器模式在实际开发中的应用？（如 Spring 中的适配器）","answer":"答案：适配器模式将一个类的接口转换成客户端期望的另一个接口，让原本不兼容的类可以一起工作。类适配器使用继承（适配器同时继承目标接口和适配者类），对象适配器使用组合（适配器持有适配者对象的引用）。对象适配器更灵活（适配者子类也可适配），类适配器需要多继承（Java 中不直接支持）。\n\n解析：结构——1）Target（目标接口）：客户端期望的接口。2）Adapter（适配器）：将 Adaptee 的接口转换为 Target 接口。3）Adaptee（适配者）：需要被适配的已有的类。4）Client 通过 Target 接口调用 Adapter，Adapter 内部调用 Adaptee。对象适配器示例：`class Adapter implements Target { private Adaptee adaptee; }`——Adapter 持有 Adaptee 对象并将 Target 方法的调用转发给 Adaptee。\n\n扩展延伸：实际应用——1）Spring MVC 中的 HandlerAdapter：将不同的 handler（Controller/HttpRequestHandler/SimpleControllerHandlerAdapter）适配到统一的 DispatcherServlet 调用。2）java.util.Arrays#asList()：将数组适配到 List 接口。3）java.io.InputStreamReader：将 InputStream 适配到 Reader。4）日志门面（SLF4J）：为不同的日志框架（Log4j/Logback/java.util.logging）提供统一的适配层。适配器 vs 外观（Facade）——1）适配器：解决「接口不兼容」的问题，改变接口以匹配客户端的期望。2）外观：为复杂子系统提供简化的统一接口，不改变原有接口的能力。适配器模式通常在系统设计之后（为了兼容遗留代码），而外观模式是设计时对复杂子系统的简化封装。","hints":["类适配器用继承（不可修改 Adaptee 的行为），对象适配器用组合（更灵活，推荐使用）","适配器 vs 外观——适配器「转换接口」，外观「简化接口」。适配器是事后兼容，外观是事前面向客户端简化"],"tags":["设计模式","适配器模式","结构型","Spring"],"id":1564},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"抽象工厂模式（Abstract Factory）","content":"抽象工厂模式和工厂方法模式的区别？什么场景适合用抽象工厂模式？抽象工厂模式的「产品族」概念是什么？","answer":"答案：工厂方法模式：一个工厂创建一个产品；抽象工厂模式：一个工厂创建一组相关的产品（产品族）。抽象工厂是工厂方法的扩展——当系统需要生产多个产品系列（不同品牌/不同操作系统）时使用。\n\n解析：结构——1）AbstractFactory：声明创建一组产品的方法（createProductA/createProductB）。2）ConcreteFactory：实现创建具体产品族的方法（如 WindowsFactory 创建 WindowsButton + WindowsTextBox；MacFactory 创建 MacButton + MacTextBox）。3）AbstractProduct：产品接口（Button/TextBox）。4）ConcreteProduct：具体产品（WindowsButton/MacButton）。5）客户端只使用 AbstractFactory 和 AbstractProduct 接口，不依赖具体工厂和产品。\n\n扩展延伸：场景——1）跨平台 UI 组件：Windows/Mac/Linux 各有一套按钮、文本框、菜单（产品族）。运行时根据操作系统选择对应的 Factory。2）数据库连接：不同数据库（MySQL/PostgreSQL/Oracle）各有 Connection/Statement/ResultSet（产品族）。JDBC 是抽象工厂的经典实现。3）主题切换：不同主题（暗色/亮色）的样式组件。优缺点——优点：确保同一工厂创建的产品之间兼容；客户端与具体产品解耦；更换产品族只需更换一个工厂对象。缺点：扩展产品族中的产品困难（需要修改 AbstractFactory 接口，所有 ConcreteFactory 都需要更新）。\"产品族\"——同一工厂生产的一组互相依赖/配套的产品。同一操作系统下的所有 UI 控件（Button + TextBox + Menu 互相配合使用）就是一个产品族。","hints":["产品族（Product Family）——同一工厂生产的一组配套产品，如 Windows 的 Button + TextBox + Menu","抽象工厂 vs 工厂方法——工厂方法「一个工厂一种产品」，抽象工厂「一个工厂一组相关产品」"],"tags":["设计模式","抽象工厂","创建型","产品族"],"id":1565},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"原型模式（Prototype）","content":"原型模式（Prototype Pattern）的原理？Java 中浅克隆（Shallow Copy）和深克隆（Deep Copy）的区别？原型模式相比 new 创建对象的优势？","answer":"答案：原型模式通过复制已有对象来创建新对象（克隆），而不是通过 new 创建。在 Java 中实现 Cloneable 接口并重写 clone()。当对象创建成本较高（数据库查询/复杂初始化）且需要大量相似对象时，克隆比 new 更高效。\n\n解析：浅克隆 vs 深克隆——1）浅克隆（Shallow Copy）：复制对象本身，但对象内部的引用类型字段指向同一个对象（两个对象共享引用）。Java 的 Object.clone() 默认实现是浅克隆。2）深克隆（Deep Copy）：不仅复制对象本身，还递归复制所有引用类型的字段，使得原始对象和克隆对象完全独立。实现方式：重写 clone() 在内部递归 clone 引用字段；序列化（Serializable + ObjectOutputStream）；手动 new 并逐个赋值。3）Cloneable 接口是一个标记接口（不包含方法），clone() 方法来自 Object。如果对象没有实现 Cloneable 却调用了 clone()，会抛出 CloneNotSupportedException。\n\n扩展延伸：原型模式的应用——1）Spring 框架中的原型作用域（prototype scope）：每次获取 Bean 时返回一个新的实例（原型模式的应用）。2）Java 的 ArrayList、HashMap 等都提供了 clone() 方法（返回浅克隆）。3）游戏开发中大量对象的创建（子弹/敌人生成）——避免频繁 new 的开销。4）配置对象的复制——从默认配置克隆出定制配置。原型 vs new——克隆不调用构造函数，因此不会执行构造方法中的逻辑。这意味着如果构造器中有关键初始化操作，克隆会直接绕过（既是优点也是缺点）。序列化实现深克隆注意事项：所有被克隆的类都需要实现 Serializable。JSON 序列化（Jackson/Gson）可以实现深克隆但不要求 Serializable。","hints":["Java 的 clone() 是浅克隆——引用类型字段指向同一个对象，修改一个会影响另一个","原型模式不调用构造函数——直接内存复制创建对象，绕过构造器中的初始化逻辑"],"tags":["设计模式","原型模式","创建型","克隆"],"id":1566},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"中介者模式（Mediator）","content":"中介者模式（Mediator Pattern）的目的是什么？它如何解决对象之间多对多交互的复杂度问题？中介者模式和观察者模式的区别？实际应用（如 MVC 中的 Controller）？","answer":"答案：中介者模式通过引入一个中介对象来封装一组对象之间的交互，将多对多的交互简化为一对多的交互（对象→中介者）。各个对象不需要显式引用彼此，减少了耦合度。中介者控制交互逻辑，使得交互规则的变化集中在中介者中。\n\n解析：结构——1）Mediator（中介者接口）：定义通信方法（notify/send）。2）ConcreteMediator：维护各个 Colleague 的引用，协调交互逻辑。3）Colleague（同事）：每个同事对象持有中介者的引用，通过中介者与其他同事通信。不直接调用其他同事的方法。同事之间只通过中介者通信——例如聊天室中的用户不直接发消息给彼此，而是发给聊天室（中介者），由聊天室广播给其他人。\n\n扩展延伸：实际应用——1）MVC 架构：Controller 是 Model 和 View 的中介者——View 的事件发给 Controller，Controller 更新 Model，Model 变化通过 Controller 通知 View。2）聊天室（Chat Room）：用户通过聊天室发消息，聊天室协调消息的转发。3）Air Traffic Control（塔台）：飞机不直接通信，通过塔台协调航线。4）java.util.Timer：schedule 方法中 Timer 协调任务的执行。中介者 vs 观察者——1）观察者模式：一对多关系（一个 Subject 通知多个 Observer），Observer 之间无直接交互。2）中介者模式：多对多关系，Colleague 之间可能互相影响，交互逻辑集中在中介者。3）两者可以组合使用：中介者内部使用观察者模式实现消息通知。","hints":["中介者将多对多关系简化为一对多——Colleague 只与 Mediator 通信，不直接引用其他 Colleague","MVC 中的 Controller 是中介者模式——View 的事件传 Controller，Controller 更新 Model，Model 变化传回 View"],"tags":["设计模式","中介者模式","行为型","MVC"],"id":1567},{"category":"design_patterns","difficulty":"easy","type":"short_answer","title":"备忘录模式（Memento）","content":"备忘录模式（Memento Pattern）用于解决什么问题？备忘录模式的三个角色分别是什么？备忘录模式在实现撤销（Undo）功能时如何使用？","answer":"答案：备忘录模式在不破坏封装的前提下捕获一个对象的内部状态，并在该对象之外保存这个状态，以便之后将对象恢复到原先保存的状态。核心思想：状态保存和恢复由原发器（Originator）自己负责，备忘录（Memento）是一个不可变的状态快照，管理者（Caretaker）保存和提供备忘录但不查看其内容。\n\n解析：三个角色——1）Originator（原发器）：需要保存和恢复状态的对象。提供 createMemento()（创建快照）和 restore(memento)（从快照恢复）方法。2）Memento（备忘录）：存储 Originator 的内部状态（不可变对象）。对外提供有限接口（getState 可能只暴露部分信息或不暴露内部状态）。3）Caretaker（管理者）：负责保存 Memento（如存储在 List\\ 中）。不对 Memento 的内容做任何操作（不读取也不修改）。\n\n扩展延伸：应用——1）文本编辑器的 Undo/Redo：每次修改文本创建一个 Memento（文本内容的快照），Undo 时恢复到上一个 Memento。2）游戏存档：玩家进度存档机制——游戏状态保存为 Memento，读档时通过 Memento 恢复。3）事务回滚：操作前保存状态快照，操作失败时恢复。4）关于封装性：备忘录模式的关键保护在于 Memento 只暴露给 Originator 必要的信息，对 Caretaker 隐藏内部细节。如果 Memento 对外可见其内部状态，就破坏了 Originator 的封装性。Java 实现技巧：使用内部类（Memento 作为 Originator 的内部类，对外部类私有字段可见，对其他类隐藏）。","hints":["备忘录模式的关键是封装性的保护——Memento 对 Originator 开放内部状态，对 Caretaker 隐藏","Caretaker 只负责保存和提供 Memento，不能查看或修改 Memento 的内容"],"tags":["设计模式","备忘录模式","行为型","撤销"],"id":1568},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"Zookeeper 的 Zab 协议","content":"Zab（Zookeeper Atomic Broadcast）协议的原理是什么？Zab 与 Raft 协议的区别？Leader 选举和原子广播的过程？Zab 如何保证消息的顺序性和一致性？","answer":"答案：Zab 是 Zookeeper 使用的崩溃恢复原子广播协议，保证在 Leader 崩溃后恢复时不会丢失已提交的提案。Zab 的特点是「主从 + 原子广播」，所有写请求必须通过 Leader 广播到多数 Follower 后才算提交。\n\n解析：Zab 的两个阶段——1）Leader 选举（崩溃恢复）：当 Leader 宕机或大多数 Follower 联系不上 Leader 时进入选举。使用 Fast Leader Election——每个节点投票给自己，收到更高 epoch/zxid 的节点时改投，多数节点达成一致时选出新 Leader。关键：zxid（Zookeeper Transaction ID）越大表示数据越新。新 Leader 必须是拥有最新数据的节点（zxid 最大）。2）原子广播（Atomic Broadcast）：Leader 将写请求转为 Proposal（提案）广播给所有 Follower。Follower 写本地日志后回复 ACK（确认）。Leader 收到多数 ACK 后提交（Commit），并通知 Follower 提交。\n\n扩展延伸：Zab vs Raft——1）选举条件：Zab 基于 zxid（事务 ID，越大越新），Raft 基于 term（任期）+ log index（日志索引）。2）日志方向：Zab 的日志方向是固定的（Leader→Follower），Raft 也是 Leader→Follower 但方向更灵活。3）成员变更：Raft 有明确的 Joint Consensus 成员变更协议，Zab 的处理不如 Raft 清晰。Raft 更易理解、更广泛用于新系统（etcd/Consul 等使用 Raft）。Zab 在 Zookeeper 中运行良好但不如 Raft 易移植。Zab 在 CAP 理论中属于 CP（一致性+分区容忍性）——Leader 宕机时集群不可用 30-120 秒（选举期间不可用），但保证选举后数据一致。","hints":["Zab 的 zxid（事务 ID）包含 epoch 和 counter——epoch 标记 Leader 任期，大的 zxid 表示更新的数据","Zab 在 Leader 选举期间不可用（CP 系统）——选举时间通常 30-120 秒，频繁选举会影响可用性"],"tags":["分布式","Zookeeper","Zab","一致性"],"id":1665},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"Raft 协议与 Leader 选举","content":"Raft 协议中 Leader 选举的过程？Term（任期）和 Election Timeout（选举超时）的作用？Raft 如何保证最多只有一个 Leader 被选出？","answer":"答案：Raft 使用随机超时（随机 Election Timeout）来避免选举平局。每个节点有三种状态：Follower（跟随者，初始状态）、Candidate（候选者，发起选举）、Leader（领导者，处理客户端请求）。Term（任期）是一个递增的编号，每个 Term 最多有一个 Leader。\n\n解析：选举过程——1）Follower 如果在 Election Timeout（150-300ms 随机）内没有收到 Leader 的心跳（Heartbeat），就转为 Candidate。2）Candidate 增加自己的 Term，给自己投票，并向所有其他节点发送 RequestVote RPC。3）其他节点在同一 Term 内只能投一票（先到先得），如果 Candidate 的日志比自己的更新则投票。4）Candidate 收到多数节点的投票后成为 Leader，立即发送 Heartbeat 通知其他节点。5）如果同时多个 Candidate 都没有获得多数票（Split Vote），各自超时后再次发起选举。\n\n扩展延伸：安全性保证——1）Election Safety：每个 Term 最多只有一个 Leader（通过「同一 Term 一票」机制保证）。2）Leader Append-Only：Leader 不能覆盖或删除自己日志中的已有条目。3）Log Matching：如果两个日志在相同 index 和 term 的 entry 相同，那么之前的所有 entry 也相同。4）Leader Completeness：被提交的日志条目在后续选举中一定存在于新 Leader 中（Candidate 的日志必须比现有节点「至少一样新」才可能当选）。Raft 的应用——etcd（Kubernetes 使用 etcd 存储集群状态）、Consul（服务发现）、TiDB（分布式数据库）。","hints":["随机 Election Timeout（150-300ms）避免 Split Vote——多个 Candidate 同时参选的概率降低","同一 Term 一票机制保证选举安全——每个节点在每个 Term 只能投一票，不可能选出两个 Leader"],"tags":["分布式","Raft","一致性","Leader选举"],"id":1666},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"分布式一致性哈希（Consistent Hashing）","content":"一致性哈希（Consistent Hashing）解决了什么问题？虚拟节点（Virtual Nodes）的作用？一致性哈希和普通哈希取模的区别？一致性哈希在分布式缓存（如 Redis Cluster）中的应用？","answer":"答案：一致性哈希解决了分布式系统中节点增减导致的大量数据重新映射问题。普通哈希取模（hash(key) % N）在节点数量 N 变化时几乎所有键都要重新映射（缓存雪崩风险）。一致性哈希在哈希环上做映射，节点的增减只影响环上相邻节点的数据。\n\n解析：工作原理——1）将哈希值空间组织成一个环（0 → 2^32-1）。2）节点（服务器的 hash）和键（key 的 hash）都映射到环上。3）每个键找到环上顺时针方向最近的节点，存储在该节点上。4）节点增减时，只影响该节点在环上顺时针方向的相邻节点（的数据需要迁移），其他节点不受影响。虚拟节点（Virtual Nodes）——每个物理节点在环上创建多个虚拟节点（复制品）。解决两个问题：节点在环上分布不均匀（导致数据倾斜）；某节点宕机时，其负载只能转移到相邻节点（可能压垮那个节点）。虚拟节点让负载更均匀地分布到所有物理节点。\n\n扩展延伸：应用场景——1）Redis Cluster 使用一致性哈希的变体（16384 个槽位机制——Hash Slot，而不是一致性哈希环）。每个节点负责一段槽位，节点增减时迁移槽位。2）Amazon DynamoDB/DynamoDB 使用一致性哈希（带虚拟节点）。3）Akamai CDN 使用一致性哈希将内容映射到缓存服务器。4）Cassandra 使用一致性哈希（Token Ring）。一致性哈希的问题——节点分布不均匀时数据倾斜（虚拟节点解决）。热点 key（某个 key 被频繁访问）——一致性哈希不解决热点问题，需要配合本地缓存或应用层负载均衡。一致性哈希保证的是映射稳定性而非负载均衡。","hints":["普通取模 N 变化时所有 key 重新映射（缓存雪崩风险），一致性哈希只有 1/N 的 key 需要迁移","虚拟节点解决数据倾斜——每个物理节点有多个虚拟节点分布在环上，负载更均匀"],"tags":["分布式","一致性哈希","负载均衡","缓存"],"id":1667},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"分布式事务解决方案","content":"分布式事务有哪些主流方案？XA（两阶段提交）的优缺点？TCC（Try-Confirm-Cancel）模式？SAGA 模式？Seata 的实现原理？","answer":"答案：分布式事务四大方案——XA（两阶段提交，强一致）、TCC（Try-Confirm-Cancel，最终一致）、SAGA（长事务补偿）、本地消息表 + MQ（可靠消息最终一致）。Seata 是主流的分布式事务框架，支持 AT（Automatic Transaction，基于 XA 的改进）、TCC、SAGA、XA 四种模式。\n\n解析：各方案——1）XA 两阶段提交（强一致性）：Prepare 阶段（所有参与者准备就绪 + 写 Undo/Redo 日志）→ Commit 阶段（协调者通知所有参与者提交）。问题：同步阻塞（Prepare 后资源锁定直到 Commit/Abort）、协调者单点、脑裂风险。适用于对一致性要求极高的场景（跨库转账）。2）TCC（Try-Confirm-Cancel）：由业务代码实现三阶段。Try：检查和预留资源（如冻结资金）。Confirm：确认执行业务（扣款）。Cancel：取消释放资源（解冻资金）。优点：无需全局锁、性能好。缺点：侵入性强（业务代码需要实现三阶段接口）。3）SAGA：将长事务拆为多个本地事务 + 补偿事务。每个子事务执行成功→执行下一个；执行失败→反向执行补偿事务。\n\n扩展延伸：Seata 的 AT 模式——1）对 XA 的优化：XA 锁的是数据库资源，AT 通过拦截 SQL 自动生成逆向 SQL（UNDO_LOG），业务无需改造。2）一阶段：执行业务 SQL + 生成 before image 和 after image（记录在 UNDO_LOG 表）。3）二阶段：如果全部成功，异步清理 UNDO_LOG。如果有失败，根据 UNDO_LOG 中的 before image 自动补偿回滚。应用场景——强一致：XA/AT（转账/支付）。最终一致：TCC（订单/库存）、SAGA（长流程审批）。可靠消息：本地消息表（异步通知类场景）。没有万能的分布式事务方案，核心是业务可以接受多强的一致性。","hints":["XA 是「资源锁定」的强一致，TCC 是「业务预留」的最终一致——TCC 性能更好但侵入性更强","Seata AT 模式通过生成逆向 SQL 自动实现回滚——不需业务代码实现补偿逻辑"],"tags":["分布式","事务","Seata","一致性"],"id":1668},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"限流算法与实现","content":"常见的限流算法有哪些？计数器算法、滑动窗口、漏桶（Leaky Bucket）、令牌桶（Token Bucket）的区别？Sentinel 和 RateLimiter 分别是如何实现限流的？","answer":"答案：四大限流算法——计数器（固定窗口，简单但有临界问题）、滑动窗口（将窗口细分，更平滑）、漏桶（恒定速率处理，可以应对突发输入）、令牌桶（允许一定程度的突发请求，最常用）。\n\n解析：算法对比——1）计数器（固定窗口）：单位时间窗口内计数达到上限后拒绝。缺点：临界问题——在窗口边界处可能出现两倍峰值（假如窗口 1s 限制 100，在 0.9s 来了 100 个请求，1.0-1.1s 又来了 100 个，实际 0.2s 内处理了 200 个）。2）滑动窗口：将窗口细分更小的格子（如 1s 窗口分为 10 个 100ms 的格子），滑动统计最近一个窗口的请求数。格子越多越精确但占用内存越多。3）漏桶（Leaky Bucket）：请求进入桶，桶以恒定速率漏水（处理请求）。桶满时新请求被丢弃。不管请求多突发，流出速率恒定。适合保护下游系统不被突发流量冲击。4）令牌桶（Token Bucket）：以恒定速率向桶中添加令牌，请求需要拿到令牌才能处理。桶中有缓存令牌时允许突发请求。Guava RateLimiter 使用令牌桶（支持预消费——允许一定程度的突发）。\n\n扩展延伸：限流工具——1）Guava RateLimiter：单机限流，令牌桶算法，支持 warmup（平滑预热，避免冷启动时瞬间限流）。2）Sentinel（Alibaba）：分布式限流，支持滑动窗口、令牌桶、漏桶多种算法。支持限流降级（熔断降级、系统自适应保护）。与 Spring Cloud 集成。3）Nginx 限流：ngx_http_limit_req_module（漏桶算法），ngx_http_limit_conn_module（连接数限制）。4）Redis 分布式限流：基于 Redis + Lua 脚本实现滑动窗口（Lua 保证原子性）。限流 vs 熔断——限流控制「请求速率」，熔断控制「错误率/延迟」（当错误率达到阈值时熔断器打开，快速失败防止级联）。","hints":["固定窗口的临界问题——窗口边界处瞬时流量可达两倍上限，滑动窗口通过细分窗口解决","令牌桶 vs 漏桶——令牌桶允许突发（攒了 N 个令牌可一次性用完），漏桶处理速率恒定（不管多突发都平滑处理）"],"tags":["分布式","限流","Sentinel","RateLimiter"],"id":1669},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"单例模式全场景分析与防破坏","content":"除了双重检查锁定和静态内部类，还有哪些实现单例的方式？如何防止反射和序列化破坏单例？Spring 的单例 Bean 与 GOF 单例模式有何不同？","answer":"答案：单例模式六种实现——饿汉、懒汉（synchronized）、双重检查锁（DCL volatile）、静态内部类、枚举、ThreadLocal 线程级单例。防御破坏：构造器内加标志位（防反射创建第二实例），实现 readResolve()（防反序列化创建新实例）。Spring 单例 Bean 是容器级别的实例管理，同类可注册多个不同 Bean，与 JVM 级别的 GOF 单例本质不同。\n\n解析：六种实现对比——1）饿汉：类加载时初始化（JVM 线程安全），可能浪费资源（提前创建）。2）懒汉（synchronized 方法）：线程安全但对每次访问都加锁，性能差。3）DCL + volatile：JDK 5+ 必需 volatile 禁止 new Singleton() 的指令重排序。4）静态内部类：Holder 模式，JLS 保证类加载线程安全且懒加载，最推荐的懒加载方案。5）枚举：JVM 保证实例唯一，天然免疫反射和反序列化攻击（Enum 的构造器不可反射调用）。6）ThreadLocal 单例：每个线程一个实例（如 SimpleDateFormat）。反序列化破坏：ObjectInputStream 反射 readObject() 创建新实例，通过 readResolve() 返回已有实例防御。反射破坏：Constructor.setAccessible(true) + newInstance()，在构造器中用 boolean 标志位检测。\n\n扩展延伸：Spring 单例的作用域——Spring IoC 容器默认 singleton scope（每个容器一个实例），但可通过 @Scope(\"prototype\") 修改。一个 @Component 类可以注册为多个 Bean（不同名称和配置）。与 GOF 单模式本质区别：谁控制实例创建——GOF 在类内部控制（私有构造器），Spring 在容器外部控制（反射创建）。分布式系统下单例需分布式锁或选主。","hints":["为什么枚举可以天然防御反射和反序列化","Spring 单例和 GOF 单例的实例控制权有何不同"],"tags":["设计模式","单例","枚举","Spring","反序列化"],"id":1670},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"工厂模式在主流框架中的实战应用","content":"Spring、MyBatis、SLF4J 等主流框架中如何实现工厂模式？如何通过工厂模式设计可扩展的插件体系？","answer":"答案：Spring 的 BeanFactory 是所有 IoC 容器的根接口；MyBatis 的 SqlSessionFactory 创建 SqlSession；SLF4J 的 LoggerFactory 按 Logger 名称创建 Logger。工厂模式实现插件体系的核心机制是「工厂接口 + SPI 注册 + 反射加载」的组合。\n\n解析：框架中的工厂模式——1）Spring BeanFactory：最基础的容器接口，getBean() 通过反射或工厂方法创建实例。FactoryBean 接口自定义创建逻辑（如 FeignClientFactoryBean、MapperFactoryBean）。抽象工厂——BeanDefinitionRegistryPostProcessor 批量注册 BeanDefinition。2）MyBatis SqlSessionFactory：通过 SqlSessionFactoryBuilder 解析配置构建，openSession() 每次打开会话。SqlSessionFactory 是工厂方法，MyBatis-Spring 整合时用 @MapperScan 扫描接口注册 MapperFactoryBean。3）SLF4J LoggerFactory：getLogger(Class) 返回 Logger 实例，底层绑定通过 SPI 查找具体实现（logback/log4j2）。插件化架构模式：定义扩展点接口 → SPI 声明实现 → ServiceLoader 或 @Autowired 注入集合 → 按类型选择实现。Spring 中 @Autowired List<Extension> 自动收集所有扩展实现。\n\n扩展延伸：策略 + 工厂组合模式——Map<String, Strategy> @Autowired 自动注入，通过 getStrategy(type) 选择。在支付网关（微信/支付宝/银联）、审批流（多级别审批策略）、消息推送（短信/邮件/推送）中广泛使用。MyBatis Mapper 动态代理——不是静态工厂而是 JDK Proxy 动态代理，Spring + MyBatis 整合时将 Mapper 接口代理注入为 Bean。","hints":["FactoryBean 和 BeanFactory 有什么区别","为什么 MyBatis 选择动态代理而非静态工厂创建 Mapper"],"tags":["设计模式","工厂","Spring","MyBatis","SPI"],"id":1671},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"观察者模式与事件驱动架构的演化","content":"观察者模式和发布-订阅模式有什么区别？观察者模式如何演变为现代事件驱动架构？Spring 的事件机制是如何实现的？","answer":"答案：观察者模式中观察者直接注册到主题（一对多，紧耦合）。发布-订阅模式通过消息代理解耦发布者和订阅者。观察者模式是事件驱动架构的雏形。现代 EDA 通过事件总线/消息队列实现异步解耦。Spring 事件机制基于 ApplicationEvent + ApplicationListener + ApplicationEventPublisher，支持同步/异步事件驱动。\n\n解析：观察者模式结构——Subject（维护观察者列表，notify 遍历调用）→ Observer（update 接口）。缺点：Subject 持有 Observer 引用，耦合强，通知均为同步阻塞。发布-订阅演进——引入消息代理（Event Channel/Broker）：Publisher → Broker → Subscriber。优点：完全解耦（Publisher 不知道 Subscriber 存在）、支持异步、支持消息过滤和路由（Topic/Exchange）。Spring 事件机制——1）定义事件类（继承 ApplicationEvent 或使用 GenericApplicationEvent）。2）@EventListener 注解方法（或实现 ApplicationListener 接口）。3）ApplicationEventPublisher.publishEvent() 发布。4）默认同步（同一线程），@Async 实现异步。5）@TransactionalEventListener 支持事务阶段（AFTER_COMMIT/AFTER_ROLLBACK）。\n\n扩展延伸：观察者模式在 GUI 编程中广泛应用（Swing/AWT 的 Listener，JS 的 addEventListener）。现代事件驱动演进路线：观察者模式 → 事件总线（Guava EventBus/Spring Events）→ 消息队列（RabbitMQ/Kafka）→ 事件溯源（Event Sourcing）+ CQRS。微服务中观察者模式升级为服务间事件驱动通信——通过 Kafka/RocketMQ 实现跨服务事件通知和最终一致性。注意：观察者不适用于一对多的强依赖场景（一个 observer 失败影响全部），应改用 CompletableFuture 异步编排或消息队列。","hints":["Spring 的 @TransactionalEventListener 有什么用","观察者模式升级到消息队列解决了哪些问题"],"tags":["设计模式","观察者","发布订阅","Spring","事件驱动"],"id":1672},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"建造者模式与流式 API 设计","content":"建造者模式如何解决构造器参数过多的问题？Lombok @Builder 的底层实现原理是什么？如何在 API 设计中实现流畅的链式调用？","answer":"答案：建造者模式将复杂对象的构建拆分为多个步骤，实现参数灵活组合、对象不可变、代码可读性高。Lombok @Builder 在编译期生成静态内部 Builder 类（Javac Annotation Processor），每个字段生成同名方法返回 Builder 自身，最后 build() 创建目标对象。流式 API 核心：每个方法返回 this，提供终端方法结束链式调用。\n\n解析：传统方案对比——1）重叠构造器（Telescoping Constructor）：参数数量组合爆炸，类型易混淆。2）JavaBean 模式（无参构造 + setter）：对象可能进入不一致状态（设置一半），final 约束丢失。3）建造者模式：构造器 private，Builder 内部类负责校验和构建。优点：对象不可变（所有字段 final）/ 可分步设置 / 构建前完成校验。Lombok @Builder 原理：APT 在编译时生成 XXXBuilder 内部类，每个字段对应方法返回 Builder，build() 调用全参构造器。核心注解 @Builder.Default 处理默认值。流式 API 设计要点——方法名用 and/with 前缀，提供终端操作（build/execute/collect）结束链式调用，支持条件链式构造（带 if 判断的 addIf）。\n\n扩展延伸：建造者模式在 Java 标准库中的应用——StringBuilder（链式 append）、Stream API（map/filter 链式）、Optional（map/orElse 链式）、OkHttp's Request.Builder。Jackson JsonNodeBuilder。建造者模式与 Lombok @Builder 配合陷阱——@Builder 生成全参构造器，手动写构造器需加 @AllArgsConstructor 或 @Builder.ObtainVia。级联 Builder（子类继承时）需用 self() 返回子类 Builder 类型。Guava 的 ImmutableList.Builder 也是经典实现。","hints":["建造者模式如何保证构造出的对象是不可变的","Lombok @Builder 和 @AllArgsConstructor 的关系"],"tags":["设计模式","建造者","Lombok","流式API","Builder"],"id":1673},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"适配器模式与 SLF4J 门面设计","content":"适配器模式和门面模式（外观模式）有什么区别？SLF4J 作为日志门面如何统一多种日志框架？实际项目中适配器模式的应用场景有哪些？","answer":"答案：适配器模式解决接口不兼容——将一个接口转为客户端期望的另一个接口。门面模式为子系统提供统一高层接口。SLF4J 通过 SPI 加载底层绑定（logback/log4j2/java.util.logging），每种框架实现 SLF4J 的 Binding 适配器。适配器应用场景：接口兼容（旧系统迁移）、三方库包装、遗留系统集成、分页适配。\n\n解析：适配器模式结构——Target（目标接口）、Adaptee（被适配者）、Adapter（转换器，持有 Adaptee 引用实现 Target 接口）。两种形式：1）类适配器（继承 Adaptee，不推荐，Java 单继承限制）。2）对象适配器（组合 Adaptee，推荐）。SLF4J 架构——SLF4J API（Logger/LoggerFactory）→ SPI 选择绑定 → 具体适配器（logback-classic/log4j-slf4j-impl/jul-to-slf4j）→ 底层日志框架。SLF4J 作为门面（Facade）的优点：1）应用代码只依赖 SLF4J，不依赖具体框架。2）切换日志框架只需替换 jar 包，代码零改动。3）通过桥接 jar（jcl-over-slf4j/log4j-over-slf4j）统一项目中多种日志依赖。门面模式与适配器区别——门面提供新接口简化使用（例如 JdbcTemplate 封装 JDBC 操作），适配器转换已有接口保持兼容。\n\n扩展延伸：适配器在 Spring 中的经典实现——1）HandlerAdapter：MVC 中不同控制器（@Controller/HttpRequestHandler）通过适配器统一调用。2）AOP AdvisorAdapter：将不同通知类型适配到统一拦截器链。门面在 Spring 中的体现——RestTemplate（封装 HTTP 客户端）、JdbcTemplate（封装 JDBC）、TransactionTemplate（声明事务）。适配器 vs 装饰器：装饰器增强功能（BufferedInputStream 增强 FileInputStream），适配器转换接口形式（Arrays.asList 转换数组到 List）。设计原则——适配器模式是「开闭原则」的典型实践：通过适配器扩展而非修改原有类。","hints":["SLF4J 的桥接 jar（jcl-over-slf4j）和绑定 jar 有什么区别","适配器模式和装饰器模式在结构上相似，如何区分它们"],"tags":["设计模式","适配器","门面","SLF4J","Spring MVC"],"id":1674},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"状态模式（State Pattern）","content":"什么是状态模式？它解决什么问题？与策略模式有什么区别？请结合状态机的例子说明状态模式的应用。","answer":"答案：状态模式允许对象在内部状态改变时改变其行为，看起来就像对象本身的类变了。它通过将每个状态的行为封装到独立的状态类中，消除大量的条件判断语句。\n\n解析：状态模式的结构——1）Context（上下文）：持有当前状态对象的引用，将行为委托给状态对象处理。2）State（抽象状态）：定义所有状态下的行为接口。3）ConcreteState（具体状态）：实现特定状态下的行为，并负责在合适的时候切换到下一个状态。关键区别 vs 策略模式——状态模式的状态之间有关联和流转，状态的切换由状态对象或 Context 管理。策略模式的策略之间独立，由客户端（调用方）选择和切换策略。状态模式的行为变化是自动的（由内部状态驱动），策略模式的行为变化是主动的（由外部选择）。\n\n扩展延伸：经典应用——1）TCP 连接状态机：ESTABLISHED（收发数据）、CLOSE_WAIT（等待关闭）、TIME_WAIT（等待确认）。每个状态对 open/close/acknowledge 等操作有不同的响应。2）订单状态流转：待支付->已支付->已发货->已收货->已完成。每个状态下对「取消」「退款」「修改地址」等操作有不同的处理逻辑。3）游戏角色状态：普通->狂暴->眩晕->隐身，不同状态下移动、攻击等行为不同。状态模式的优点：状态转换逻辑集中管理，新增状态不影响现有代码（开闭原则）。缺点：状态类数量会膨胀。","hints":["状态模式和策略模式的结构很像但意图不同——关键在于状态之间是否可以转换","状态模式如何消除大量的 if-else 或 switch-case"],"tags":["状态模式","State Pattern","设计模式","GoF"],"id":1675},{"category":"design_patterns","difficulty":"easy","type":"short_answer","title":"策略模式（Strategy Pattern）","content":"什么是策略模式？它的使用场景是什么？如何在运行时动态切换策略？请以支付方式或排序算法为例说明。","answer":"答案：策略模式定义一系列算法（策略），将每个算法封装在独立的策略类中，使它们可以互相替换。客户端可以在运行时选择不同的策略，而不用修改使用策略的上下文代码。\n\n解析：策略模式的结构——1）Strategy（抽象策略）：定义所有策略必须实现的方法。2）ConcreteStrategy（具体策略）：实现具体的算法或行为。3）Context（上下文）：持有一个策略引用，在运行时可以切换策略。运行时切换策略——Context 暴露 setStrategy() 方法，客户端根据条件选择策略并注入。示例：支付系统——策略接口 PayStrategy.processPayment(amount)。具体策略：AlipayStrategy、WechatPayStrategy、CreditCardStrategy。Context（OrderService）保存策略引用，用户选择支付方式时调用 setStrategy(payStrategy) 切换，结算时调用 strategy.processPayment(total)。\n\n扩展延伸：策略模式的典型应用——1）Java Comparator 接口：Collections.sort(list, comparator) 传入不同比较器即不同排序策略。2）Spring ResourceLoader：根据资源类型（classpath:/、file:/、http://）使用不同策略加载资源。3）线程池拒绝策略：AbortPolicy、DiscardPolicy、CallerRunsPolicy 等都是策略模式的体现。与状态模式的区别：策略之间互相独立，没有状态流转（状态模式的状态之间有因果关系）。优点：消除条件判断、符合开闭原则、策略可独立测试和复用。缺点：客户端需要了解不同策略的区别（违反最少知识原则）。","hints":["Comparator 接口本身就是策略模式的最佳案例","为什么策略模式和状态模式结构相似但意图完全不同"],"tags":["策略模式","Strategy Pattern","设计模式","GoF"],"id":1676},{"category":"design_patterns","difficulty":"easy","type":"short_answer","title":"模板方法模式（Template Method Pattern）","content":"什么是模板方法模式？它如何实现「好莱坞原则」（Don't call us, we'll call you）？在框架设计中为什么模板方法模式非常重要？","answer":"答案：模板方法模式在父类中定义一个操作中的算法骨架（模板方法），将一些步骤延迟到子类中实现。子类可以重新定义算法的某些特定步骤而不改变算法的结构。好莱坞原则体现了「父类调用子类」的控制反转思想。\n\n解析：模板方法模式的结构——1）AbstractClass（抽象类）：定义模板方法（通常是 final，不可重写），其中包含一系列步骤调用。步骤分为两类：抽象方法（子类必须实现）和钩子方法（Hook，子类可选覆盖）。2）ConcreteClass（具体类）：实现抽象方法，可选覆盖钩子方法来控制模板流程中的特定步骤。好莱坞原则——父类（框架）控制算法的整体流程，在适当的时候调用子类（用户代码）的方法。子类不决定何时被调用，而是被动地等待父类调用。\n\n扩展延伸：模板方法模式的经典应用——1）Spring 中的 JdbcTemplate：query() 方法定义了「获取连接->创建 Statement->执行 SQL->处理 ResultSet->关闭连接」的骨架，用户只需实现 RowMapper 回调来处理结果集。2）Servlet 中的 doGet()/doPost()：service() 模板方法处理 HTTP 请求分发，子类覆盖 doGet() 实现具体逻辑。3）Java 的 AbstractList：定义了一系列公共操作，子类只需实现 get() 和 size()。4）JUnit：TestCase 的 runBare() 定义了 setUp()->runTest()->tearDown() 的测试执行骨架。优点：代码复用、扩展方便（修改公共行为只需改父类）。缺点：限制了灵活性（子类必须遵循父类的算法骨架）。","hints":["模板方法模式中的钩子（Hook）方法有什么作用——子类如何控制父类算法的执行","JdbcTemplate 是模板方法模式还是策略模式"],"tags":["模板方法","Template Method","设计模式","好莱坞原则"],"id":1677},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"命令模式（Command Pattern）","content":"什么是命令模式？它解决了什么设计问题？命令模式如何实现操作的撤销（Undo）、重做（Redo）和队列化？","answer":"答案：命令模式将请求封装为对象，使请求的发送者（调用者）和接收者（执行者）解耦。命令对象可以参数化、队列化、持久化、支持撤销和重做。\n\n解析：命令模式的结构——1）Command（抽象命令）：声明 execute() 方法，可选 undo() 方法。2）ConcreteCommand（具体命令）：持有接收者（Receiver）的引用，在 execute() 中调用接收者的业务方法。3）Invoker（调用者）：持有命令对象，在需要时调用 command.execute()。4）Receiver（接收者）：实际执行请求的业务逻辑。撤销和重做——1）每个命令保存执行前的状态（快照或增量），undo() 方法恢复该状态。2）历史记录栈（Undo Stack）：每次 execute() 将命令压栈，撤销时弹栈并调用 undo()。3）重做（Redo）栈：撤销时从 Undo 栈弹出并压入 Redo 栈，重做时从 Redo 栈弹出并重新 execute()。\n\n扩展延伸：命令模式的经典应用——1）编辑器撤销/重做：每个编辑操作（插入字符、删除、替换格式）都是一个命令，保存执行前后的文档快照或操作记录。2）Java Runnable：将代码封装为 Runnable 对象，可提交到线程池执行（线程池是 Invoker）。3）事务回滚：数据库事务中每个写操作记录 undo 日志，回滚时依次执行 undo（Command 模式的工程实践）。4）任务队列和定时执行：将任务封装为命令对象，放入队列异步执行或定时调度。命令模式 vs 策略模式：命令模式关注「做什么」（将操作封装为对象），策略模式关注「怎么做」（将算法封装为对象）。","hints":["Undo 操作需要保存哪些信息——快照方式 vs 增量方式各有什么优劣","Java 的 Runnable 接口是命令模式吗——它缺少什么"],"tags":["命令模式","Command Pattern","设计模式","Undo/Redo"],"id":1678},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"责任链模式（Chain of Responsibility）","content":"什么是责任链模式？它如何实现请求发送者和接收者的解耦？请结合 Servlet Filter 或拦截器的例子分析。","answer":"答案：责任链模式将多个处理对象连成一条链，沿着链传递请求直到有对象处理它。每个处理对象决定自己是否处理以及是否将请求传递给下一个处理者。发送者不需要知道哪个接收者会处理请求。\n\n解析：责任链模式的结构——1）Handler（抽象处理者）：定义处理请求的接口，持有下一个处理者的引用。2）ConcreteHandler（具体处理者）：检查请求是否符合自己的处理条件，符合则处理并终止传递（或继续传递），不符合则传递给下一个。链的构建——可以是链表结构（每个 Handler 持有 next 指针），也可以是集合结构（遍历 Handler 列表逐一尝试）。两种传递模式：纯责任链（一个处理者处理后就终止），不纯责任链（请求经过每个处理者，每个处理者都执行自己的逻辑后再传递，如 Servlet Filter）。\n\n扩展延伸：经典应用——1）Servlet Filter：javax.servlet.Filter 的 doFilter(ServletRequest, ServletResponse, FilterChain) ——每个 Filter 在 doFilter() 中执行前置逻辑，调用 chain.doFilter() 交给下一个 Filter，返回时执行后置逻辑。典型的 AOP 拦截器模式。2）Spring Security Filter Chain：每个 SecurityFilter 负责一种安全检查（认证、授权、CORS、CSRF 等），全部通过后才到达 Controller。3）Java Logger：logger.trace() -> logger.debug() -> logger.info() -> logger.warn() -> logger.error()，每个级别决定是否输出。4）审批流：请假审批——直属主管 -> 部门经理 -> HR -> CEO，每个节点根据自己的权限决定审批或转交。责任链优点：降低耦合、灵活组合处理顺序。缺点：调试困难（请求最终处理者不易跟踪）、过长的链影响性能。","hints":["Servlet Filter 与责任链模式的关系——doFilter() 调用 chain.doFilter() 的执行流程","纯责任链和不纯责任链的区别——Spring Security Filter 属于哪一种"],"tags":["责任链模式","Chain of Responsibility","设计模式","Filter"],"id":1679},{"category":"design_patterns","difficulty":"easy","type":"short_answer","title":"空对象模式","content":"请解释空对象模式（Null Object Pattern）的用途和实现方式。它如何避免空指针异常？与 Optional 的区别是什么？","answer":"答案：空对象模式用一个不做任何操作或返回默认值的对象替代 null 引用，避免调用方每次都要做 null 检查。\n\n实现方式：定义一个接口和两个实现——真实对象（Real Object）和空对象（Null Object）。空对象实现接口的所有方法但行为是\"空操作\"或返回默认值。\n\n示例：interface Logger { void log(String msg); }\nclass FileLogger implements Logger { void log(String msg) { writeToFile(msg); } }\nclass NullLogger implements Logger { void log(String msg) { /* no-op */ } }\n\n与 Optional 的区别：\n1. 设计意图不同：空对象提供默认行为（\"什么都不做\"），Optional 表示值可能缺失\n2. 使用场景不同：空对象适用于策略/行为（日志、缓存、通知），Optional 适用于数据返回值\n3. 空对象是结构型模式，Optional 是容器类型\n\n扩展延伸：空对象模式在 Java 标准库中的应用：java.io.InputStream.nullInputStream()、Console.nullWriter()。Reactive 编程中的 Mono.empty() 和 Flux.empty() 也类似。空对象在日志框架（Log4j 的 NullAppender）、缓存（通读缓存的 NullCache）中常见。","hints":["空对象模式用\"什么都不做的对象\"替代 null 引用","Optional 表示值可能缺失，空对象提供默认行为"],"tags":["设计模式","空对象","Null Object"],"id":1690},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"责任链模式","content":"请介绍责任链模式（Chain of Responsibility）。责任链如何解耦请求的发送者和接收者？在 Java Servlet Filter 和 Netty Pipeline 中责任链是如何实现的？","answer":"答案：责任链将多个处理对象组成链，请求沿着链传递直到被处理。\n\n核心思想：\n- 多个处理者（Handler）各自决定是否能处理请求\n- 每个处理者持有下一个处理者的引用\n- 请求沿着链传递（直到某一步处理完或到达链的末端）\n- 发送者不需要知道哪个处理者会处理请求（解耦）\n\n实现方式：\n1. 纯责任链：请求被某个处理者处理后不再继续传递（如审批流程——找到对级别的审批人）\n2. 不纯责任链：请求经过每个处理者，每个处理者可以决定处理部分逻辑（如中间件——日志→认证→缓存→路由）\n\nJava Servlet Filter：\n- FilterChain 传入 Filter 列表\n- 每个 Filter 决定是否调用 chain.doFilter()（继续链）\n- 如果不调用 doFilter()，请求被拦截（权限校验失败）\n- 典型顺序：CharacterEncodingFilter → SecurityFilter → CacheFilter → Servlet\n\nNetty Pipeline（更复杂）：\n- ChannelPipeline 包含一系列 ChannelHandler（入站和出站）\n- 入站：从网络读取数据 → 解码器 → 业务 Handler\n- 出站：编码器 → 写入网络\n- 每个 Handler 通过 ctx.fireChannelRead() 传递到下一个 Handler\n\n优点：\n- 解耦发送者和接收者\n- 灵活性：链的顺序可以动态调整\n- 可以在链中动态增加/删除处理者\n- 符合开闭原则（增加新处理者不需要修改现有代码）\n\n缺点：\n- 调试困难（请求在多个处理者之间传递）\n- 性能开销（请求经过多个处理者）\n- 可能导致循环引用\n\n扩展延伸：中间件（Middleware）是责任链的变体——Express.js/Koa 的中间件、Redux middleware、FastAPI 中间件。Spring Security 的 Filter Chain——认证过滤器链，每个过滤器检查一种认证方式。责任链与装饰器模式的区别——装饰器为对象增加功能，责任链决定请求由谁处理。","hints":["责任链 = 请求沿处理器链传递，直到被处理或链结束。解耦发送者和接收者","Servlet Filter（chain.doFilter 继续/阻断链）→ Netty Pipeline（入站+出站 Handler + ctx.fireChannelRead）"],"tags":["设计模式","责任链","Filter","Pipeline"],"id":1691},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"观察者模式与发布订阅","content":"请介绍观察者模式（Observer）和发布订阅模式（Pub/Sub）的区别。两者在实现消息通知和解耦方面有什么不同？Java 的 Observable 和 Spring 的 ApplicationEvent 如何使用？","answer":"答案：观察者和发布订阅都用于实现事件驱动的松耦合通信，但 Pub/Sub 更彻底。\n\n观察者模式：\n- 主题（Subject）维护观察者列表，状态变化时直接通知所有观察者\n- 主题知道有哪些观察者（有引用关系）\n- 同步通信：主题直接调用观察者的 update() 方法\n- 耦合：主题和观察者之间有直接引用（虽然是通过接口）\n- 适用于：同一进程内的通知（Java awt/Swing 事件、JavaFX 属性绑定）\n\n发布订阅模式（Pub/Sub）：\n- 发布者和订阅者之间通过事件总线/消息代理通信\n- 发布者和订阅者不知道对方的存在（完全解耦）\n- 异步通信：事件通过消息队列传递\n- 可以跨进程、跨网络通信\n- 适用于：微服务消息（Kafka/RabbitMQ）、EventBus（Guava）、领域事件\n\nJava 的 Observer/Observable（JDK 9 已弃用）：\nclass WeatherData extends Observable {\n    void setMeasurements(float temp) {\n        setChanged();  // 标记状态已变化\n        notifyObservers(temp);  // 通知所有观察者\n    }\n}\n\nclass Display implements Observer {\n    public void update(Observable o, Object arg) {\n        // 收到通知后处理\n    }\n}\n\nSpring ApplicationEvent（观察者模式实现）：\n// 事件类\nclass OrderCreatedEvent extends ApplicationEvent {\n    Long orderId;\n}\n\n// 发布事件\napplicationEventPublisher.publishEvent(new OrderCreatedEvent(orderId));\n\n// 监听事件（@EventListener 注解）\n@Component\nclass OrderNotificationListener {\n    @EventListener\n    public void handleOrderCreated(OrderCreatedEvent event) {\n        // 异步处理（需要 @Async 注解或 @EnableAsync）\n    }\n}\n\n观察者 vs Pub/Sub 对比：\n- 耦合度：观察者（直接引用）> Pub/Sub（完全解耦）\n- 通信方式：观察者（同步）> Pub/Sub（异步，可通过 MQ 或 EventBus）\n- 错误隔离：观察者（观察者异常影响主题）> Pub/Sub（事件总线隔离）\n- 适用场景：观察者适合单进程内事件，Pub/Sub 适合分布式事件\n\n扩展延伸：响应式编程（RxJava / Reactor）——观察者模式的增强版（背压 + 操作符 + 异步）。Spring Cloud Stream——基于 Pub/Sub 的微服务消息驱动框架。Guava EventBus——Google 的进程内 Pub/Sub 实现。","hints":["观察者 = Subject→Observer 直接通知（有引用，同步，单进程内）","Pub/Sub = 发布者↔EventBus/消息队列↔订阅者（完全解耦，异步，跨进程）"],"tags":["设计模式","观察者","Pub/Sub","事件驱动"],"id":1692},{"category":"design_patterns","difficulty":"easy","type":"short_answer","title":"工厂模式家族","content":"请介绍简单工厂、工厂方法（Factory Method）和抽象工厂（Abstract Factory）三种模式的区别。什么时候使用工厂方法而不是抽象工厂？","answer":"答案：三种工厂模式逐步提升抽象层次。\n\n简单工厂（Simple Factory）：\n- 一个工厂类，一个静态方法，根据参数创建不同的产品\n- 不是 GoF 的正式模式，但最常用\n- 示例：DocumentFactory.createDocument(\"PDF\") → PDFDocument / WordDocument\n- 缺点：添加新产品需要修改工厂类（违反开闭原则）\n\n工厂方法（Factory Method）：\n- 定义一个创建对象的接口，让子类决定实例化哪个类\n- Creator（抽象的工厂类）→ ConcreteCreator（具体的工厂子类）→ Product（产品）\n- 每个产品对应一个工厂（每个工厂只创建一种产品）\n- 添加新产品只需要增加新的工厂子类（符合开闭原则）\n- 示例：\n  interface DocumentFactory { Document create(); }\n  class PDFFactory implements DocumentFactory { public Document create() { return new PDFDocument(); } }\n  class WordFactory implements DocumentFactory { public Document create() { return new WordDocument(); } }\n\n抽象工厂（Abstract Factory）：\n- 创建一组相关的产品（产品族），不需指定具体类\n- 一个工厂可以创建多种产品（同一产品族的所有产品）\n- 示例（GUI 跨平台）：\n  interface GUIFactory { Button createButton(); TextField createTextField(); }\n  class WinFactory implements GUIFactory { Button createButton() { return new WinButton(); } TextField createTextField() { return new WinTextField(); } }\n  class MacFactory implements GUIFactory { Button createButton() { return new MacButton(); } TextField createTextField() { return new MacTextField(); } }\n\n工厂方法 vs 抽象工厂：\n- 工厂方法：一个工厂创建一种产品（继承层次）\n- 抽象工厂：一个工厂创建一整套产品（产品族）\n- 工厂方法使用继承（子类重写工厂方法）\n- 抽象工厂使用组合（通过参数选择工厂实现）\n- 选择：如果需要创建的产品总是成组出现（如不同主题的 UI 控件），用抽象工厂\n  如果需要灵活扩展产品类型，用工厂方法\n\n扩展延伸：Spring 的 BeanFactory——IoC 容器的工厂模式实现。建造者模式（Builder）——工厂模式创建对象后，Builder 模式进一步设置对象的复杂属性。依赖注入（DI）——工厂模式的进化版（不用自己写工厂，由 IoC 容器负责）。","hints":["简单工厂（一个类，根据参数创建不同产品）→ 工厂方法（每种产品一个工厂子类）→ 抽象工厂（创建一整套产品族）","工厂方法适合单产品扩展，抽象工厂适合产品族（同一主题的成套产品）"],"tags":["设计模式","工厂","抽象工厂","创建型"],"id":1693},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"策略模式","content":"请介绍策略模式（Strategy）。策略模式如何替代复杂条件语句（if-else/switch）？策略模式和状态模式（State）的区别是什么？","answer":"答案：策略模式将算法封装为独立的策略类，使它们可以互相替换。\n\n核心思想：\n- 定义策略接口（Strategy），所有具体策略实现该接口\n- 上下文（Context）持有策略引用，通过组合委托给策略\n- 客户端选择合适的策略注入到上下文中\n- 算法独立于客户端变化（开闭原则：添加新策略不需要修改现有代码）\n\n传统 if-else 的缺点：\nif (paymentType == \"CREDIT_CARD\") {\n    // 信用卡支付逻辑\n} else if (paymentType == \"ALIPAY\") {\n    // 支付宝逻辑\n} else if (paymentType == \"WECHAT\") {\n    // 微信支付逻辑\n}\n// 添加新支付方式需要修改这个条件语句\n\n策略模式实现：\n// 策略接口\ninterface PaymentStrategy {\n    void pay(double amount);\n}\n\n// 具体策略\nclass CreditCardStrategy implements PaymentStrategy { ... }\nclass AlipayStrategy implements PaymentStrategy { ... }\nclass WechatStrategy implements PaymentStrategy { ... }\n\n// 上下文\nclass PaymentContext {\n    private PaymentStrategy strategy;\n    void setStrategy(PaymentStrategy strategy) { this.strategy = strategy; }\n    void executePayment(double amount) { strategy.pay(amount); }\n}\n\n// 使用\nPaymentContext context = new PaymentContext();\ncontext.setStrategy(new AlipayStrategy());\ncontext.executePayment(100.0);\n\n策略模式 vs 状态模式：\n- 策略模式：客户端选择策略（外部决定），策略之间独立无关联\n- 状态模式：状态自动切换（内部决定），状态转换有固定的规则\n- 策略模式：解决\"怎么做\"（算法替换）\n- 状态模式：解决\"是什么状态的表现\"（行为随状态变化）\n- 类比：策略像选择交通工具（客户端选），状态像红绿灯（自动转换）\n\n实际应用：\n- Java Comparator（排序策略）\n- Spring Security 的 AuthenticationProvider（认证策略）\n- 支付系统（多种支付方式）\n- 促销活动（多种折扣策略）\n\n扩展延伸：策略模式的简化——函数式编程中策略就是高阶函数（Java 8 的 lambda + EnumMap<Type, Strategy> 消除多态开销）。Null Object 模式（空对象模式）——策略模式的一种特殊变体（提供什么都不做的默认策略）。","hints":["策略 = 算法封装成独立策略类 + 上下文通过组合委托给策略（运行时替换）","策略（客户端选，算法替换）vs 状态（内部自动转换，行为随状态变化）"],"tags":["设计模式","策略","行为型","状态"],"id":1694},{"category":"design_patterns","difficulty":"easy","type":"short_answer","title":"适配器模式","content":"请介绍适配器模式（Adapter）。适配器如何让不兼容的接口协同工作？类适配器和对象适配器的区别？适配器、代理和装饰器模式有什么区别？","answer":"答案：适配器模式将一个接口转换成客户端期望的另一个接口。\n\n适配器的作用：\n- 客户端使用 Target 接口\n- Adaptee 是现有类（接口不兼容）\n- Adapter 实现 Target 接口，内部调用 Adaptee 的方法\n\n对象适配器（基于组合，推荐）：\nclass MediaPlayerAdapter implements MediaPlayer {\n    private AdvancedMediaPlayer advancedPlayer;\n\n    public MediaPlayerAdapter(AdvancedMediaPlayer player) {\n        this.advancedPlayer = player;\n    }\n\n    @Override\n    public void play(String fileName) {\n        // 将简单的 play 接口转换为高级播放器的接口\n        advancedPlayer.playAdvanced(fileName);\n    }\n}\n\n类适配器（基于继承）：\nclass MediaPlayerAdapter extends AdvancedMediaPlayer implements MediaPlayer {\n    @Override\n    public void play(String fileName) {\n        playAdvanced(fileName);  // 直接调用继承的方法\n    }\n}\n\n适配器 vs 代理 vs 装饰器：\n- 适配器（Adapter）：转换接口（解决接口不兼容）\n  示例：将 USB-C 接口转换为 HDMI 接口\n- 代理（Proxy）：控制对对象的访问（延迟加载/权限/远程代理）\n  示例：代理控制谁可以访问目标对象\n- 装饰器（Decorator）：动态增加功能（不改变接口）\n  示例：给咖啡加糖/加奶（同类接口，增加功能）\n\n关键区别：\n- 适配器改变接口（让 Target 和 Adaptee 可以一起工作）\n- 代理不改变接口（控制访问）\n- 装饰器不改变接口（增强功能）\n\n实际应用：\n- 兼容旧系统（Legacy System）：包装旧系统的接口为新系统可用\n- Java I/O：InputStreamReader 适配 InputStream 到 Reader\n- Spring 的 HandlerAdapter：将不同的 Controller 类型适配到统一的 DispatcherServlet 接口\n- SLF4J：日志框架适配器（统一接口适配 Log4j/Logback/JUL）\n\n扩展延伸：双向适配器——让两个接口互相适配（实现两个接口，互相调用）。缺省适配器（Default Adapter）——提供接口的默认空实现，子类只需重写需要的方法（JDK 8 中的 default 方法解决了这个问题）。外观模式（Facade）与适配器的区别——外观提供简化的接口（面向子系统），适配器解决接口不兼容。","hints":["适配器 = 转换接口（Target←Adapter→Adaptee），解决接口不兼容","适配器（改接口，兼容旧系统）vs 代理（不改接口，控制访问）vs 装饰器（不改接口，增强功能）"],"tags":["设计模式","适配器","结构型","兼容性"],"id":1695},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"单例模式的多种实现方式","content":"请介绍单例模式（Singleton）的多种实现方式及其优缺点。","answer":"答案：单例模式确保一个类只有一个实例并提供全局访问点。\n\n饿汉式（Eager Initialization）：\n\n实现：\n```java\npublic class Singleton {\n    private static final Singleton INSTANCE = new Singleton();\n    private Singleton() {}\n    public static Singleton getInstance() { return INSTANCE; }\n}\n```\n优点：简单、线程安全\n缺点：类加载时就创建，可能造成资源浪费\n\n懒汉式（Lazy Initialization）：\n\n实现：\n```java\npublic class Singleton {\n    private static Singleton instance;\n    private Singleton() {}\n    public static synchronized Singleton getInstance() {\n        if (instance == null) instance = new Singleton();\n        return instance;\n    }\n}\n```\n优点：延迟创建\n缺点：synchronized 方法造成性能开销\n\n双重检查锁定（Double-Checked Locking）：\n\n实现：\n```java\npublic class Singleton {\n    private static volatile Singleton instance;\n    private Singleton() {}\n    public static Singleton getInstance() {\n        if (instance == null) {\n            synchronized (Singleton.class) {\n                if (instance == null) instance = new Singleton();\n            }\n        }\n        return instance;\n    }\n}\n```\n关键：volatile 禁止指令重排序（new 对象不是原子操作）\n\n静态内部类（Bill Pugh）：\n\n```java\npublic class Singleton {\n    private Singleton() {}\n    private static class Holder {\n        static final Singleton INSTANCE = new Singleton();\n    }\n    public static Singleton getInstance() { return Holder.INSTANCE; }\n}\n```\n优点：延迟加载（Holder 类在 getInstance 时才加载）、线程安全\n\n枚举实现：\n\n```java\npublic enum Singleton {\n    INSTANCE;\n    // 添加方法\n}\n```\n最安全的方式：防止反射攻击、序列化安全、天然单例\n\n对比：\n| 方式 | 延迟加载 | 线程安全 | 防反射 | 序列化安全 |\n|------|----------|----------|--------|------------|\n| 饿汉式 | 否 | 是 | 否 | 否 |\n| 懒汉式 | 是 | 是（锁）| 否 | 否 |\n| DCL | 是 | 是（volatile）| 否 | 否 |\n| 静态内部类 | 是 | 是 | 否 | 否 |\n| 枚举 | 否 | 是 | 是 | 是 |\n\n扩展延伸：现代框架中单例模式被依赖注入容器（Spring IoC）管理，无需手动实现单例。Spring 默认 Bean 的作用域就是 singleton。框架层面的单例管理更灵活（可以随时切换为 prototype 作用域）。","hints":["五种实现：饿汉式（简单但无延迟）、懒汉式（有延迟但需同步）、DCL（volatile 防重排）","最推荐：静态内部类（延迟+线程安全）和枚举（防反射+序列化安全）","框架层面：Spring IoC 默认单例，比手动单例更灵活"],"tags":["设计模式","单例模式","Java","多线程"],"id":1696},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"工厂模式系列（简单/工厂/抽象工厂）","content":"请介绍简单工厂、工厂方法和抽象工厂三种模式的区别和适用场景。","answer":"答案：工厂模式是一组创建对象的模式，核心是\"将对象的创建和使用分离\"。\n\n简单工厂（Simple Factory）：\n\n结构：\n- 一个工厂类，根据参数创建不同产品\n- 产品类都实现同一个接口\n\n```java\npublic class SimpleFactory {\n    public Product create(String type) {\n        if (type.equals(\"A\")) return new ProductA();\n        if (type.equals(\"B\")) return new ProductB();\n        throw new IllegalArgumentException();\n    }\n}\n```\n优点：简单、客户端无需知道产品类的创建细节\n缺点：添加新产品需修改工厂类（违反开闭原则）\n\n工厂方法（Factory Method）：\n\n结构：\n- 抽象工厂类定义创建方法（但不实现）\n- 子类决定实例化哪个具体产品\n\n```java\npublic abstract class Factory {\n    public abstract Product createProduct();\n}\npublic class FactoryA extends Factory {\n    public Product createProduct() { return new ProductA(); }\n}\n```\n优点：符合开闭原则（添加新产品只需新增子类）\n缺点：每增加一个产品就需要增加一个工厂子类\n\n抽象工厂（Abstract Factory）：\n\n结构：\n- 一个工厂创建一组相关产品（产品族）\n- 客户端使用同一个工厂创建整组产品\n\n```java\npublic interface GUIFactory {\n    Button createButton();\n    TextField createTextField();\n}\npublic class WinFactory implements GUIFactory {\n    public Button createButton() { return new WinButton(); }\n    public TextField createTextField() { return new WinTextField(); }\n}\n```\n优点：确保产品族的兼容性\n缺点：扩展产品族困难（新增产品类型需修改所有工厂）\n\n对比：\n| 模式 | 目的 | 扩展方式 | 复杂度 |\n|------|------|----------|--------|\n| 简单工厂 | 集中创建逻辑 | 修改工厂类 | 低 |\n| 工厂方法 | 子类决定产品类型 | 新增子类 | 中 |\n| 抽象工厂 | 创建整组相关产品 | 新增实现 | 高 |\n\n扩展延伸：Spring 中 BeanFactory 是工厂方法模式的经典应用。MyBatis 的 SqlSessionFactory 也是工厂方法。实际工程中建议从简单工厂开始，当需要扩展时再重构为工厂方法或抽象工厂。","hints":["简单工厂：一个方法根据参数创建不同产品（违反开闭原则，适合简单场景）","工厂方法：抽象工厂类 + 子类决定具体产品（符合开闭原则，一对一的工厂层次）","抽象工厂：创建一组兼容产品（产品族），如跨平台 UI 组件"],"tags":["设计模式","工厂模式","抽象工厂","创建型"],"id":1697},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"观察者模式与发布订阅模式","content":"请介绍观察者模式（Observer）和发布订阅模式（Pub/Sub）的区别及各自的应用场景。","answer":"答案：两者都是事件驱动的设计模式，但在耦合度和通信方式上有本质区别。\n\n观察者模式（Observer）：\n\n结构：\n- Subject（主题）：维护观察者列表，通知状态变化\n- Observer（观察者）：接收主题的通知\n- 观察者直接注册到主题上\n\n```java\n// Subject\ninterface Subject {\n    void registerObserver(Observer o);\n    void removeObserver(Observer o);\n    void notifyObservers();\n}\n// Observer\ninterface Observer {\n    void update(String data);\n}\n```\n\n特点：\n- 主题和观察者直接耦合（主题知道观察者的存在）\n- 通信是同步的（主题通知时观察者立即响应）\n- 实现简单\n- 扩展困难（新增观察者类型需要修改主题）\n\n发布订阅模式（Pub/Sub）：\n\n结构：\n- Publisher（发布者）：发布事件到消息通道\n- Subscriber（订阅者）：从消息通道订阅事件\n- Broker/EventBus（事件总线）：解耦发布者和订阅者\n\n```javascript\n// Event Bus\nclass EventBus {\n    constructor() { this.subscribers = {}; }\n    subscribe(event, callback) {\n        if (!this.subscribers[event]) this.subscribers[event] = [];\n        this.subscribers[event].push(callback);\n    }\n    publish(event, data) {\n        this.subscribers[event]?.forEach(cb => cb(data));\n    }\n}\n```\n\n特点：\n- 发布者和订阅者完全解耦（通过 Broker）\n- 可以异步通信（消息队列）\n- 可扩展性强\n- 调试困难（事件流难以追踪）\n\n对比：\n| 维度 | 观察者模式 | 发布订阅模式 |\n|------|-----------|-------------|\n| 耦合度 | 高（主题知观察者） | 低（通过 Broker） |\n| 通信方式 | 同步 | 同步或异步 |\n| 实现复杂度 | 低 | 高 |\n| 调试容易度 | 容易 | 较难 |\n| 典型场景 | GUI 事件监听 | 微服务间消息通信 |\n\n实际应用：\n- 观察者模式：Java AWT/Swing 事件监听、React 的 props 变化通知\n- 发布订阅模式：消息队列（Kafka/RabbitMQ）、EventBus（Guava/GreenRobot）、Redis Pub/Sub\n\n扩展延伸：响应式编程（RxJS）结合了两种模式——Observable 是发布者，Observer 是订阅者，通过操作符链（pipe）实现数据流的变换。Vue 的响应式系统也是观察者模式的变体（Proxy 监听数据变化，通知依赖更新）。","hints":["观察者模式：主题直接通知观察者（高耦合、同步、简单）——GUI 事件监听","发布订阅模式：通过 Broker 解耦发布者和订阅者（松耦合、可异步）——消息队列","响应式编程（RxJS）是两者的结合：Observable + Observer + 操作符链"],"tags":["设计模式","观察者模式","发布订阅","事件驱动"],"id":1698},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"策略模式与状态模式","content":"请介绍策略模式（Strategy）和状态模式（State）的区别及各自的应用场景。","answer":"答案：策略模式和状态模式结构相似（都使用组合替代继承），但意图完全不同。\n\n策略模式（Strategy）：\n\n意图：定义一系列算法，使它们可以互相替换。\n\n结构：\n- Context（上下文）：持有策略引用，委托策略执行算法\n- Strategy（策略接口）：定义算法接口\n- ConcreteStrategy（具体策略）：实现具体算法\n\n```java\n// 策略接口\ninterface PaymentStrategy {\n    void pay(int amount);\n}\n// 具体策略\nclass CreditCardPayment implements PaymentStrategy {\n    public void pay(int amount) {\n        // 信用卡支付逻辑\n    }\n}\nclass WechatPayment implements PaymentStrategy {\n    public void pay(int amount) {\n        // 微信支付逻辑\n    }\n}\n// 上下文\nclass Order {\n    private PaymentStrategy strategy;\n    public void setStrategy(PaymentStrategy strategy) {\n        this.strategy = strategy;\n    }\n    public void checkout(int amount) {\n        strategy.pay(amount);\n    }\n}\n```\n\n适用：\n- 同一行为有多种实现方式\n- 需要避免 if-else 或 switch 分支\n- 策略在运行时切换（由客户端决定）\n\n状态模式（State）：\n\n意图：允许对象在内部状态变化时改变其行为。\n\n结构：\n- Context（上下文）：持有状态引用，委托状态处理行为\n- State（状态接口）：定义状态相关的行为\n- ConcreteState（具体状态）：实现状态特定的行为\n\n```java\n// 状态接口\ninterface OrderState {\n    void pay();\n    void ship();\n    void cancel();\n}\n// 具体状态\nclass PaidState implements OrderState {\n    public void pay() { throw new IllegalStateException(\"已支付\"); }\n    public void ship() { /* 发货逻辑，切换到 ShippedState */ }\n    public void cancel() { /* 取消逻辑，切换到 CancelledState */ }\n}\n// 上下文\nclass Order {\n    private OrderState state = new PendingState();\n    public void pay() { state.pay(); }\n    public void ship() { state.ship(); }\n}\n```\n\n适用：\n- 对象的行为随状态变化\n- 有多个状态的转换逻辑\n- 状态由对象内部自动转换（而非客户端控制）\n\n核心区别：\n| 方面 | 策略模式 | 状态模式 |\n|------|----------|----------|\n| 意图 | 可替换的算法 | 随状态变化的行为 |\n| 谁决定切换 | 客户端 | 状态内部自动转换 |\n| 数量 | 通常固定 | 按状态流转 |\n| 关系 | 并列关系 | 转换关系 |\n\n扩展延伸：在工程实践中，状态模式经常和策略模式结合使用。Spring StateMachine（状态机框架）结合了两种模式——每个状态定义了该状态下可执行的策略，状态转换由 DSL 配置驱动。","hints":["策略模式：客户端选择算法（并列可替换，如支付方式选择）","状态模式：状态内部驱动转换（流转不可选，如订单状态机）","核心区别：策略由客户端选，状态自动转换"],"tags":["设计模式","策略模式","状态模式","行为型"],"id":1699},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"代理模式与装饰器模式","content":"请介绍代理模式（Proxy）和装饰器模式（Decorator）的区别及其应用场景。","answer":"答案：代理模式和装饰器模式结构相似（都包装了目标对象），但目的不同。\n\n代理模式（Proxy）：\n\n意图：控制对目标对象的访问。\n\n结构：\n- Subject（抽象接口）：定义代理和真实对象的共同接口\n- RealSubject（真实对象）：实际执行业务的对象\n- Proxy（代理）：持有 RealSubject 引用，控制对其的访问\n\n```java\ninterface Image {\n    void display();\n}\nclass RealImage implements Image {\n    private String filename;\n    public RealImage(String filename) {\n        this.filename = filename;\n        loadFromDisk();  // 昂贵的操作\n    }\n    public void display() { /* 显示图片 */ }\n}\nclass ImageProxy implements Image {\n    private RealImage realImage;\n    private String filename;\n    public ImageProxy(String filename) { this.filename = filename; }\n    public void display() {\n        if (realImage == null) realImage = new RealImage(filename);\n        realImage.display();\n    }\n}\n```\n\n常见类型：\n1. 虚拟代理（Virtual Proxy）：延迟加载（图片懒加载）\n2. 保护代理（Protection Proxy）：权限控制\n3. 远程代理（Remote Proxy）：RPC/RMI\n4. 智能引用代理（Smart Reference）：引用计数、日志\n\n装饰器模式（Decorator）：\n\n意图：动态为对象添加功能，而不影响其他对象。\n\n结构：\n- Component（组件接口）：定义对象的接口\n- ConcreteComponent（具体组件）：基础对象\n- Decorator（装饰器）：持有一个 Component 引用，实现相同接口\n- ConcreteDecorator（具体装饰器）：添加具体功能\n\n```java\ninterface Coffee {\n    double cost();\n    String description();\n}\nclass SimpleCoffee implements Coffee {\n    public double cost() { return 10; }\n    public String description() { return \"咖啡\"; }\n}\nabstract class CoffeeDecorator implements Coffee {\n    protected Coffee coffee;\n    public CoffeeDecorator(Coffee coffee) { this.coffee = coffee; }\n}\nclass MilkDecorator extends CoffeeDecorator {\n    public double cost() { return coffee.cost() + 3; }\n    public String description() { return coffee.description() + \" + 牛奶\"; }\n}\n```\n\n核心区别：\n| 维度 | 代理模式 | 装饰器模式 |\n|------|----------|------------|\n| 目的 | 控制访问（限制、增强、延迟） | 添加功能（扩展行为） |\n| 创建时机 | 代理在编译时确定 | 装饰器在运行时动态组合 |\n| 关系 | 代理和真实对象的关系固定 | 装饰器可以多层嵌套 |\n| 客户端感知 | 客户端不知道代理的存在 | 客户端知道（需要传入被装饰对象） |\n\n扩展延伸：Spring AOP 的 JDK 动态代理和 CGLIB 代理是代理模式的经典应用。Java I/O 流是装饰器模式的经典应用（BufferedInputStream -> FileInputStream 的多层嵌套）。实际工程中代理模式负责横切关注点（日志、事务、安全），装饰器负责纵向功能扩展（缓存、校验、格式化）。","hints":["代理模式：控制访问——延迟加载、权限控制、远程代理（编译时确定，客户端无感知）","装饰器模式：动态扩展功能——多层嵌套组合（运行时组合，客户端知道在装饰）","经典应用：代理=Spring AOP，装饰器=Java I/O 流（BufferedInputStream 包装 FileInputStream）"],"tags":["设计模式","代理模式","装饰器模式","结构型"],"id":1700},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"适配器模式与外观模式","content":"请对比适配器模式（Adapter）和外观模式（Facade）的区别。","answer":"答案：适配器解决接口不兼容问题，外观为复杂子系统提供简化接口\n\n解析：适配器模式（Adapter）将一个类的接口转换成客户端期望的另一个接口，解决已有类和客户端接口之间的兼容性问题。适配器持有被适配者的引用，将客户端的调用转换为被适配者的接口调用（对象适配器）或继承被适配者（类适配器）。外观模式（Facade）为子系统的一组接口提供一个统一的高层接口，隐藏子系统的复杂性。外观不改变子系统的接口，只是组合了多个调用。结构上适配器一般包装单个类/接口，外观聚合多个类的方法调用。意图上适配器是接口转换，外观是接口简化。\n\n扩展延伸：适配器 vs 代理 vs 装饰器：适配器转换接口，代理控制访问，装饰器增强功能。在代码中适配器常见的案例：1）封装第三方库的接口使其符合系统的接口规范 2）适配老版本 API 与新版本接口。外观模式典型案例：1）Spring 的 JdbcTemplate（封装了 JDBC 的 connection/statement/resultset 等复杂操作）2）SLF4J（为各种日志框架提供统一外观）3）各种 API 网关模式本质上也是外观模式。两种模式的变体：双向适配器支持两个方向接口转换；可插拔适配器在运行时动态改变适配行为。注意：过度使用外观可能导致外观类变成上帝类（God Class），承担过多职责。","hints":["适配器和外观模式最本质的意图差异是什么","外观模式与中介者模式在某些情况下怎么区分"],"tags":["设计模式","Adapter","Facade"],"id":1701},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"责任链模式与请求处理","content":"请解释责任链模式（Chain of Responsibility）及其在框架中的应用。","answer":"答案：责任链将请求沿着处理器链传递，每个处理器决定处理请求或传递给下一个\n\n解析：核心结构：抽象处理器定义 handle 方法和 successor（下一个处理器引用），具体处理器实现自己的处理逻辑，如果无法处理则调用 successor.handle()。可以纯责任链（每个处理器要么处理要么传递，但不能同时）或者拦截器链（每个处理器既能处理又能决定是否继续传递）。构建链可以用链表（手动设置 successor）或用列表（遍历列表依次调用）。典型应用：1）Java Servlet Filter（doFilter 方法决定是否调用 chain.doFilter 继续过滤链）2）Spring Security 的 Filter Chain 3）Netty 的 Pipeline（ChannelHandler 链式处理入站出站事件）4）中间件框架（Express/Koa 中间件，AOP 拦截器 5）审批流（不同级别的审批人依次处理报销单）。\n\n扩展延伸：责任链模式与装饰器模式的异同：两者都有链式结构，但装饰器重在对对象功能的增强和叠加，责任链重在解耦请求的发送者和接收者。纯责任链 vs 拦截器链：纯责任链中一个节点处理完请求后不再传递，拦截器链每个节点都做部分处理（如日志->认证->业务处理）。动态构建链：通过注解和配置驱动（Spring 中拦截器顺序的配置）。责任链在分布式日志中的使用：Logback 的 Appender 链（Appender 可以附加到另一个 Appender 上）。性能注意：太长的链可能影响请求处理时间，在需要快速响应的业务场景减少链长度或提前中断。CompletableFuture 的 thenApply 链在语义上类似于责任链但本质不同（是流式计算的串联而非请求处理链）。","hints":["责任链模式和装饰器模式的结构差异与意图差异","纯责任链和拦截器链两种变体的区别"],"tags":["设计模式","责任链","过滤器"],"id":1702},{"category":"design_patterns","difficulty":"medium","type":"choice","title":"设计模式分类","content":"以下哪种设计模式属于行为型模式（Behavioral Pattern）？","options":["A) 工厂方法（Factory Method）","B) 桥接模式（Bridge）","C) 备忘录模式（Memento）","D) 享元模式（Flyweight）"],"answer":"C","hints":["工厂模式是创建型，桥接和享元是结构型，备忘录是行为型"],"tags":["设计模式","分类","GoF"],"id":1703},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"状态模式与策略模式","content":"请深入对比状态模式（State）和策略模式（Strategy）的实现异同。","answer":"答案：两者结构相似，关键区别在于状态模式中状态之间可切换，策略模式中策略由客户端选择固定不变\n\n解析：结构上两者都使用组合：Context 持有抽象状态/策略的引用，具体状态/策略实现接口。区别在于意图和调用方：策略模式中客户端主动选择策略并设置到 Context 中（客户端主导），策略对象通常不会主动切换 Context 中的策略。状态模式中状态对象自己控制 Context 中的状态切换（状态主导），Context 的行为随内部状态变化而变化，客户端不关心状态切换逻辑。在策略模式中策略是独立的算法族（如排序策略、压缩策略），在状态模式中状态是对象可能的内部条件（如订单状态：新建->已支付->已发货->已完成）。\n\n扩展延伸：状态模式的适用场景：1）对象的行为依赖其状态且状态的转换逻辑复杂（比如 TCP 连接的状态机）2）状态转换的条件表达式遍布在业务代码中（用状态模式集中管理）。策略模式的经典案例：1）Collections.sort 的比较器 2）Spring 的 ResourceLoader（按资源类型选择不同实现）3）Spring Security 的 AuthenticationProvider。两种模式与有限状态机（FSM）的关系：状态模式是实现 FSM 的常见 OOP 方式。在 Spring State Machine 中，状态和事件驱动的转换由框架管理。性能注意事项：两种模式都会增加类数量，状态模式中状态对象可以共享（不包含内部状态的情况下用单例，如 OrderState 状态是轻量的无状态对象）。","hints":["策略模式和状态模式最核心的区别在于谁负责切换","上下文在两种模式中对策略/状态的生命周期控制差异"],"tags":["设计模式","State","Strategy"],"id":1704},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"桥接模式与实现分离","content":"请解释桥接模式（Bridge）如何实现抽象部分和实现部分的解耦。","answer":"答案：桥接模式将抽象和实现放在不同的类层次中，使两者可以独立变化\n\n解析：桥接的核心是使用组合代替继承：抽象类持有实现接口的引用，将具体实现的调用委托给实现类。传统继承会为每个抽象-实现的组合创建大量子类（如 4 种形状 × 3 种颜色 = 12 个子类），桥接模式通过组合将乘法组合变为加法（4 个形状 + 3 个颜色 = 7 个类）。桥接是封装变化的经典原则变体——将变化的维度分离到不同的类层次中，通过组合桥接起来。\n\n扩展延伸：桥接模式在 JDK 中的应用：JDBC 驱动架构——DriverManager（抽象部分）和具体的数据库驱动（实现部分），应用程序通过 JDBC 接口操作数据库，具体实现根据加载的驱动自动桥接到相应的数据库（MySQL/Oracle/PostgreSQL）。类似案例：SLF4J 日志门面 + Logback/Log4j 实现。适配器模式 vs 桥接模式：适配器让不兼容的类一起工作（事后补救），桥接模式在设计中就考虑抽象和实现的分离（事先设计）。抽象和实现的独立性：抽象可以有自己的扩展层次（增加新的抽象子类），实现也可以有自己的扩展（增加新的具体实现）——两者通过桥接引用连接而不是继承耦合。Java AWT 的 Peer 架构也是桥接模式的经典案例。","hints":["桥接相比多层继承解决了什么设计问题","JDBC 中 DriverManager 和具体 Driver 的关系是桥接模式吗"],"tags":["设计模式","Bridge","解耦"],"id":1705},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"策略模式 vs 状态模式的结构性差异","content":"策略模式（Strategy）和状态模式（State）在 GoF 分类中都属于行为型模式，且它们的 UML 类图结构非常相似——Context 持有抽象接口引用，具体实现类实现该接口。请深入分析这两种模式在结构上的本质差异，特别是状态转换的控制权归属、策略/状态对象之间的关系，以及这些差异在实际编码中如何判定。","answer":"答案：策略模式和状态模式在类图结构上几乎一致（都是 Context 持有抽象接口引用，通过组合委托行为），但它们在控制权归属和对象关系上存在本质差异。策略模式由客户端主动选择和切换策略（外部驱动），策略之间彼此独立、互不感知，Context 的行为接口固定、仅实现方式不同。状态模式的状态切换由状态对象自身或 Context 内部管理（内部驱动），状态之间有固定的流转规则，Context 的行为随状态变化而自动变化。\n\n解析：\n\n结构相似性的来源——两种模式都遵循「用组合替代继承」的 GoF 原则。Context 中持有抽象接口字段，所有具体实现类实现该接口。Context 的方法委托给当前持有的对象执行。这导致仅看类图难以区分。\n\n核心差异一：控制权归属。策略模式——客户端（Client）调用 context.setStrategy(strategy) 主动选择和切换策略。策略对象不知道自己何时被切换，也不关心其他策略的存在。例如支付系统中，客户端根据用户选择的支付方式（支付宝/微信/银行卡）动态切换到对应策略。状态模式——状态转换由状态对象自身（在 handle() 方法末尾调用 context.setState(new NextState())）或 Context 内部管理。客户端触发一个操作（如 order.pay()）但无法绕过状态机的流转规则直接切换状态。例如 TCP 连接中 ESTABLISHED 状态收到 close 请求后自动切换到 CLOSE_WAIT。\n\n核心差异二：对象间关系。策略模式——策略之间是并列的、独立的。新增策略（如比特币支付）不需要修改任何现有策略。策略可以被多个 Context 共享（无状态策略常用单例）。状态模式——状态之间有向流转关系（有限状态机）。状态对象之间互相知道对方（StateA 知道自己可以转换到 StateB）。新增状态可能要求修改已有状态的转换逻辑（如订单新增「待审核」状态后，已支付状态需要知道新状态的转换规则）。\n\n核心差异三：Context 的角色。策略模式——Context 是策略的持有者和调用者，不关心策略何时切换、为何切换。Context 的行为接口是固定的（如 execute()），只是行为的具体实现随策略不同而变化。状态模式——Context 需要维护状态引用，行为随内部状态变化而变化。同一个业务方法（如 doAction()）在不同状态下有不同的表现和行为结果。\n\n扩展延伸：\n\n实际编码中的判定方法——如果代码中出现 setState() 且状态转换逻辑在状态实现类内部（state.handle() 中调用 context.setState(new NextState())），是状态模式。如果切换逻辑完全由客户端（调用方）的 if-else 或配置驱动，是策略模式。两者可结合：在状态模式的每个状态内部，可以使用策略模式处理该状态下的不同算法分支。\n\n从抽象层面看——策略模式体现「has-a」（有一个算法）关系，客户端决定用哪个。状态模式体现「is-a-like」关系——对象在不同状态下「像是」不同的东西。这也是为什么状态模式的目标被描述为「允许对象在内部状态改变时改变其行为，看起来像改变了类」。","hints":["从「谁调用了 setStrategy/setState」入手分析控制权归属——客户端驱动 vs 状态对象自身驱动","策略之间没有依赖关系和转换规则，状态之间有固定的流转图——这是判定两种模式的最直接依据"],"tags":["设计模式","策略模式","状态模式","GoF","行为型"],"id":1706},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"组合模式的透明性与安全性的取舍","content":"组合模式（Composite Pattern）有两种常见的设计变体——透明组合（Transparent Composite）和安全组合（Safe Composite）。请解释它们的区别、各自的优缺点和适用场景。在 Java AWT/Swing、React 组件树、文件系统 API 等实际框架中分别采用了哪种变体？","answer":"答案：透明组合在抽象 Component 接口中声明所有管理子组件的方法（add、remove、getChild），叶子节点也必须继承这些方法（通常抛出 UnsupportedOperationException）。安全组合只在 Composite 类中声明管理子组件的方法，Component 接口只包含业务方法。透明组合追求「客户端统一对待」，安全组合追求「接口隔离和编译时安全」。\n\n解析：\n\n透明组合（Transparent Composite）——Component 接口包含所有方法：业务方法（如 display()）和管理方法（add()、remove()、getChild()）。Leaf 类实现管理方法时抛出 UnsupportedOperationException 或空操作。\n\n优点：1）客户端完全不需要区分 Leaf 和 Composite，所有操作通过 Component 接口完成。2）新增节点类型不需要修改客户端代码。3）非常适合递归遍历场景（如遍历 UI 组件树统一调用 repaint()）。\n\n缺点：1）Leaf 被迫实现它不需要的方法，违反接口隔离原则（ISP）。2）客户端在编译时无法捕获对 Leaf 调用 add() 的错误（运行时才抛出异常）。3）Component 接口承担了过多职责。\n\n安全组合（Safe Composite）——Component 接口只声明业务方法（如 display()、getName()）。add()、remove()、getChild() 只在 Composite 类中定义。Leaf 类没有管理子节点的方法，对 Leaf 调用 add() 会在编译时报错。\n\n优点：1）符合接口隔离原则，Leaf 只包含它需要的方法。2）类型安全——错误在编译时即可发现。3）接口职责清晰。\n\n缺点：1）客户端需要使用 instanceof 或类型转换来区分 Leaf 和 Composite 才能调用管理方法，破坏了组合模式「统一对待」的核心价值。2）遍历操作变得更繁琐（需要 if-else 分支处理不同类型）。\n\n实际框架的选择——\n\n1）Java AWT/Swing：采用透明组合。java.awt.Component 类直接定义了 add() 和 remove() 方法。Button（叶子）可以调用 add()，虽然实际上没有效果。这使得容器（Container）和组件可以统一操作。\n\n2）React/Vue 组件树：更接近安全组合。React 中函数组件和类组件不共享相同的子节点管理 API。父组件通过 children prop 访问子节点，子组件自身没有 addChild 方法。这种设计在编译时就区分了容器组件和展示组件。\n\n3）java.nio.file.FileVisitor：采用安全组合。Files.walkFileTree() 的 FileVisitor 接口为文件和目录提供了不同的回调方法：visitFile() 处理普通文件，preVisitDirectory() 和 postVisitDirectory() 处理目录，调用者自己根据上下文区分类型。\n\n4）XML/JSON DOM 解析器：通常采用透明组合。Node 接口统一了 Element（有子节点）和 TextNode（叶子节点），appendChild() 等方法在 TextNode 上抛出 DOMException。\n\n扩展延伸：\n\n折中方案——在工程实践中，常采用以下折中：1）Component 接口包含最常用的稳定方法（如 getParent()），不常用的管理方法放在 Composite 中。2）使用 Visitor 模式来避免客户端 instanceof 判断，由 Visitor 双分派机制自动区分节点类型。3）在 Component 接口中提供默认实现（Java 8 的 default 方法）——leaf.add(child) 默认抛出异常，composite 重写此方法。\n\n选择决策树——如果系统的核心操作是遍历树并对所有节点执行统一操作（如 UI 渲染），选择透明组合。如果核心操作是安全地构建和修改树结构（如 IDE 的项目文件浏览器），选择安全组合。如果是框架/类库设计（不确定下游如何使用），优先考虑透明组合加默认实现。","hints":["透明组合的「统一对待」vs 安全组合的「编译时安全」——这是接口设计中的经典取舍","Java AWT 的 Component.add() 方法为什么在 Button 上可用但不做任何事——透明组合的设计代价"],"tags":["设计模式","组合模式","Composite","结构型","接口设计"],"id":1707},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"访问者模式的双分派机制","content":"访问者模式（Visitor Pattern）的核心机制是双分派（Double Dispatch）。请解释什么是单分派和双分派，Java 为什么需要访问者模式来模拟双分派，accept/visit 的双层调用是如何实现双分派的，以及双分派解决了什么设计问题。","answer":"答案：双分派是访问者模式的核心机制——第一次分派由 element.accept(visitor) 根据 Element 的实际运行时类型决定调用哪个 accept 实现；第二次分派由 visitor.visit(this) 根据 Visitor 的实际类型和 this 的编译时类型决定调用哪个 visit 重载方法。双分派解决了「操作依赖于两个对象的具体类型」的问题——即方法的行为同时取决于 Element 的类型和 Visitor 的类型。\n\n解析：\n\n单分派的局限——Java 是单分派语言：方法调用时，只有接收者（receiver）参与动态分派。例如 shape.draw() 调用哪个 draw() 只取决于 shape 的实际类型（Circle 或 Rectangle）。方法参数的重载选择是编译时根据参数的静态类型决定的——不是动态分派。因此，如果直接写 visitor.visit(shape)（其中 shape 是 Element 类型），即使 visitor 是 PDFExporter，也只会调用 visit(Element) 而不会调用 visit(Circle)——因为参数的静态类型是 Element。\n\n访问者模式的双分派实现——分为两步：\n\n第一次分派（Element.accept）：客户端调用 element.accept(visitor)。Java 的多态机制根据 element 的实际运行类型（Circle / Rectangle）动态分派到正确的 accept 方法。关键代码：Circle.accept(Visitor v) 被调用。\n\n第二次分派（Visitor.visit）：在 accept 方法内部，元素调用 visitor.visit(this)。此时 this 的编译时类型是具体的 Element 子类型（在 Circle.accept 中 this 是 Circle 类型）。Java 的重载方法选择是在编译时根据参数的静态类型决定的——visit(Circle c) 被选中（不是 visit(Element e)）。同时 Visitor 的类型被动态分派——如果 visitor 是 PDFExporter，则 PDFExporter.visit(Circle c) 被执行。\n\n代码模拟双分派的完整路径：\n客户端 → element.accept(visitor) → [动态分派] → Circle.accept(visitor) → visitor.visit(this) → [visit 方法重载在编译时绑定到 visit(Circle)，动态分派到 PDFExporter.visit(Circle)]\n\n如果没有访问者模式——要实现「用 PDFExporter 导出 Circle」和「用 JSONExporter 导出 Circle」的组合（2 种元素 × 2 种导出器 = 4 种行为），需要写 instanceof 判断：if (e instanceof Circle) { if (v instanceof PDFExporter) ... }。组合爆炸。\n\n扩展延伸：\n\n双分派解决的核心问题——「操作」和「数据结构」的双向扩展。在传统 OOP 中，新增一种 Element 子类是容易的（只需要修改相应的 visit 方法），但新增一种操作需要修改所有 Element——违反开闭原则。访问者模式的精巧之处在于它反转了这种依赖：新增一种操作（新增 Visitor 子类）不需要修改任何 Element 类。代价是新增一种 Element 子类需要修改所有 Visitor 接口。\n\n其他语言中的多分派——1）C++ 通过 dynamic_cast 手动实现多分派，或使用 std::visit + std::variant 实现编译期多分派。2）C# 4.0+ 使用 dynamic 关键字实现动态分派。3）Common Lisp 的 CLOS 原生支持多分派（方法根据所有参数的类型分派）。4）Kotlin 的 when 表达式 + sealed class 提供了另一种解决思路。\n\nVisitor 的设计代价——1）双向扩展的不对称：新增 Element 困难（需要修改所有 Visitor），新增 Visitor 容易。因此访问者模式适用于 Element 层次结构相对稳定、操作频繁变化的场景（如编译器 AST 遍历）。2）破坏封装：Visitor 需要访问 Element 的内部状态才能完成操作，这意味着 Element 需要暴露内部细节。3）循环依赖：Element 依赖 Visitor 接口，Visitor 依赖具体 Element 类，可能形成双向依赖。\n\n实际应用——ANTLR 的 AST 遍历、ASM 字节码框架的 ClassVisitor/MethodVisitor、Spring 的 BeanDefinitionVisitor、Jackson 的 JsonSerializer/JsonDeserializer 体系。所有这些场景的共同特征：数据结构（AST 节点/字节码元素）相对固定，而操作（代码生成/类型检查/序列化方式）频繁新增和变动。","hints":["Java 的方法重载是编译时根据参数静态类型绑定的——v.visit(e) 不会根据 e 的实际类型动态分派，这是为什么需要 accept 做第一次分派的原因","双分派的本质就是两次单分派叠加——第一次用 accept 的动态分派确定 Element 类型，第二次用 visit 的重载+分派确定 Visitor 类型和 Element 具体类型的组合"],"tags":["设计模式","访问者模式","Visitor","双分派","行为型"],"id":1708},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"桥接模式在 JDBC 驱动中的应用","content":"JDBC（Java Database Connectivity）是桥接模式（Bridge Pattern）的经典工程实现。请详细分析 JDBC 架构中 DriverManager、Driver、Connection、Statement、ResultSet 等核心组件如何体现桥接模式的角色映射，以及这种设计如何实现了「抽象与实现独立变化」。JDBC 的四种驱动类型（Type 1-4）在桥接模式中的实现差异是什么？","answer":"答案：JDBC 架构是桥接模式在工业界的经典落地——DriverManager 和 JDBC 标准接口（Connection、Statement、ResultSet）构成抽象部分，各数据库厂商实现的 Driver 和对应的具体连接库构成实现部分。桥接模式使应用程序面向 JDBC 标准接口编程，底层数据库实现（MySQL/Oracle/PostgreSQL）可以独立切换和演进，抽象层次和实现层次通过 Driver 接口桥接。\n\n解析：\n\nJDBC 中的桥接模式角色映射：\n\n1）Abstraction（抽象部分）——DriverManager 类是桥接的抽象侧入口。它提供了统一的高层接口 getConnection(url, user, password)，内部维护已注册的 Driver 列表。DriverManager 不依赖具体数据库——它遍历所有已注册的 Driver，调用 driver.connect(url) 直到某个 Driver 成功返回 Connection。\n\n2）RefinedAbstraction（扩展抽象）——Connection、Statement、ResultSet 等 JDBC 标准接口构成了抽象层次的扩展。Connection 负责会话管理（事务、元数据），Statement 负责 SQL 执行，ResultSet 负责结果集遍历。应用程序基于这些接口编写数据访问代码，不依赖任何数据库特有的 API。实际框架（如 MyBatis、Hibernate）进一步扩展了这些抽象，提供了连接池、ORM 映射等高级功能。\n\n3）Implementor（实现接口）——java.sql.Driver 接口定义了实现侧的统一契约。核心方法：connect(String url, Properties info) 建立连接；acceptsURL(String url) 判断该 Driver 是否能处理该 URL；getMajorVersion() / getMinorVersion() 返回版本信息。每个数据库厂商必须实现这个接口。\n\n4）ConcreteImplementor（具体实现）——各数据库厂商的驱动实现：com.mysql.cj.jdbc.Driver、org.postgresql.Driver、oracle.jdbc.OracleDriver 等。每个 Driver 实现类负责：解析 JDBC URL、建立 TCP 连接、完成数据库握手认证协议、返回 Connection 实现类的实例（如 JDBC4Connection 包装了 MySQL 协议实现）。\n\n完整的桥接工作流程：1）驱动加载阶段：通过 Class.forName() 或 Java SPI（META-INF/services/java.sql.Driver）加载 Driver 实现类，每个 Driver 在静态初始化块中调用 DriverManager.registerDriver(this) 完成注册。2）连接建立阶段：应用程序调用 DriverManager.getConnection(url)。DriverManager 遍历已注册的 Driver 列表，调用每个 driver.acceptsURL(url) 筛选出能处理该 URL 的 Driver，然后调用 driver.connect(url, info) 建立连接。3）操作执行阶段：返回的 Connection 实际是 MySQLConnection 或 PostgreSQLConnection 等具体实现，但应用程序通过 JDBC 标准接口操作——createStatement() 返回具体 Statement，executeQuery() 返回具体 ResultSet。\n\n扩展延伸：\n\nJDBC 四种驱动类型与桥接模式——\n\nType 1（JDBC-ODBC Bridge）：过时的桥接实现。JDBC 接口桥接到 ODBC（C 语言 API），再通过 ODBC 驱动访问数据库。性能差、需要配置 ODBC 数据源、已不推荐使用。\n\nType 2（Native API Driver，部分 Java 实现）：桥接模式的适配器变体。Java 的 JDBC 接口通过 JNI 桥接到数据库厂商的原生 C/C++ 客户端库（如 Oracle OCI）。优点：性能较好。缺点：需要安装平台相关库。\n\nType 3（Network Protocol Driver，纯 Java 客户端 + 中间件）：三层桥接。客户端 JDBC 驱动通过纯 Java socket 连接到中间件服务器（如 Sequoia、Athena），中间件再将请求转发到数据库。优点：中间件可以做连接池、读写分离、结果集缓存。缺点：多一次网络跃迁，延迟增加。\n\nType 4（Pure Java Driver，纯 Java 直连）：最纯粹的桥接模式实现。纯 Java 实现，直接通过 TCP Socket 实现数据库通信协议（MySQL Protocol、PG Protocol）。优点：纯 Java、无依赖、性能最好。缺点：每个数据库有自己的协议实现。\n\n桥接模式的收益在 JDBC 架构中的体现——1）数据库厂商可独立实现 Driver——Oracle 不需要知道 MySQL 的实现，只要遵守 Driver 接口契约即可。2）应用代码零改动切换数据库——只需要替换驱动 jar（Classpath）和修改连接 URL，业务代码不需要任何修改。3）JDBC 标准可独立演进——JDBC 3.0→4.0→4.2 增加了批量更新、ROWID、National Character Set 等新特性，各驱动厂商分别适配即可，应用层有计划地升级。4）Connection/Statement/ResultSet 三个抽象接口各自有独立的实现层次，细分了桥接模式中的实现职责——Connection 负责会话生命周期、Statement 负责 SQL 执行策略、ResultSet 负责结果集遍历。","hints":["JDBC 的桥接角色映射：DriverManager=Abstraction 入口、Driver=Implementor 接口、各厂商 Driver=ConcreteImplementor、Connection/Statement/ResultSet=RefinedAbstraction","Type 4 驱动（纯 Java 直连）是最纯粹的桥接模式实现——它不需要任何平台相关库，完全在 Java 层面桥接 JDBC 抽象和数据库协议"],"tags":["设计模式","桥接模式","JDBC","Bridge","结构型"],"id":1709},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"中介者模式 vs 观察者模式的事件分发对比","content":"中介者模式（Mediator）和观察者模式（Observer）都用于对象间的通信和事件分发，但它们的通信拓扑结构、耦合程度和控制流程有本质差异。请对比这两种模式在事件分发机制上的核心区别，分析各自的优缺点，并说明在实际系统（如 MVC、消息队列、Redux）中它们各自的应用形式。","answer":"答案：观察者模式采用「一对多」的直接通知拓扑——Subject 持有 Observer 引用列表，状态变化时直接遍历通知。中介者模式采用「多对多」的间接通信拓扑——Colleague 之间不直接通信，通过 Mediator 中转转发。核心区别：观察者模式建立了 Subject 到 Observer 的固定订阅关系，发布者知道自己在通知谁；中介者模式的 Colleague 之间互不知晓，所有通信经过 Mediator 协调和转发。\n\n解析：\n\n通信拓扑的差异——观察者模式：星形拓扑变体。Subject 是中心，Observer 是外围节点。事件方向固定（Subject→Observer），不支持 Observer 向 Subject 主动通信（如果需要，则需要额外的机制）。中介者模式：完全的中心化拓扑。Mediator 作为唯一枢纽，所有 Colleague 之间的通信必须经过 Mediator。通信方向完全双向——Colleague A 可以通过 Mediator 向 Colleague B 发送消息，也可以从 Colleague B 接收消息。\n\n耦合程度的差异——观察者模式：Subject 通过抽象 Observer 接口引用 Observer，两者之间存在编译期依赖。Subject 知道自己在通知哪些 Observer（虽然通过接口而非具体类）。Observer 注册和注销需要 Subject 的公共接口支持。中介者模式：Colleague 只依赖 Mediator 接口，不依赖其他 Colleague。Colleague 之间没有任何直接引用关系——完全解耦。但 Mediator 承担了所有的通信协调逻辑，Mediator 因此依赖所有 Colleague 接口（耦合从 Colleague 之间转移到了 Mediator 上）。\n\n控制流程的差异——观察者模式：Subject 发布事件后同步（或通过事件总线异步）通知所有注册的 Observer。每个 Observer 收到完整事件数据，独立处理。Subject 不关心 Observer 的处理顺序和结果（fire-and-forget）。中介者模式：Mediator 收到消息后可以执行复杂的协调逻辑——决定消息的路由目标、转换消息格式、编排多个 Colleague 的执行顺序（如先验证→再存储→后通知）、条件判断是否传递消息。Mediator 是控制流的决策者。\n\n可扩展性的差异——观察者模式：新增 Observer 只需注册到 Subject，Subject 不需要修改（符合开闭原则）。Observer 之间互相独立，新增一个不影响其他。中介者模式：新增 Colleague 可能需要修改 Mediator（如果通信规则涉及新 Colleague）。随着 Colleague 数量增多，Mediator 可能膨胀为 God Object。\n\n扩展延伸：\n\n实际系统中的应用对比——\n\n1）MVC 架构中的 Controller（中介者模式）：Controller 协调 View 和 Model 的交互。View 触发事件→Controller 接收→更新 Model→Model 变化通过观察者机制通知 View 更新。Controller 是 Mediator，Model 和 View 之间的数据绑定（数据变化自动更新 UI）则是观察者模式。两者在这里是互补使用的。\n\n2）消息队列 Kafka/RabbitMQ（中介者模式）：Broker 充当 Mediator，Producer 和 Consumer 完全不直接通信。Broker 负责消息的路由（Topic/Exchange）、持久化、负载均衡和故障转移。Producer 和 Consumer 都可以独立扩缩容。\n\n3）Redux（中介者 + 观察者结合）：Store 是 Mediator——Component dispatch Action → Store 转发给 Reducer → 更新 State → Component 通过 subscribe 观察 State 变化。Store 协调了 Action 的分发和 State 的管理，Component 则通过观察者模式订阅所需的状态片段。\n\n4）Spring ApplicationEvent（观察者模式）：@EventListener 注解的 Bean 作为 Observer，ApplicationEventPublisher 作为 Subject。发布者和监听者通过事件类型解耦，但本质是一对多通知。\n\n5）GUI 对话框（中介者模式）：对话框中的 UI 组件（下拉框、输入框、按钮）不直接交互。选择省份→Mediator 协调→刷新城市下拉框→Mediator 协调→更新区县列表。每个组件只和 Mediator 通信，Mediator 集中管理组件间的协作逻辑。\n\n性能与维护权衡——观察者模式：一对多通知的性能取决于 Observer 数量和单个处理时间。同步模式下慢速 Observer 可能阻塞整个通知链。事件总线（异步观察者）可以缓解此问题。中介者模式：Mediator 成为通信瓶颈和单点故障（SPOF）。Mediator 的逻辑复杂度随 Colleague 数量非线性增长——需要持续重构或拆分 Mediator 的职责。\n\n选择原则——如果通信模式是「一个事件源→多个独立接收者」，选择观察者模式（更轻量）。如果通信需要双向交互、复杂编排或条件路由，选择中介者模式（控制力更强）。在大型系统中，两者通常共存：中介者负责编排流程，观察者负责状态同步通知。","hints":["观察者模式的通信是预先建立好的订阅关系（发布者知道自己通知谁），中介者模式的通信经过中心枢纽转发（通信双方互不知晓）","MVC 中的 Controller 是中介者（协调 View 和 Model），而 Model 变化通知 View 是观察者——这两种模式在 MVC 中是互补共存的"],"tags":["设计模式","中介者模式","观察者模式","Mediator","行为型"],"id":1710},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"命令模式实现撤销重做","content":"如何用命令模式（Command Pattern）实现一个支持撤销（Undo）和重做（Redo）的操作系统？请说明核心组件和工作流程。","answer":"命令模式的核心思想是将一个请求封装为一个对象，从而使你可以用不同的请求对客户端进行参数化。\\n组件：\\n1. Command 接口：定义 `execute()` 和 `undo()` 方法。\\n2. ConcreteCommand：实现具体的操作逻辑和对应的撤销逻辑。\\n3. Invoker：维护一个命令历史栈 `undoStack` 和一个重做栈 `redoStack`。\\n4. Receiver：实际执行操作的对象（如文本编辑器中的 Document）。\\n工作流程：\\n- 每次执行一个命令时，调用 `command.execute()`，然后将该命令压入 `undoStack`，同时清空 `redoStack`（因为新操作后重做历史无效）。\\n- 撤销时，从 `undoStack` 弹出顶部命令，调用 `command.undo()`，然后将该命令压入 `redoStack`。\\n- 重做时，从 `redoStack` 弹出顶部命令，调用 `command.execute()`，再压回 `undoStack`。\\n- 约束：`undoStack` 需要设置最大深度（如 100 步），防止内存溢出。\\n示例（伪代码）：\\n```\\nclass TextEditorCommand implements Command {\\n    execute() { insertText(text); }\\n    undo() { deleteText(length); }\\n}\\nInvoker {\\n    Stack undoStack, redoStack;\\n    void executeCommand(cmd) {\\n        cmd.execute();\\n        undoStack.push(cmd);\\n        redoStack.clear();\\n    }\\n    void undo() {\\n        cmd = undoStack.pop(); cmd.undo(); redoStack.push(cmd);\\n    }\\n    void redo() {\\n        cmd = redoStack.pop(); cmd.execute(); undoStack.push(cmd);\\n    }\\n}\\n```","hints":["关键在于每个命令同时封装了「执行」和「撤销」两个操作","redo 栈在每次新操作时被清空——这是常见的设计决策"],"tags":["命令模式","撤销重做","行为型模式"],"id":1711},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"原型模式与深浅拷贝","content":"原型模式（Prototype Pattern）中，浅拷贝（Shallow Copy）和深拷贝（Deep Copy）的区别是什么？分别适用什么场景？Java 中如何实现？","answer":"原型模式通过复制一个已有对象来创建新对象，而非通过 new 调用构造器。核心是 Java 中的 `Cloneable` 接口和 `clone()` 方法。\\n浅拷贝：\\n- 复制基本类型字段的值和引用类型字段的引用地址。\\n- 复制后的对象和原对象共享引用类型字段指向的同一个实例。\\n- Java 默认的 `super.clone()` 就是浅拷贝。\\n- 适用于：对象只包含基本类型和不可变对象（如 String、Integer），或者你明确希望共享内部状态。\\n深拷贝：\\n- 递归复制所有引用类型字段指向的对象，创建完全独立的副本。\\n- 修改副本的内部状态不会影响原对象。\\n- 实现方式：a) 重写 `clone()` 方法，手动 clone 所有可变引用字段；b) 序列化 + 反序列化；c) 手动构造器（copy constructor）。\\n- 适用于：对象包含可变引用类型，且需要完全隔离的副本。\\n- 问题：深拷贝在对象图复杂时可能引发循环引用问题（A 引用 B，B 引用 A），此时需要手动处理。\\n注意：数组的 `clone()` 是浅拷贝——数组元素如果是引用类型，复制的是引用而非对象本身。\\n最佳实践：实现了 `Cloneable` 却不重写 `clone()` 为 public 是常见的错误。更推荐使用 copy constructor 或 factory method 替代 `Cloneable`。","hints":["Java 的 Cloneable 是一个标记接口（marker interface），不重写 clone() 会抛 CloneNotSupportedException","深拷贝在性能敏感场景中可能成为瓶颈——考虑是否需要完全隔离"],"tags":["原型模式","深浅拷贝","创建型模式"],"id":1712},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"享元模式与字符串常量池","content":"享元模式（Flyweight Pattern）是什么？请以 Java 字符串常量池（String Pool）为例说明其应用和设计要点。","answer":"享元模式的核心思想是共享大量细粒度对象以减少内存占用。当系统中存在大量相似对象时，将对象中「不变的部分（内在状态）」提取出来共享，「变化的部分（外在状态）」由客户端维护。\\nJava String Pool 是典型的享元模式应用：\\n- 内在状态（共享）：字符串的字符数组内容。\\n- 外在状态（不共享）：无（字符串本身是不可变的，所以全部状态都内在化）。\\n- 当通过字面量创建字符串时（如 `String s = 「hello」`），JVM 会先在常量池中查找，如果已存在则返回引用，否则创建新对象放入池中。\\n- 通过 `new String(「hello」)` 创建的字符串不进入常量池（除非调用 `intern()`），这是为了避免所有字符串都被强制共享。\\n设计要点：\\n1. 区分内在状态（Intrinsic State，可共享）和外在状态（Extrinsic State，不可共享）。\\n2. 享元对象应该是不可变的（immutable）——如果共享对象允许修改，会导致并发问题。\\n3. 需要有一个享元工厂（Flyweight Factory）来管理池中的对象，实现对象的复用和创建。\\n4. 适用场景：对象数量巨大、对象状态可被外部化、对象大部分状态可共享。典型的真实应用包括线程池、连接池、缓存系统。\\n在 Java 中，除了 String Pool，Integer 的 `valueOf()` 也使用了享元模式（缓存 -128 到 127 的 Integer 对象）。","hints":["享元模式的关键问题是「哪些状态可以共享」——这是设计难点","享元对象必须是不可变的，否则共享带来的并发问题会抵消内存收益"],"tags":["享元模式","字符串常量池","结构型模式"],"id":1713},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"责任链模式与 Web 中间件","content":"责任链模式（Chain of Responsibility Pattern）在 Web 框架的中间件（Middleware）中如何实现？请以 Express.js 或 Django 中间件为例说明。","answer":"责任链模式将多个处理对象连成一条链，请求沿着链传递直到某个处理器处理它。Web 中间件的本质就是责任链模式。\\nExpress.js 实现：\\n```\\napp.use((req, res, next) => {\\n    console.log('Request:', req.url);\\n    next();  // 传递给下一个中间件\\n});\\napp.use('/api', authMiddleware);\\napp.use('/api', errorHandler);\\n```\\n每个中间件要么调用 `next()` 将控制权传递给链中的下一个中间件，要么直接发送响应终止链。\\nDjango 实现：\\nDjango 的中间件是洋葱模型（Onion Model）。请求从外到内穿过所有中间件，响应从内到外反向穿过。\\n```\\nclass MyMiddleware:\\n    def __init__(self, get_response):\\n        self.get_response = get_response\\n\\n    def __call__(self, request):\\n        # 请求阶段（进入洋葱）\\n        response = self.get_response(request)  # 调用下一个中间件\\n        # 响应阶段（退出洋葱）\\n        return response\\n```\\n设计要点：\\n1. 每个处理器知道下一个处理器是谁（通过 `next` 指针或列表索引）。\\n2. 处理器可以决定是否继续传递（调用 `next()`）或终止链（直接返回响应）。\\n3. 链的顺序很重要——中间件的注册顺序决定了处理顺序。\\n4. 适用场景：有多个处理器按顺序处理同一个请求，且处理器之间的顺序是动态可变的。典型的真实应用还有 Java Servlet Filter、ASP.NET Core Middleware、Logging 框架的日志级别过滤。\\n优点：解耦了请求发送者和接收者，新增处理器不需要修改现有代码。\\n缺点：调试困难——请求在链中传递的路径可能不直观。","hints":["Express.js 是线性链（单向传递），Django 是洋葱模型（双向传递）——理解了区别才算真正懂了","中间件顺序决定行为——认证中间件应该在业务中间件之前"],"tags":["责任链模式","Web 中间件","行为型模式"],"id":1714},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"MVC vs MVVM vs MVP","content":"MVC、MVVM、MVP 三种架构模式的核心区别是什么？各自的优缺点和适用场景是什么？","answer":"三种模式都是关注点分离（Separation of Concerns）的实现，核心区别在于 View 和 Model 之间的通信方式以及 Controller/Presenter/ViewModel 的角色。\\nMVC（Model-View-Controller）：\\n- 流程：View → Controller → Model → View。Controller 接收用户输入，更新 Model，Model 通知 View 更新。\\n- 优点：最经典，概念简单，适合传统 Web 应用（如 Rails、Django）。\\n- 缺点：Controller 容易变得臃肿（Fat Controller），View 和 Model 之间仍有直接依赖。\\n- 适用：服务端渲染的 Web 应用，iOS 开发（Apple 官方推荐）。\\nMVVM（Model-View-ViewModel）：\\n- 流程：View ↔ ViewModel ↔ Model。ViewModel 通过数据绑定（Data Binding）自动同步 View 状态，无需手动操作 DOM。\\n- 优点：View 和 Model 完全解耦，ViewModel 可独立测试，数据绑定减少了大量样板代码。\\n- 缺点：数据绑定调试困难，复杂场景下性能开销大。\\n- 适用：前端框架（Vue、React with hooks、Angular），WPF、SwiftUI。\\nMVP（Model-View-Presenter）：\\n- 流程：View ↔ Presenter ↔ Model。Presenter 是中间人，View 将所有用户事件委托给 Presenter。\\n- 优点：View 和 Model 完全分离（比 MVC 更彻底），Presenter 易于单元测试（因为 View 被抽象为接口）。\\n- 缺点：需要为每个 View 定义接口，导致文件数量多；Presenter 层会变得非常厚重。\\n- 适用：Android 原生开发（早期推荐），复杂 UI 的桌面应用。\\n简单对比：MVC 的 Controller 对 View 了解较少；MVP 的 Presenter 知道 View 的接口；MVVM 的 ViewModel 完全不知道 View 的存在（通过数据绑定间接通信）。","hints":["三者的核心区别是「View 是否知道 Model」——MVC 知道，MVP/MVVM 不知道","MVVM 依赖数据绑定框架（如 Vue/React），没有框架支持很难纯手工实现"],"tags":["架构模式","MVC","MVVM","MVP"],"id":1715},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"Refactoring god class with Strategy and Factory patterns","content":"You have inherited a PaymentProcessor class with 3,000 lines of code. It handles credit card payments, PayPal, cryptocurrency, bank transfers, and gift cards — all in a single class with switch statements scattered across 15 methods. The class is brittle and adding a new payment method requires touching 8+ files. How would you refactor this using design patterns?","answer":"Refactoring plan using Strategy + Factory patterns:\n\nStep 1: Identify the Strategy interface.\nExtract a common interface IPaymentStrategy with methods:\n- validate()\n- authorize(amount: Money)\n- capture(authorizationId: String)\n- refund(transactionId: String)\n- getFee(): Money\n\nEach payment method becomes a concrete strategy implementing IPaymentStrategy: CreditCardStrategy, PayPalStrategy, CryptoStrategy, BankTransferStrategy, GiftCardStrategy.\n\nStep 2: Create a PaymentStrategyFactory.\n- The factory takes a payment type enum and returns the appropriate strategy.\n- Strategy selection is centralized in one place instead of scattered switch statements.\n- New payment methods = new strategy class + factory registration — no existing code changes (Open/Closed Principle).\n\nStep 3: Refactor PaymentProcessor into a Context class.\n- PaymentProcessor now holds a reference to the current IPaymentStrategy.\n- It delegates all payment operations to the strategy.\n- It handles cross-cutting concerns: logging, metrics, transaction orchestration.\n\nStep 4: Extract shared behavior.\n- Some strategies share logic (e.g., credit/debit card validation, 3D Secure). Use Template Method pattern: create AbstractCardPaymentStrategy with shared card validation, letting specific card types override only their differences.\n\nStep 5: Handle stateful flows.\n- If payment flows have states (initiated, authorized, captured, refunded), introduce a State pattern inside each strategy or use a lightweight state machine.\n\nResult: PaymentProcessor drops from 3000 lines to ~100 lines of orchestration. Each strategy is 100-400 lines, independently testable, and new payment methods require zero changes to existing strategies.","hints":["The core problem is that different payment methods vary in implementation but share the same interface contract.","Think about which pattern handles object creation vs. which handles algorithm selection."],"tags":["strategy-pattern","factory-pattern","refactoring","open-closed-principle","god-class"],"id":1716},{"category":"design_patterns","difficulty":"medium","type":"choice","title":"Choosing the right pattern for event notification system","content":"You are designing a system where multiple components need to be notified when a user completes a purchase: email service, analytics service, inventory service, and loyalty points service. These services are independently deployed and may be added or removed without affecting the purchase flow. Which design pattern BEST fits this scenario?","options":["A) Chain of Responsibility — each service tries to handle the event and passes it along if it can't.","B) Observer — the purchase system maintains a list of subscribers and notifies them when an event occurs.","C) Command — encapsulate the purchase event as a command object with all necessary data.","D) Decorator — wrap the purchase system with additional behavior for each service."],"answer":"B","hints":["The key requirement is that services can be added/removed independently without changing the purchase flow.","Think about which pattern explicitly supports a one-to-many dependency where changes to one object require notifying others."],"tags":["observer-pattern","event-driven","loose-coupling","distributed-systems","pub-sub"],"id":1717},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"Applying patterns to handle API rate limiting across services","content":"Your microservices architecture calls a third-party API that has a strict rate limit of 100 requests per minute per API key. You have 5 services that all need to call this API. Currently, each service manages its own retries, leading to frequent 429 errors and wasted quota. Design a solution using appropriate design patterns.","answer":"Solution combining Proxy, Singleton, and Decorator patterns:\n\nCore pattern: Proxy-based rate limiter.\n\nLayer 1: RateLimiterProxy (Proxy pattern)\n- Stands between the 5 services and the third-party API.\n- All API calls go through this proxy instead of directly.\n- The proxy enforces the rate limit and queues/retries requests.\n\nLayer 2: Token Bucket Algorithm (implemented within the proxy)\n- A single token bucket instance manages the shared pool of 100 requests/minute.\n- Each request consumes a token; if no tokens available, the request is queued or rejected with a friendly retry-after header.\n- Tokens refill at a steady rate (100 per 60 seconds = ~1.67 tokens/sec).\n\nLayer 3: Singleton for the token bucket state.\n- The rate limiter state must be shared across all service instances. Use a distributed singleton (e.g., Redis) rather than an in-memory singleton, because services run on different nodes.\n- Redis: a sorted set or counter with TTL stores each service's usage, or a simple INCR with EXPIRE for a sliding window.\n\nLayer 4: Decorator for observability.\n- Wrap the proxy with a decorator that logs rate limit hits, measures latency, and emits metrics (e.g., requests queued, requests rejected, average wait time).\n- This keeps observability concerns out of the rate limiter logic.\n\nAlternative approach: Circuit Breaker (from stability patterns)\n- If rate limits are consistently hit, a circuit breaker trips and prevents all calls for a cooldown period, giving the quota time to recover.\n- This can be combined with the proxy: the proxy checks the circuit breaker before attempting to consume a token.\n\nResult: Services call the rate limiter proxy as if it were the original API. They don't need to know about rate limits at all. Adding a new service requires zero rate-limit configuration changes.","hints":["The proxy pattern controls access to an object, which is exactly what rate limiting needs.","A distributed system requires a shared state mechanism — in-memory singletons won't work across services."],"tags":["proxy-pattern","singleton-pattern","rate-limiting","distributed-systems","resilience-patterns"],"id":1718},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"When NOT to use design patterns — real-world trade-off","content":"A junior engineer on your team wants to introduce the Abstract Factory pattern to create different types of database connections (MySQL, PostgreSQL, MongoDB). Currently the app only uses PostgreSQL and there are no firm plans to add another database. The proposed pattern would add 6 new classes and increase setup complexity. How do you evaluate this design decision?","answer":"Evaluation framework: Apply YAGNI (You Ain't Gonna Need It) and consider the cost of abstraction.\n\nArguments against the Abstract Factory:\n1. Premature abstraction: YAGNI principle says don't build extensibility until you need it. Adding a database switch is speculative.\n2. Concrete cost: 6 new classes, a DI configuration change, more complex testing setup (you now need factory mocks). Every developer touching this code pays this cost forever.\n3. Actual extensibility: Real-world database migrations rarely just swap a connection. Each database has different query syntax, transaction semantics, and optimization strategies. An Abstract Factory over connection objects gives false confidence — the real work is in the query layer, not the connection layer.\n4. Conway's Law: The architecture encourages treating databases as interchangeable, which may not reflect the actual business domain.\n\nWhen you MIGHT accept the pattern:\n- If the app is a library/framework meant to be deployed by different customers with different DB requirements.\n- If the team has a concrete roadmap item for multi-DB support in the next quarter.\n- If the PostgreSQL code already has hardcoded references that would be painful to extract later (technical debt value).\n\nAlternative: A much simpler abstraction — a single DatabaseAdapter interface with one PostgresAdapter implementation. If (and only if) a second database is ever needed, the interface is already in place without the factory complexity. Cost: 2 classes instead of 8.\n\nKey lesson: Design patterns solve problems — don't introduce them because they look clean or future-proof. The simplest solution that works today is often the best long-term choice.","hints":["YAGNI is specifically about not building for speculative future requirements.","Consider the maintenance burden of the abstraction itself, not just its benefits."],"tags":["yagni","over-engineering","abstract-factory","design-tradeoffs","simplicity"],"id":1719},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"Implementing undo/redo with the Command and Memento patterns","content":"You are building a diagram editor (like draw.io or Figma lite) where users can add, move, resize, and delete shapes. You need to support undo (Ctrl+Z) and redo (Ctrl+Shift+Z) with unlimited history, but storing full state snapshots would be too memory-intensive. How would you implement undo/redo using design patterns?","answer":"Hybrid approach: Command pattern for actions + Memento pattern for selective state capture.\n\nCore architecture: Command pattern.\n\n1. Define a Command interface with: execute(), undo(), and optionally redo().\n2. Each user action becomes a concrete command:\n   - AddShapeCommand(shapeData)\n   - MoveShapeCommand(shapeId, oldPosition, newPosition)\n   - ResizeShapeCommand(shapeId, oldDimensions, newDimensions)\n   - DeleteShapeCommand(shapeData) — stores the full shape data for undo\n3. Maintain two stacks: undoStack (past commands) and redoStack (undone commands).\n4. When a command executes → push to undoStack, clear redoStack.\n5. Undo: pop from undoStack, call command.undo(), push to redoStack.\n6. Redo: pop from redoStack, call command.execute(), push to undoStack.\n\nMemory optimization: Instead of storing full state, each command stores only the delta (old/new values). For MoveShapeCommand, you store shapeId + oldX/Y + newX/Y — not the entire canvas state. This is where Command pattern shines over storing snapshots.\n\nWhen Memento is needed (for complex operations):\n- Some operations affect many objects (e.g., group delete, paste with 50 shapes). Storing per-object deltas could be costly.\n- Alternative: for grouped operations, use a CompositeCommand that wraps multiple commands. Undo of CompositeCommand iterates child commands in reverse.\n- For irreversible operations (e.g., rasterizing a vector layer), use the Memento pattern: capture the affected objects' state before the operation, and restore the memento on undo.\n\nPractical limit: Cap the undo stack at 500 commands. For cases exceeding this, periodically save a full canvas snapshot as a baseline and keep subsequent commands as deltas (checkpoint-based undo).\n\nBonus: Use Prototype pattern to deep-clone shape objects for mementos, avoiding shared reference bugs.","hints":["Storing inverse operations is much cheaper than storing full state snapshots.","Think about what minimal information each command type needs to reverse itself."],"tags":["command-pattern","memento-pattern","undo-redo","state-management","memory-optimization"],"id":1720},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"策略模式在支付场景中的实战应用","content":"一个支付系统需要支持微信支付、支付宝、银联三种支付方式，每种方式的支付流程、回调处理、退款逻辑都不同。请用代码结构说明策略模式如何组织这些支付方式，以及策略模式与简单工厂模式如何配合使用。","answer":"答案：策略模式将每种支付方式封装为独立的策略类，通过统一的接口调用。工厂模式负责根据条件选择具体的策略实现。\n\n代码结构示意：\n\n```\n// 策略接口\ninterface PaymentStrategy {\n    PaymentResult pay(PayRequest request);\n    CallbackResult handleCallback(CallbackData data);\n    RefundResult refund(RefundRequest request);\n}\n\n// 具体策略\nclass WechatPayStrategy implements PaymentStrategy { /* 微信支付逻辑 */ }\nclass AlipayStrategy implements PaymentStrategy { /* 支付宝逻辑 */ }\nclass UnionPayStrategy implements PaymentStrategy { /* 银联逻辑 */ }\n\n// 策略工厂\nclass PaymentStrategyFactory {\n    private Map<String, PaymentStrategy> strategies = new HashMap<>();\n    \n    public PaymentStrategyFactory() {\n        strategies.put(\"wechat\", new WechatPayStrategy());\n        strategies.put(\"alipay\", new AlipayStrategy());\n        strategies.put(\"unionpay\", new UnionPayStrategy());\n    }\n    \n    public PaymentStrategy getStrategy(String type) {\n        PaymentStrategy strategy = strategies.get(type);\n        if (strategy == null) throw new IllegalArgumentException(\"不支持的支付方式\");\n        return strategy;\n    }\n}\n\n// 使用方（业务代码无需知道具体支付逻辑）\nclass PaymentService {\n    private PaymentStrategyFactory factory;\n    \n    public PaymentResult pay(String type, PayRequest request) {\n        PaymentStrategy strategy = factory.getStrategy(type);\n        return strategy.pay(request);\n    }\n}\n```\n\n如果不用策略模式而用 if-else，代码会变成：\n```\nif (\"wechat\".equals(type)) {\n    // 30 行微信支付逻辑\n} else if (\"alipay\".equals(type)) {\n    // 30 行支付宝逻辑\n} else if (\"unionpay\".equals(type)) {\n    // 30 行银联逻辑\n}\n```\n\n每增加一种支付方式就要修改现有代码（违反开闭原则），且 if-else 中的逻辑无法独立测试。\n\n策略模式的优点：\n1）开闭原则——新增支付方式只需增加新的策略类，无需修改现有代码。\n2）独立测试——每种支付策略可以独立单元测试。\n3）运行时切换——可以在运行时根据条件切换策略。\n4）消除条件判断——业务代码从「根据 type 做不同的事」变成「让策略对象做它的事」。\n\n解析：策略模式+工厂模式是实际项目中最常用的组合之一，广泛用于支付、通知、配送、优惠券等「多路分支」场景。注意：如果策略数量少且不太可能扩展（如只有 2 种且确定不会新增），用 if-else 反而更简单——模式不是银弹，引入复杂度需要理由。\n\n扩展延伸：策略模式的高级用法——策略枚举（Enum Strategy）：把策略定义在枚举中（结合 enum 的 constant-specific method implementation），减少类数量。Spring 中通过 @Autowired Map<String, Strategy> 自动注入所有策略 Bean，连工厂都不需要手写。","hints":["策略模式解决的是「多个类做同一件事但方式不同」的问题","工厂模式和策略模式组合使用时，工厂负责「选」策略，策略负责「做」","开闭原则：对扩展开放、对修改关闭——策略模式让新增支付方式不用改现有代码"],"tags":["设计模式","策略模式","工厂模式","支付","开闭原则"],"id":1721},{"category":"design_patterns","difficulty":"medium","type":"choice","title":"多渠道通知系统的模式选择","content":"你需要设计一个通知系统，支持邮件、短信、App Push、站内信四种渠道。每种渠道发送逻辑不同但调用方式一致。未来可能增加 Webhook 渠道。不同类型的通知（验证码、营销消息、系统告警）需要路由到不同渠道（验证码优先走短信，营销走邮件，告警全部渠道发送）。应选择哪种设计模式组合？","options":["A) 工厂模式 + 单例模式——Factory 创建不同的渠道实例，每个渠道是 Singleton","B) 策略模式 + 责任链模式——每个渠道是一个策略，用责任链决定通知按什么顺序尝试渠道","C) 观察者模式 + 建造者模式——发布通知事件，各渠道作为观察者订阅并发送","D) 模板方法模式 + 工厂模式——抽象模板定义发送流程骨架，子类实现具体渠道逻辑，工厂创建实例"],"answer":"B","hints":["问题的关键有两个：1）每个渠道发送逻辑不同但接口一致（策略模式）；2）通知路由有优先级和链式判断（验证码优先短信→失败？→站内信）","责任链模式可以让每个渠道决定是否能处理该通知，不能处理则传递给下一个渠道"],"tags":["设计模式","策略模式","责任链模式","通知系统","架构设计"],"id":1722},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"代理模式与装饰器模式的本质区别","content":"代理模式（Proxy）和装饰器模式（Decorator）都通过包装对象来扩展功能，类图非常相似。请从意图（Intent）、职责（Responsibility）、控制权（Control）三个维度详细说明两者的本质区别，并用实际场景说明什么情况下该用 Proxy 而非 Decorator，反之亦然。","answer":"答案：Proxy 和 Decorator 结构相似但意图完全不同。\n\n1）意图（Intent）区别：\n- Proxy 的目的是「控制访问」——控制客户端对真实对象的访问方式。代理站在客户端和真实对象之间，决定是否转发请求、何时转发、转发前做什么。\n- Decorator 的目的是「增强功能」——在不改变接口的前提下给对象动态添加新行为。装饰器不控制是否调用真实对象，而是在转发调用的前后附加行为。\n\n2）职责（Responsibility）区别：\n- Proxy 关注的是对象的生命周期、访问权限、延迟加载等「管理性」职责。典型：虚拟代理（Virtual Proxy——延迟创建大对象）、保护代理（Protection Proxy——控制访问权限）、远程代理（Remote Proxy——隐藏远程调用的复杂性）。\n- Decorator 关注的是功能的叠加和组合。典型：Java I/O 中的 BufferedInputStream(new FileInputStream(file))——Buffer 功能装饰 FileInputStream。每个装饰器只增加一个维度的能力，多个装饰器可以自由组合。\n\n3）控制权（Control）区别：\n- Proxy 决定「被代理对象是否被调用」——代理可以完全拦截请求、缓存结果、拒绝访问。代理不一定会把请求转发给真实对象（如缓存代理命中时直接返回）。\n- Decorator 不决定真实对象是否被调用——装饰器的职责是添加行为，最终还是调用被装饰对象的方法。装饰器默认会转发请求（super.method() 或 component.method()），只是在前/后附加操作。\n\n实际场景对比：\n\n用 Proxy 的场景：\n- 图片懒加载：用 Virtual Proxy 代替真实图片对象，用户请求时才创建真实图片对象。\n- API 缓存：用 Caching Proxy 在请求前检查缓存是否命中，命中直接返回。\n- RPC/远程调用：用 Remote Proxy 让本地代码像调用本地方法一样调用远程服务。\n\n用 Decorator 的场景：\n- Java I/O：new BufferedReader(new FileReader(file))——FileReader 读取文件，BufferedReader 添加缓冲功能，可以再套 LineNumberReader 添加行号功能。\n- 中间件：Express/Koa 的中间件机制——每个中间件是装饰器，在 HTTP 请求处理前后添加功能（日志、认证、压缩）。\n- 日志和监控：在一个已有的 Service 上装饰日志记录和耗时统计功能，不改原有代码。\n\n解析：判断标准很简单——问自己「我是在控制对对象的访问，还是在给对象加功能？」如果是前者用 Proxy，后者用 Decorator。Proxy 和 Decorator 有时可以互换实现（都在调用前后加逻辑），但意图不同决定了后续的可维护性和可理解性。\n\n扩展延伸：JDK 中的动态代理（Dynamic Proxy）是 Proxy 模式的 Java 实现，而 Spring AOP 本质上是一个装饰器——把横切关注点织入到目标对象的方法前后。AOP 和 Decorator 的关系：AOP 是 Decorator 模式在横切关注点上的泛化。","hints":["Proxy 回答「要不要调用」，Decorator 回答「调用前后做什么」","Proxy 可以完全拦截请求不转发，Decorator 一定会转发","想清楚意图——是控制访问还是增强功能——再选模式"],"tags":["设计模式","代理模式","装饰器模式","Proxy","Decorator"],"id":1723},{"category":"design_patterns","difficulty":"hard","type":"choice","title":"可扩展数据处理框架的架构模式选择","content":"你正在设计一个数据处理框架，需要支持用户在核心处理流程中插入自定义逻辑（如数据清洗、格式转换、异常检测）。用户可以注册多个插件，插件有执行顺序，且一个插件可以决定是否中断后续流程。以下哪种架构模式组合最适合？","options":["A) 策略模式——每个插件是一个策略，由框架统一调度执行","B) 责任链模式——每个插件是一个链上的处理器，处理器可以选择把请求传给下一个或中断","C) 管道过滤器模式——每个插件是一个过滤器，数据依次流过管道中的每个过滤器","D) 观察者模式——框架发布处理事件，插件作为观察者订阅并按优先级执行"],"answer":"B","hints":["关键需求是「一个插件可以决定是否中断后续流程」——这不是管道过滤器（数据必须流过所有过滤器）能做到的","责任链模式中每个处理器可以决定处理请求、转发或终止"],"tags":["设计模式","责任链模式","管道过滤器","插件架构","框架设计"],"id":1724},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"状态模式与策略模式的区别和选型判断","content":"状态模式（State Pattern）和策略模式（Strategy Pattern）的类图几乎一样——都使用组合 + 接口多态。但两者的目的和适用场景完全不同。请通过一个「订单状态流转」的例子说明状态模式的本质（状态之间的迁移和上下文相关行为切换），对比策略模式（算法独立替换，不关心上下文状态），并总结一段代码出现什么特征时说「这里应该用 State 而不是 Strategy」。","answer":"答案：State 和 Strategy 结构相同但意图相反——State 是「内在状态驱动行为切换」，Strategy 是「外部注入算法替换」。\n\n订单状态流转的例子（State Pattern）：\n\n订单有多个状态：待支付、已支付/待发货、已发货/待收货、已收货/已完成、已取消。每个状态下，相同操作的行为不同：\n- 「取消」操作：待支付状态可以取消（退款），已发货状态不能取消（只能退货）, 已完成状态不能取消。\n- 「支付回调」操作：待支付状态下处理支付结果，已支付状态下不应该再来支付回调。\n\nState Pattern 的实现：\n```\ninterface OrderState {\n    void pay(OrderContext context, PayData data);     // 支付\n    void cancel(OrderContext context);                 // 取消\n    void ship(OrderContext context);                   // 发货\n    void confirmReceive(OrderContext context);         // 确认收货\n}\n\nclass PendingPaymentState implements OrderState {\n    void pay(OrderContext context, PayData data) {\n        // 处理支付逻辑\n        context.setState(new PaidState());  // → 状态迁移到已支付\n    }\n    void cancel(OrderContext context) {\n        // 处理取消逻辑（退款）\n        context.setState(new CancelledState());  // → 状态迁移到已取消\n    }\n    void ship(OrderContext context) {\n        throw new IllegalStateException(\"未支付不能发货\");\n    }\n    // ...\n}\n```\n关键特征：状态对象知道下一个状态是什么（状态迁移逻辑封装在状态类中），Context 根据当前状态委托行为，行为执行后可能切换到新状态。\n\n对比 Strategy Pattern：\n```\ninterface SortStrategy {\n    void sort(int[] data);\n}\nclass QuickSort implements SortStrategy { /* 快速排序算法 */ }\nclass MergeSort implements SortStrategy { /* 归并排序算法 */ }\n\nclass Sorter {\n    private SortStrategy strategy;\n    void setStrategy(SortStrategy strategy) { this.strategy = strategy; }\n    void sort(int[] data) { strategy.sort(data); }\n}\n// 使用方决定用什么策略：sorter.setStrategy(new QuickSort());\n```\n关键特征：策略不关心 Context 的状态，不会改变 Context 的状态，策略之间没有迁移关系。使用方从外部决定用哪个策略。\n\n判断标准——出现以下特征时用 State 而不是 Strategy：\n1）同一个操作在不同「场景/状态」下行为不同——「取消」在待支付时可以，已支付时需要额外扣费，已发货时不能。\n2）行为执行后会改变后续行为——支付后「取消」的行为变了，说明状态发生了迁移。\n3）对象有明确的「状态生命周期」——状态的流转有方向和约束（待支付→已支付→已发货→已完成，不能跳转）。\n4）你发现代码中有大量的 if/switch 判断「当前是什么状态然后做什么」——这是典型的「状态模式引入信号」：`if (state == PENDING) { ... } else if (state == PAID) { ... }`\n\n如果只是「我想让算法可以自由替换，算法之间没有顺序和迁移关系」，用 Strategy。\n如果行为根据对象的内在状态不同，且行为会改变状态，用 State。\n\n解析：一句话总结——Strategy 是「怎么做的选择」（算法替换），State 是「什么时候做什么」（状态驱动的行为切换）。选错的话，用 Strategy 实现订单状态会变成在 Strategy 里维护全量的 if-else 判断，失去了模式的意义。","hints":["State 模式中状态对象知道下一个状态是什么（迁移逻辑在状态类中），Strategy 中策略不知道其他策略的存在","代码中出现大量 switch(state) 说明你应该用 State 模式了","Strategy 是行为选择（怎么做），State 是行为变更（什么时候做什么）"],"tags":["设计模式","状态模式","策略模式","State","Strategy"],"id":1725},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"策略模式与模板方法模式的选择","content":"在你的支付系统中，不同的支付渠道（微信/支付宝/银联）有相同的流程骨架（验证 → 扣款 → 回调 → 通知），但每个步骤的具体实现不同。你会用策略模式还是模板方法模式？为什么？","answer":"答案：优先使用模板方法模式（Template Method Pattern）。因为支付渠道的流程结构是固定的，变化的是每个步骤的实现——这正是模板方法模式的适用场景。\n\n解析：两者的核心区别在于控制反转的方向：\n\n1）模板方法模式——父类定义算法骨架（固定流程），子类重写特定步骤（差异化实现）。控制权在父类。\n适用场景：不同实现共享相同的流程结构，且流程几乎不变。\n你的支付案例：validateOrder() → deductMoney() → handleCallback() → sendNotification() 是固定流程，不同渠道只需要重写各步骤的具体逻辑。\n\n2）策略模式——定义策略接口，客户端选择具体策略注入上下文。控制权在客户端。\n适用场景：算法可以完全互换，且流程可能变化。\n如果不同渠道不仅实现不同，连流程步骤都可能不一样（如某些渠道不需要回调、某些渠道需要额外步骤），策略模式更合适。\n\n扩展延伸：组合使用——抽象工厂 + 模板方法：用抽象工厂创建各支付渠道的策略族，用模板方法定义执行流程。实际项目中，Spring 的 JdbcTemplate、RestTemplate 都是模板方法模式的体现——固定流程（打开连接 → 执行 SQL → 处理结果 → 关闭连接），变化的步骤通过回调接口（Callback）注入，这是一种结合了策略和模板方法的变体。选型建议：如果未来可能新增支付渠道，模板方法模式更易扩展（新增一个子类即可）。如果未来可能改变流程（新增步骤或调整顺序），策略模式更灵活。","hints":["模板方法——「骨架固定、步骤可变」；策略模式——「算法可互换、流程可变化」","如果流程结构未来可能变化，选策略模式；如果结构固定但实现变化，选模板方法"],"tags":["设计模式","策略模式","模板方法","支付"],"id":1726},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"观察者模式的现代替代方案","content":"你的系统需要在用户下单后执行一系列动作（发送邮件、更新库存、通知物流、记录日志）。传统方案是用观察者模式（Observer Pattern）实现事件通知。在现代 Java/Spring 架构中，以下哪种方案更推荐？\n\nA) 继续使用观察者模式——定义一个 Subject 接口，所有观察者注册到 Subject 上\nB) 使用 Spring 的 ApplicationEvent / EventListener 机制实现观察者\nC) 使用消息队列（RabbitMQ/Kafka）解耦所有后置动作\nD) 在一个事务中同步调用所有后置服务","answer":"正确答案：B\n\n解析：Spring 的 ApplicationEvent / @EventListener 本质上就是观察者模式的标准实现，但比手写观察者更好——它利用 IoC 容器自动管理观察者注册，支持异步执行（@Async），且与事务同步（TransactionEventListener）。\n\nA 选项的问题：手写观察者模式在 Spring 生态中属于重复造轮子。Spring 已经提供了完整的实现，包括同步/异步、事务绑定、异常处理。\n\nC 选项的问题：引入消息队列的复杂度远高于当前需求——需要部署和维护消息中间件、处理消息幂等性和顺序问题。消息队列适合跨服务、跨进程的事件驱动架构，不适合单体应用内的服务间通信。\n\nD 选项的问题：在同一个事务中同步调用所有后置服务会导致：1）主流程被后置动作阻塞（用户要等到邮件发送完毕才能看到下单成功）；2）任何一个后置服务失败都会导致主事务回滚（邮件发送失败导致订单取消）。\n\n扩展延伸：实际项目的推荐分层：1）同进程内的后置动作（日志记录、缓存更新）→ 用 Spring Event（同步或异步）；2）同服务但跨聚合的事件（订单完成→通知库存预留）→ 用 Spring Event + @Async 或 @TransactionalEventListener；3）跨服务的事件（订单完成→物流系统发货）→ 用消息队列 + 事件总线。观察者模式的现代演变是从「类级别的硬编码通知」到「框架级别的声明式事件驱动」。勿过度设计——在单一应用内引入消息队列来解决「下单后发邮件」的问题是大炮打蚊子。","hints":["Spring Event 是观察者模式的现代实现——利用 IoC 容器管理观察者注册","同进程内用 Spring Event，跨服务用消息队列——选型取决于是否需要跨服务解耦"],"tags":["设计模式","观察者","Spring","事件驱动"],"id":1727},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"SAGA 模式编排 vs  choreography 选型","content":"在微服务架构中实现分布式事务，你有两个 SAGA 模式的实现选项：编排（Choreography，每个服务发布事件驱动下一个服务）和协编（Orchestration，一个协调器调用各服务）。你在什么场景下选择编排、什么场景下选择协编？","answer":"答案：编排适合流程简单、服务数量少且变化不频繁的场景；协编适合流程复杂、服务多、需要集中管理和监控的场景。\n\n解析：\n\n编排模式（Choreography）：\n- 实现方式：每个服务完成自己的本地事务后发布领域事件，下一个服务监听事件并执行自己的步骤。如果某个步骤失败，各服务发布补偿事件回滚。\n- 优点：松耦合（服务间不知道彼此存在）、架构简单（不需要协调器）、适合事件驱动架构。\n- 缺点：流程逻辑分散在各服务中（难以看到全局流程）、新增步骤需要修改多个服务的监听逻辑、调试困难（事件链分散）。\n- 适合场景：服务 3-5 个且流程稳定、团队对事件驱动有经验、异步最终一致性可接受。\n\n协编模式（Orchestration）：\n- 实现方式：一个 Saga 协调器（Orchestrator）负责告诉每个服务做什么、并在失败时告诉各服务做什么补偿。协调器通常是有状态的服务（存储 Saga 执行状态）。\n- 优点：流程逻辑集中（一个地方看到全貌）、变更影响可控（只改协调器）、易监控和调试（Saga 执行状态可追踪）。\n- 缺点：协调器成为耦合点（需要知道所有服务的接口）、协调器本身是单点（需要高可用）。\n- 适合场景：服务 5 个以上、流程频繁变化、需要严格监控和治理的分布式事务。\n\n扩展延伸：实际项目中的折中方案——混合使用：用编排处理简单分支（发布事件驱动下游），用协编处理核心链路（订单主流程用协调器控制）。工具选型：编排型可选 Axon Framework、Eventuate；协编型可选 Camunda（BPMN 工作流引擎）、Temporal（微服务编排平台）、Seata。编排和协编不是非此即彼，同一个系统里可以两者共存。关键判断：如果你需要「一眼看到整个事务流程」——用协编；如果你更关注服务间的解耦——用编排。","hints":["编排 = 服务间通过事件链式驱动；协编 = 一个协调器集中指挥所有服务","编排更松耦合但流程不透明；协编更集中可控但耦合度高"],"tags":["设计模式","SAGA","分布式事务","微服务","编排"],"id":1728},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"Bridge 模式 vs Adapter 模式区分","content":"你需要为一个消息推送系统做设计。系统需要支持多种消息类型（文本/图片/富媒体）和多种推送渠道（短信/邮件/App Push）。你希望新增消息类型或推送渠道时不影响已有的代码。以下哪个模式组合最适合这个需求？\n\nA) Adapter 模式——为每个渠道写一个适配器\nB) Bridge 模式——将消息抽象和推送实现分离为两个独立的维度\nC) Factory 模式——根据参数创建不同的推送对象\nD) Facade 模式——为推送系统提供一个统一的简化接口","answer":"正确答案：B\n\n解析：这个需求有两个独立变化的维度——消息类型和推送渠道。Bridge 模式专门解决多维度的组合问题：将抽象部分（消息类型）与实现部分（推送渠道）分离，各自可以独立变化和扩展。\n\nA 选项的问题：Adapter 模式是让不兼容的接口能够一起工作（如封装第三方 SDK 的差异接口），它不解决多维组合问题。\n\nC 选项的问题：Factory 模式解决的是对象创建的问题，虽然可以和 Bridge 组合使用（工厂创建具体的 Message + Sender 组合），但不解决维度扩展。\n\nD 选项的问题：Facade 模式是提供简化接口，隐藏子系统复杂性，不解决可变维度的扩展。\n\n扩展延伸：Bridge 模式的典型实现：\n\n```java\n// 实现维度：推送渠道\ninterface MessageSender { void send(String content); }\nclass EmailSender implements MessageSender { ... }\nclass SmsSender implements MessageSender { ... }\n\n// 抽象维度：消息类型\nabstract class Message {\n    protected MessageSender sender;\n    abstract void send();\n}\nclass TextMessage extends Message { ... }\nclass RichMediaMessage extends Message { ... }\n```\n\n新增消息类型 → 新增 Message 子类；新增推送渠道 → 新增 MessageSender 实现类。两者互不干扰。Bridge 模式在跨平台 GUI 框架（不同操作系统 × 不同 UI 组件）和 JDBC 驱动（不同数据库 × 不同 SQL 操作）中都有广泛应用。","hints":["当你有两个独立变化的维度时——用 Bridge 模式","Bridge = 抽象（消息类型）和实现（推送渠道）分离，各自独立变化"],"tags":["设计模式","Bridge","Adapter","架构"],"id":1729},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"Transaction Outbox 模式实现细节","content":"你的订单服务需要在创建订单后向消息队列发送事件。但直接发送 MQ 存在风险——订单创建成功但消息没发出去（网络闪断），或消息发了但事务回滚了（幽灵消息）。Transaction Outbox 模式如何解决这些问题？有哪些实现方式？","answer":"答案：Transaction Outbox 模式将事件先写入数据库（与业务操作在同一事务中），再由独立的进程（Message Relay）异步将事件发布到消息队列。核心思路——「业务操作和事件存储在一个本地事务中，保证两者原子性」。\n\n解析：标准实现流程：\n\n1）订单服务在同一个数据库事务中完成两件事：a) 写入订单表（业务操作）；b) 写入 outbox 表（事件记录，状态=待发送）。\n\n2）一个独立的消息发布进程（可以是单独的微服务或同一个应用的后台线程）定时或通过 CDC（Change Data Capture）扫描 outbox 表。\n\n3）发现状态=待发送的事件时，将其发布到消息队列，发布成功后将状态更新为已发送（或直接删除该记录）。\n\n三种实现方式：\n\n方式一：定时轮询 Polling Publisher——后台线程每 N 秒扫描 outbox 表。实现简单，但存在轮询延迟和重复扫描。\n\n方式二：CDC（Change Data Capture）——使用 Debezium 等工具监听数据库的 binlog（MySQL）/ WAL（PostgreSQL），当 outbox 表有新记录时实时捕获并推送给消息队列。延迟低，不侵入业务代码，适合高吞吐场景。\n\n方式三：Transactional outbox + Kafka Connect——结合 Kafka Connect 的 JDBC Sink Connector 直接读取数据库 outbox 表写入 Kafka。\n\n扩展延伸：注意事项：1）幂等性——消息可能重复发送（Publisher 在发送成功后崩溃，重新扫描时又重新发送了一次），消费者需要做幂等处理。2）event_id 唯一索引——outbox 表的事件 ID 使用唯一索引或 UUID，防止重复插入。3）清理过期数据——定期清理已发送的 outbox 记录（保留 3-7 天用于排查）。4）消息顺序——如果需要消息的严格顺序（FIFO），单分区 + 单线程 Publisher + 事件按序写入。Transaction Outbox 模式是目前业界解决「本地事务 + 消息发送一致性」的标准方案，在订单、支付、财务等场景中被广泛使用。","hints":["核心思想——业务操作和事件存储放在同一个本地事务中","CDC（如 Debezium）是比定时轮询更优雅的实现方式"],"tags":["设计模式","Outbox","消息","一致性","微服务"],"id":1730},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"解释器模式在 DSL 设计中的应用","content":"请详细解释解释器模式（Interpreter Pattern）的核心结构和适用场景。解释器模式适合解决什么样的问题？它与 Visitor 模式有什么区别和联系？结合 Spring SpEL（Spring Expression Language）或 SQL 解析的场景，说明解释器模式的具体设计方式以及它的局限性（性能问题、文法复杂度爆炸）。","answer":"答案：解释器模式（Interpreter Pattern）为语言文法定义表达式类层次结构，每个文法规则对应一个表达式类，通过递归组合解析语法树。\n\n解析：解释器模式的结构包括 AbstractExpression（抽象表达式，定义 interpret 方法）、TerminalExpression（终结符表达式——文法中的最小单元）、NonterminalExpression（非终结符表达式——由子表达式组合而成）、Context（上下文——存储解析过程中的全局状态）。\n\n经典示例：四则运算解析。文法：expression ::= number | expression '+' expression | expression '-' expression | expression '*' expression | expression '/' expression。每个规则映射为一个类：NumberExpression（终结符）、AddExpression/SubtractExpression/MultiplyExpression/DivideExpression（非终结符）。`3 + 5 * 2` 解析为 AddExpression(Number(3), MultiplyExpression(Number(5), Number(2)))。\n\n与 Visitor 模式的对比：Visitor 模式在固定的类结构上增加新的操作（操作易扩展，结构难扩展），Interpreter 模式在固定的操作上增加新的表达式（结构易扩展，操作难扩展）。实际中两者经常组合——用 Interpreter 解析文法构建 AST，用 Visitor 遍历 AST 执行语义分析。\n\n适用场景：1）DSL 解析（Spring SpEL、JPQL、Liquibase 的变更日志 DSL）——文法简单且变化不频繁时非常适合；2）配置文件解析（如正则表达式引擎）；3）计算公式引擎（如业务规则引擎中的公式求值）。\n\n局限性：1）文法规则多时类数量爆炸——每个规则一个类；2）性能问题——递归解释深度大时效率低（大量虚函数调用和对象创建）；3）维护困难——文法复杂后解释器代码极难调试。\n\n实际建议：简单的 DSL 用 Interpreter 模式清晰可控；复杂的语言解析请直接用 ANTLR/PEG 解析器生成器，不要手写解释器模式。","hints":["Interpreter 模式和 Visitor 模式在 AST 处理中如何分工——Interpreter 建树、Visitor 遍历","解释器模式的性能瓶颈在哪里——递归深度大时的对象创建开销和虚函数调用"],"tags":["解释器模式","Interpreter","DSL","设计模式"],"id":1731},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"注册模式（Registry）的设计与实战","content":"请解释注册模式的设计目的和实现方式。注册模式和享元模式（Flyweight）有什么关联和区别？与 Service Locator 有什么区别？结合 MyBatis 的 TypeAliasRegistry 或 Spring 的 Singleton Bean 注册表，说明注册模式的实际应用。","answer":"答案：注册模式（Registry）提供一个全局的、集中式的对象存储和查找机制，允许通过键（Key）快速获取共享实例。核心功能是「登记 + 查找」。\n\n解析：注册模式的核心结构非常简单——一个全局 Map，key 为标识（字符串/Class/枚举），value 为共享对象实例。\n\n```java\npublic class ServiceRegistry {\n    private final Map<String, Object> services = new ConcurrentHashMap<>();\n    public void register(String name, Object service) { services.put(name, service); }\n    @SuppressWarnings(\"unchecked\")\n    public <T> T get(String name) { return (T) services.get(name); }\n}\n```\n\n与享元模式的关系：Registry 是实现享元模式的核心基础设施。享元模式通过 Registry 管理共享的享元对象池，确保相同的外部状态对象不被重复创建。区别：Registry 是一种结构型/创建型模式（专注于存储和查找），Flyweight 是一种优化模式（专注于内存节省）。\n\n与 Service Locator 的区别：Service Locator 是 Registry 的演进版——除了查询功能外还封装了创建逻辑（如果找不到则创建并注册）。Registry 被动接收注册，Service Locator 主动管理生命周期。\n\n实战案例：1）MyBatis TypeAliasRegistry——registerAlias(\"integer\", Integer.class) 将类型别名注册到 Map；2）Spring 的 DefaultSingletonBeanRegistry——三级缓存解决循环依赖；3）枚举 Registry——CommandRegistry 注册命令处理器，避免庞大的 switch-case。\n\n设计考量：1）Registry 本质上是全局状态（有点像全局变量），过度使用会破坏依赖清晰度；2）线程安全——ConcurrentHashMap 是标配；3）初始化时机——静态初始化块 / Spring @PostConstruct。","hints":["Registry 和 Service Locator 的区别——Registry 只管查询，Service Locator 还能自动创建","Registry 的全局状态问题是它的最大缺点——依赖关系变得隐式"],"tags":["注册模式","Registry","Service Locator","设计模式"],"id":1732},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"备忘录模式与事务回滚设计","content":"请深入解释备忘录模式（Memento Pattern）的设计结构和应用场景。备忘录模式如何实现「不破坏封装性的前提下保存和恢复对象内部状态」？它和命令模式（Command Pattern）在实现撤销（Undo）时的分工合作是怎样的？在数据库事务回滚或分布式 Saga 补偿事务中，如何借鉴备忘录模式的设计思想？","answer":"答案：备忘录模式在不暴露对象内部实现的前提下捕获并外部化其内部状态，以便之后可以将对象恢复到之前的状态。\n\n解析：三角色结构——1）Originator（原发器）：需要保存状态的对象，负责创建 Memento 和从 Memento 恢复状态；2）Memento（备忘录）：存储 Originator 内部状态的不透明对象（对外只暴露窄接口，对 Originator 暴露宽接口）；3）Caretaker（管理者）：负责保存 Memento 但不操作其内容。\n\n```java\n// 窄接口——对外部可见\npublic interface Memento {}\n// 宽接口——仅 Originator 使用\nclass ConcreteMemento implements Memento {\n    private final String state;\n    ConcreteMemento(String state) { this.state = state; }\n    String getState() { return state; }\n}\nclass Originator {\n    private String state;\n    public Memento save() { return new ConcreteMemento(state); }\n    public void restore(Memento m) { state = ((ConcreteMemento) m).getState(); }\n}\n```\n\n与命令模式的协作：Command 模式存储「操作」，Memento 模式存储「状态」。实现支持撤销的命令时，Command 在执行前调用 Originator.save() 保存状态到 Memento，撤销时调用 Originator.restore(memento) 恢复。Caretaker 维护一个 Memento 栈实现多级撤销。\n\n在事务回滚中的应用：1）本地事务——数据库的 Undo Log 本质上是 Memento 模式的实现（保存修改前的数据映像）；2）Saga 补偿事务——每个 Saga 步骤记录补偿操作（State Memento），失败时从 Memento 栈中依次执行逆操作。\n\n设计考量：1）Memento 可能很大（如包含整个对象图的快照）——可用差异存储（只保存变化的部分）或增量快照优化；2）封装性——Java 中用包级访问权限或内部类确保只有 Originator 能访问 Memento 内部状态。","hints":["Memento 的「宽窄接口」设计如何保证封装——Originator 能读写，Caretaker 只能存","数据库 Undo Log 是备忘录模式的思想体现——记录修改前的状态用于回滚"],"tags":["备忘录模式","Memento","事务回滚","Undo","设计模式"],"id":1733},{"category":"design_patterns","difficulty":"medium","type":"short_answer","title":"惰性初始化模式（Lazy Initialization）","content":"请详细解释惰性初始化（Lazy Initialization Pattern）的核心设计思想、实现方式以及适用场景。延迟初始化和单例模式结合时需要注意什么问题？在 Spring 中 @Lazy 注解的工作原理是什么？MyBatis 的延迟加载（Lazy Loading）是如何实现的？","answer":"答案：惰性初始化模式将对象的创建从「类加载时」推迟到「首次使用时」，以节省资源、减少启动时间。核心思想是将创建开销从初始化阶段平摊到首次使用时刻。\n\n解析：基本实现方式——1）简单延迟初始化（非线程安全）：判断引用是否为 null，为 null 则创建；2）同步延迟初始化（synchronized）：线程安全但每次访问都有锁开销；3）双重检查锁定（Double-Checked Locking）；4）Holder 模式（Class Holder Idiom）——利用 JVM 类加载机制，静态内部类在首次被引用时才加载和初始化。\n\n```java\npublic class LazySingleton {\n    private LazySingleton() {}\n    private static class Holder {\n        static final LazySingleton INSTANCE = new LazySingleton();\n    }\n    public static LazySingleton getInstance() {\n        return Holder.INSTANCE;\n    }\n}\n```\n\n与单例模式结合的问题：饿汉式单例在类加载时立即创建实例，即使从未使用。懒汉式单例（延迟初始化 + 单例）可以避免这种浪费，但必须处理线程安全。建议使用 Holder 模式或枚举类实现线程安全的懒加载单例。\n\nSpring @Lazy 注解：1）作用于 @Bean 时——Bean 不在容器启动时创建，在首次注入时才创建；2）作用于 @Configuration 时——该配置类中所有 @Bean 都延迟初始化；3）作用于 @Component 时——该组件延迟初始化；4）作用于 @Autowired/@Inject 时——创建代理对象注入，首次调用代理时才真正解析依赖。底层通过 CglibSubclassingInstantiationStrategy + LazyResolutionProxyFactoryBean 实现。\n\nMyBatis 延迟加载：配置 lazyLoadingEnabled=true 后，MyBatis 为关联对象（如 association/collection）创建 Javassist 或 Cglib 代理对象。首次访问关联属性时，代理触发 SQL 查询填充实际对象。\n\n注意事项：1）延迟初始化不适合「首次访问延迟敏感」的场景（如在请求处理路径中触发耗时的初始化）；2）内存泄漏风险——延迟创建的对象持有上下文引用可能导致 GC 无法回收。","hints":["Holder 模式为什么既是线程安全的又能延迟初始化——JVM 保证静态内部类的类加载线程安全","Spring @Lazy 创建的代理对象在首次调用时才解析依赖，这是如何实现的"],"tags":["惰性初始化","Lazy Initialization","性能优化","多线程"],"id":1734},{"category":"design_patterns","difficulty":"hard","type":"short_answer","title":"双重检查锁定（DCL）的正确实现与陷阱","content":"双重检查锁定（Double-Checked Locking, DCL）是一种在延迟初始化场景中减少锁竞争的优化手段。为什么在 Java 中 DCL 的「经典写法」是错误的？volatile 关键字如何修复 DCL？在 JDK 9+ 中 VarHandle 对 DCL 有什么影响？为什么 Holder 模式和枚举单例通常比 DCL 更推荐？","answer":"答案：经典 DCL 写法 `if (instance == null) { synchronized(this) { if (instance == null) { instance = new Instance(); } } }` 在 Java 5 之前是错的，问题根源在于「指令重排序」。\n\n解析：`instance = new Instance()` 在 JVM 中分解为三个步骤：1）分配内存空间（allocate）；2）调用构造方法初始化对象（initialize）；3）将引用指向分配的内存地址（assign）。在不做特殊处理的情况下，JVM 可能将步骤 2 和 3 重排序——另一个线程在第 3 步完成后、第 2 步完成前获取到引用，读到的是「部分构造的对象」（部分字段为默认值而非构造方法设置的值）。\n\n修复方案：将 instance 声明为 volatile。Java 5 之后 volatile 语义增强，volatile 写操作会插入 Store-Store 和 Store-Load 屏障，禁止步骤 2 和 3 的重排序。同时 volatile 保证可见性——一个线程修改后其他线程立即可见。\n\n```java\npublic class CorrectDCL {\n    private static volatile CorrectDCL instance;\n    public static CorrectDCL getInstance() {\n        CorrectDCL result = instance;\n        if (result == null) {\n            synchronized (CorrectDCL.class) {\n                result = instance;\n                if (result == null) {\n                    instance = result = new CorrectDCL();\n                }\n            }\n        }\n        return result;\n    }\n}\n```\n注意：方法内将 instance 赋值给局部变量 result 可以避免每次访问都读 volatile（减少约 25% 的 volatile 读开销）。\n\nJDK 9+ VarHandle：VarHandle 提供了更底层的语义——MethodHandles.lookup().findStaticVarHandle(X.class, \"instance\", X.class).getAcquire() 和 .setRelease(o) 可以替代 volatile 实现更细粒度的内存排序语义。\n\n为什么 Holder 模式和枚举单例更推荐：1）Holder 模式利用 JVM 类加载的线程安全机制——<clinit> 在类首次被引用时由 JVM 保证线程安全地执行；2）枚举单例（enum Singleton { INSTANCE }）——JVM 保证枚举实例的线程安全创建，天然防止反射攻击和序列化破坏。DCL 代码复杂、容易出错，除非需要精细控制初始化时机，否则优先选 Holder 或枚举。","hints":["DCL 问题的本质是「指令重排序」——构造方法在引用赋值之后才执行","volatile 如何修复 DCL——Java 5 之后 volatile 写禁止与之前的普通写重排序"],"tags":["双重检查锁定","DCL","并发","JMM","volatile"],"id":1735}];
